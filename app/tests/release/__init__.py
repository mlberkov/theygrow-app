"""RSN-P2 — tests for the release signature detector.

The directory carries an `__init__.py` so mypy resolves it as the package
`release`; without it, its modules would collide with the other `app/tests`
suites as duplicate top-level modules, for the same reason `app/tests/schema`
and `app/tests/export` carry theirs.
"""
