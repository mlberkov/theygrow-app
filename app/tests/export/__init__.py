# The export-contour test package (L1-P3).
#
# A package rather than a bare directory for the same reason app/tests/schema is
# one: mypy resolves it as `export` and cannot collide it with `api/tests`.
