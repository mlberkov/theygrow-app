"""Provider adapters — concrete implementations behind the ``ports`` seams.

These are out-of-perimeter *provider* clients (e.g. the cloud embedder), NOT the
``diary-memory-service`` engine, which stays a code/data donor only (ADR-005).
"""
