"""Domain logic lifted from the engine (``memory_rag.core.domain``; ADR-005 §7).

Code donor only — the engine stays OUT of the live perimeter (AGENTS.md §3 /
ADR-005). These modules are transferred (not imported) and carry no network or
provider dependency.
"""
