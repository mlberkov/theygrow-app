"""Retrieval-layer services (M4-P3+).

Orchestration that composes the library legs (``retrieval/search_repository.py``) into a
single fused candidate list. No HTTP surface; consumed by tests now and the P4 grounded-ask
assembly later.
"""
