# Enforced invariants — theygrow-app

## Bar

`INVARIANTS.md` is **enforced-only**. An invariant goes here only when a packet enforces it in code — a test, a lint rule, a schema constraint, a runtime guard, or a CI gate. Intent and runtime contract do **not** live here:

- **Intent-level invariants** (what the contract says we will do) live in `AGENTS.md` §2 and §3.
- **Documented runtime behavioral contract** (what the running system must do, before code lands) lives in [`RUNTIME-INVARIANTS.md`](RUNTIME-INVARIANTS.md).

Keeping this file enforced-only prevents it from drifting into a wish list.

## Invariants

### M1-P3-INV-001 — No secrets committed

- **Statement.** No secret or credential material is committed to the repository.
- **Enforced by.** `.pre-commit-config.yaml` (gitleaks hook) and `.github/workflows/ci.yml` (runs the gitleaks hook via `pre-commit run --all-files`, with full history fetched).
- **Landed in.** M1-P3.
- **Scope.** Covers tracked content and commit history reachable in CI. Public, non-secret identifiers (e.g. the GA4 Measurement ID, the GCP project id / region) are not secrets; should the scanner ever flag one, the remedy is an exact non-secret allowlist entry in a `.gitleaks.toml` at the repository root. No such file exists today and nothing has been allowlisted — the hook runs on its stock ruleset. Genuine secrets are never allowlisted.

### M1-P3-INV-002 — Contract integrity

- **Statement.** Within the contract + spine documents, three conditions hold: (a) superseded stack names do not reappear as active targets; (b) the legacy pre-rename repository directory name does not appear; (c) live-infra identifiers stay confined to `docs/RUNBOOK.md`.
- **Enforced by.** `scripts/check-contract-integrity.sh`, wired into `.pre-commit-config.yaml` (local hook) and `.github/workflows/ci.yml`. The script holds the authoritative match patterns for all three checks; they are intentionally not restated inline here, since this file is itself within the scanned corpus.
- **Landed in.** M1-P3.
- **Scope.** Governs the contract + spine corpus: `AGENTS.md`, `CLAUDE.md`, `docs/INVARIANTS.md`, `docs/RUNTIME-INVARIANTS.md`, `docs/execution-map.md`, `docs/RUNBOOK.md`, `docs/product/BuildPlan.md`, `docs/product/TechSpec.md`, `.cursor/rules/masterplan.mdc`. Live-deploy paths are out of scope — they carry live-infra identifiers as operational reality. Historical artifacts (`docs/decision-log.md`, `data/mvp_masterplan.md`) are exempt — they record the superseded plan by design. This promotes the previously-manual P1/P2 negative-checks into an enforced gate. P4 tightened check (b) to a strict ban by removing its interim pairing exception once the residual naming sweep was complete.

### M2-P2-INV-001 — No child PII in logs / telemetry (forward guard)

- **Statement.** No known child-PII field (child name, diary text, birthdate / dob) is emitted through the logging boundary: known PII field names carried on a log record are redacted to `[REDACTED]` before any handler formats the record.
- **Enforced by.** `api/theygrow_api/logging.py` (`PiiRedactionFilter`) + `api/tests/test_logging_redaction.py`, run by `mypy api` + `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M2-P2.
- **Scope.** A **forward guard**: it establishes telemetry/redaction discipline from the first byte of backend code (AGENTS.md §4). At P2 there is **no live child-PII data path** — those arrive at M3 — so this covers the logging-boundary redaction *mechanism* and its test, not end-to-end coverage over live child data; coverage extends as data paths land (M3 → M5). It governs known structured PII field names only, not free-text message bodies.

### M2-P2-INV-002 — `/api` is type- and test-gated

- **Statement.** All Python under `api/` is type-checked (mypy strict) and unit-tested (pytest) on every CI run; a failure blocks the gate.
- **Enforced by.** `.github/workflows/ci.yml` (installs `./api[dev]`, runs `mypy api` + `pytest api`) and `.pre-commit-config.yaml` (mypy hook with `additional_dependencies`, checking `api/`).
- **Landed in.** M2-P2.
- **Scope.** Covers the `api/` subtree. Replaces the M1 zero-Python mypy guard with unconditional teeth now that `/api` exists. Does not cover `/app` (static PWA, no Python).

### M4-P1-INV-001 — Sparse FTS leg is index-backed

- **Statement.** The sparse (lexical) retrieval leg matches against a generated, DB-maintained `tsvector` (`event_chunks.chunk_text_tsv`, `to_tsvector('simple', chunk_text)`) backed by a GIN index, and is community-scoped — a query never crosses community boundaries.
- **Enforced by.** `api/alembic/versions/0002_notes_event_chunks.py` (the GENERATED STORED column + `idx_event_chunks_chunk_text_tsv` GIN index) + `api/tests/test_search_repository.py` (match, `ts_rank_cd` ordering, community scoping, inclusive `note_date` range, empty-query / non-positive-limit short-circuits), run by `mypy api` + `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P1.
- **Scope.** Covers the sparse leg's **mechanics** only. It does **not** establish Russian lexical recall adequacy: the `'simple'` config does no morphological stemming (the documented limitation and named port-out trigger, `M4-DL-001`); recall is measured at the M4-close mini-eval, not here. The dense leg, RRF fusion, and the episodic-eligibility filter are out of scope (M4-P2/P3).

### M4-P1-INV-002 — Re-derivation is idempotent

- **Statement.** Re-running the offline `notes` / `event_chunks` re-derivation over the same `source_messages` converges to the same derived rows — no duplication and no drift (deterministic ids + delete-then-insert over the processed source ids).
- **Enforced by.** `api/theygrow_api/derivation.py` (deterministic `note_id` / `chunk_id`; delete-then-insert) + `api/tests/test_rederive.py` (`test_rederive_is_idempotent`, plus the derived-layer / fallback / `valid_at`-recovery / route-scoping cases), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P1.
- **Scope.** Covers the offline re-derivation pass over already-imported live `{note, draft}` rows. Embeddings are out of scope (M4-P2); the pass writes none.

### M4-P1-INV-003 — Operational signals are §4-safe

- **Statement.** Every emitted operational signal carries only counts / ids / timings — never child diary text (`raw_text` / `chunk_text`) nor family-identifying ids (`community_id`). Signals emit through the single `SignalSink` seam, whose default implementation routes through the PII-guarded logging boundary.
- **Enforced by.** `api/theygrow_api/signals.py` (typed `Signal.fields()` payloads + `LoggingSignalSink` over the `logging.py` boundary) + `api/tests/test_signals.py` (asserts payloads exclude the §4 field set and are numeric) + the emission assertions in `api/tests/test_rederive.py` / `api/tests/test_search_repository.py`, run by `mypy api` + `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P1.
- **Scope.** Covers the P1-emitted signals (derivation counters; sparse candidate count + latency). Downstream kinds (`grounding.coverage`, `degradation.event`) are defined in the taxonomy but not emitted until their producing code lands (P3/P4); the §4 payload constraint binds them when they do.

### M4-P2-INV-001 — Per-chunk embedding is dimension-pinned and drift-guarded

- **Statement.** The dense leg's vector is per-chunk on `event_chunks.embedding`, typed `vector(1536)` as a frozen literal in the migration DDL; the schema-bound `embedding_dimension` surface value is linked to the live column type by a drift guard (a surface bump without a matching migration fails). The M3 dormant per-`source_message` `embedding` shell is dropped (per-chunk granularity is final).
- **Enforced by.** `api/alembic/versions/0003_event_chunk_embeddings.py` (the `vector(1536)` column + the dropped shell) + `api/tests/test_parameters.py` (`test_embedding_dimension_surface_matches_frozen_schema_ddl`, reading `format_type` over the live column) + `api/tests/test_embedding_schema.py` and `api/tests/test_source_message_schema.py` (`test_source_messages_has_no_embedding_column`), run by `mypy api` + `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P2.
- **Scope.** Covers the per-chunk vector's storage shape, the surface↔schema dimension link, and the dropped shell. It does not cover retrieval quality or the dense leg's ranking (RRF is M4-P3).

### M4-P2-INV-002 — Embeddings backfill is fail-closed without the ZDR clearance gate

- **Statement.** The offline embeddings backfill sends no `chunk_text` **out of the perimeter** and writes nothing unless the operator-set `embedder_privacy_cleared` flag (the ZDR + DPA + EU-residency clearance) is set — and, on the real path, the embedder endpoint/key are present. With the gate unmet the run refuses before any provider call or DB write (zero provider calls, zero writes). The gate is scoped to **egress**: it applies to the real provider path and to any injected provider that does not structurally declare zero egress, and is skipped only for an in-perimeter provider (`performs_no_egress is True`), which removes the third-party surface rather than satisfying its clearance. That check is itself fail-closed — an absent or non-`True` declaration reads as egressing — so the narrowing cannot become a bypass by omission.
- **Enforced by.** `api/theygrow_api/embeddings_backfill.py` (`_ensure_embedder_cleared` / `_build_provider`, raising `EmbedderNotReady` before any text egress) + `api/theygrow_api/ports/provider.py` (`performs_no_egress`, the fail-closed declaration predicate — private to the backfill until A2-P3 hoisted it beside the two ports it now serves) + `api/theygrow_api/config.py` (the default-false `embedder_privacy_cleared` flag) + `api/tests/test_embeddings_backfill.py` (`test_fail_closed_when_uncleared`, `test_fail_closed_when_unconfigured`) + `api/tests/test_seed_staging.py` (`test_unmarked_provider_still_gated_when_uncleared`, `test_truthy_declaration_is_not_enough`), run by `mypy api` + `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P2 (statement amended A2-P2: the gate restated as egress-scoped, with the in-perimeter exemption and its fail-closed default — `L2-DL-002`; amended A2-P3: the declaration predicate moved to `ports/provider.py`, and the same egress scoping was extended to the READ path — `L2-DL-003`, `L2-P3-INV-001`).
- **Scope.** A structural §4 gate over the one permitted child-text egress (ADR-011 §1). It governs the offline backfill path; the read path's query-embedding gate is the same mechanism and is stated at `L2-P3-INV-001` (there is still no live *request* path that embeds — chat is M5). It binds the gate mechanism, not the external provider's actual ZDR posture (an operational precondition the owner clears). Since A2-P2 it also does not bind what an in-perimeter provider computes — only that one which claims the exemption has declared it structurally; that a declaring provider genuinely performs no I/O is a property of that provider's code, not something this gate can verify.

### M4-P2-INV-003 — Embeddings backfill is idempotent and index-after-population

- **Statement.** The backfill embeds only `pending` (and, unless `--no-retry-failed`, `failed`) chunks and skips `ready` ones, so a re-run over a fully-embedded corpus is a no-op (no duplicate writes, no extra provider calls); a provider error marks the batch `failed` for retry, never a partial vector. The HNSW index is built by the backfill AFTER bulk population, not by the migration.
- **Enforced by.** `api/theygrow_api/embeddings_backfill.py` (status-driven selection; `CREATE INDEX IF NOT EXISTS` after population) + `api/tests/test_embeddings_backfill.py` (`test_backfill_is_idempotent`, `test_backfill_marks_failed_then_retries`, `test_backfill_builds_hnsw_index_after_population`) + `api/tests/test_embedding_schema.py` (`test_migration_alone_builds_no_hnsw_index`), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P2.
- **Scope.** Covers the offline backfill pass over already-derived chunks. Embedding quality / vector correctness beyond dimension is out of scope; the injected fake provider exercises mechanics, not a live embedder.

### M4-P3-INV-001 — Episodic eligibility is note-only, enforced in both retrieval legs

- **Statement.** Only chunks whose `source_messages.detected_route='note'` are retrievable; `draft` chunks — though embedded and indexed — are dropped at retrieval by a hard FK-join WHERE in BOTH the sparse and dense legs, so the fused result inherits note-only by construction (ADR-012, fork b). The filter is binary and always-on (not a tier/weight knob); `detected_route` stays on `source_messages` (not denormalized).
- **Enforced by.** `api/theygrow_api/retrieval/search_repository.py` (the `detected_route='note'` join on both `sparse_candidates` and `dense_candidates`) + `api/theygrow_api/services/retrieval.py` (`retrieve` fuses only the filtered legs) + `api/tests/test_search_repository.py` (`test_both_legs_exclude_draft_route`) + `api/tests/test_retrieval.py` (`test_retrieve_excludes_draft_end_to_end`), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P3.
- **Scope.** Covers the retrieval-layer eligibility filter (which chunks may surface). It does not cover grounded-ask assembly or the "no verified info" honest-degradation message (P4), nor retrieval ranking quality.

### M4-P3-INV-002 — Every config-surface parameter carries value provenance

- **Statement.** Every `Parameter` rendered by `current_parameters()` carries a non-empty `changed_in` decision-log id, so the parameters-as-data surface is fully traceable — a knob added without provenance fails the gate (ADR-013 operability; closes OQ#1, structural half).
- **Enforced by.** `api/theygrow_api/parameters.py` (each `Parameter` sets `changed_in`) + `api/tests/test_parameters.py` (`test_every_parameter_carries_nonempty_changed_in`), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P3.
- **Scope.** Covers the presence + non-emptiness of `changed_in` on every surface parameter. It does not assert the id resolves to a real decision-log entry, nor validate the `scope` classification.

### M4-P3-INV-003 — Emitted signal kinds are wired to real producers

- **Statement.** The set of `SignalKind` declared `emitted_now=True` in the taxonomy equals the set actually emitted when the known producers run — so flipping a defined-not-emitted kind (`GROUNDING_COVERAGE` / `DEGRADATION_EVENT`) to `emitted_now=True` without a producer fails the gate, and a new emitted producer must register here (ADR-013 operability; closes OQ#1, structural half).
- **Enforced by.** `api/theygrow_api/signals.py` (the `SIGNAL_TAXONOMY` `emitted_now` flags) + `api/tests/test_signal_emitters.py` (`test_every_emitted_now_kind_has_a_wired_producer`, driving `rederive` / the sparse leg / `embed_backfill` / `query_service.answer_query` through a recording sink and comparing kind-sets; `test_no_defined_not_emitted_kinds_remain_as_of_p4`), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P3 (extended M4-P4: the two P4 kinds `GROUNDING_COVERAGE` + `DEGRADATION_EVENT` were flipped to `emitted_now=True`, so the test now REQUIRES their real emission via the `answer_query` driver).
- **Scope.** The explicit-coupling form: the test enumerates the known producers and asserts the emitted-kind set matches the `emitted_now=True` set. It guarantees no `emitted_now` kind is unproduced and no enumerated producer emits an undeclared kind; it does not reflectively discover producers not listed in the test (adding one requires extending the driver).

### M4-P4-INV-001 — Answer synthesis is fail-closed without the answers ZDR clearance gate

- **Statement.** The grounded-ask service sends family context to the answers LLM and synthesizes an answer ONLY when `answers_privacy_cleared` is set; uncleared (or endpoint/key missing) it raises `AnswersNotReady` before any provider call or text egress, making ZERO answers-provider calls (ADR-014, per-egress clearance; the answers LLM is a distinct residency surface from the embedder, so the embedder's clearance does not clear it). Since A2-P3 the gate is scoped to **egress**: it is asked of the real provider path and of any injected provider that does not structurally declare `performs_no_egress is True`, and is skipped only for an in-perimeter answers provider, which removes the surface rather than satisfying its clearance. That check is fail-closed — an absent or non-`True` declaration reads as egressing. **The narrowing grants no clearance**: `answers_privacy_cleared` stays default-false, the real path is unchanged, and owner clearance for the answers residency surface remains an open prerequisite.
- **Enforced by.** `api/theygrow_api/services/query_service.py` (`_ensure_answers_cleared` runs first, before `retrieve`/build/call) + `api/tests/test_query_service.py` (`test_answers_fail_closed_when_uncleared_zero_calls`, asserting `AnswersNotReady` and zero recorded provider calls), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** M4-P4 (statement and scope amended A2-P3: the gate restated as egress-scoped, with the in-perimeter exemption and its fail-closed default; the narrowing grants no clearance — `L2-DL-003`, `L2-P3-INV-001`).
- **Scope.** Covers the answers/chat egress only (the second §4 surface); the query-embedding egress stays gated by `embedder_privacy_cleared` inside `retrieve()` (`L2-P3-INV-001` / ADR-011). It is a structural refusal (loud no-op), not a content degradation. Since A2-P3 it also does not bind what an in-perimeter answers provider computes — only that one claiming the exemption declared it structurally; that a declaring provider genuinely opens no socket is a property of that provider's code, not something this gate can verify.

### M4-P4-INV-002 — Grounded answers are closed-corpus or honestly degraded

- **Statement.** A non-`None` `answer_text` is produced ONLY from retrieved + cited family episodic memory — never a parametric/model or web fallback. Three independent edges enforce this: the prompt carries only retrieved `chunk_text`; the parser rejects any `cited_chunk_id` absent from the assembled context (a fabricated citation → suppressed `parse_failure`); and a pre-provider grounding gate (`grounding_min_segments`) returns an honest `no_evidence` result with ZERO provider calls below the bar. A model-declared `no_evidence` (present-but-irrelevant context) also suppresses the answer.
- **Enforced by.** `api/theygrow_api/services/context_assembler.py` (`parse_structured_answer` citation grounding) + `api/theygrow_api/services/query_service.py` (the grounding gate + degradation contours) + `api/tests/test_query_service.py` (`test_fabricated_citation_is_parse_failure`, `test_no_context_degrades_with_zero_answers_calls`, `test_present_but_irrelevant_context_llm_declares_no_evidence`), run by `pytest api`.
- **Landed in.** M4-P4.
- **Scope.** Family episodic memory is the only grounded source at M4 (canon/KB is M5, out of perimeter), so provenance is family-observation lineage only. Grounded-but-uncertain answers are RETURNED with an honesty flag (ADR-015), not suppressed — they remain closed-corpus (cited retrieved context). These are deterministic seam-mechanics guarantees with injected fakes; real retrieval recall / grounding QUALITY is the M4-CLOSE eval, not enforced here.

### VDK-P3-INV-001 — App consumes the KB only via the vendored versioned artifact

- **Statement.** The app consumes the domain KB exclusively via the vendored, versioned artifact `app/kb-v{N}.json` under the KB-artifact contract: the in-memory shape is produced by the app-side adapter (`adaptNewDataFormat`) with additive-evolution tolerance, there is no code import from `theygrow-domain-kb`, and the artifact is **byte-as-published** — the app never edits KB data (fixes are producer-side and arrive as a new artifact version). *(Namespace note: id mirrors the `VDK-P{k}` packet namespace of the M(В)-1 vendor-domain-kb-artifact track, owner-decided 2026-07-04 — first non-`M{N}` INV namespace; the "Entry format" footer below is intentionally left unedited, matching how the decision-log treats its non-spine namespaces.)*
- **Enforced by.** `scripts/sync-kb-artifact.sh` (tag-anchored vendoring from the domain-kb release tag `kb-v{N}`, byte-identical; verifies the artifact exists in the tag and `"kb_version"` equals the filename `N`) + `.pre-commit-config.yaml` (`trailing-whitespace` / `end-of-file-fixer` excludes for `app/kb-v1\.json` — the rewriting hooks never touch the artifact, while `check-json` validates it read-only on every commit).
- **Landed in.** VDK-P2 (vendoring mechanism + byte-as-published excludes); VDK-P3 (exclusivity — the inline data source was deleted from `app/index.html`).
- **Scope.** Covers the vendoring-seam **mechanics**: byte-identity to the published artifact, the version-tag anchor, `kb_version`-matches-filename, and hooks-never-rewrite. The *exclusivity* half is structural since VDK-P3 (no inline data source remains to silently regress to) but not machine-gated — no CI check greps `index.html` for re-inlined data. KB data **quality** (including the two frozen-174 curation defects riding along byte-as-published) is producer-side (`theygrow-domain-kb`), explicitly not covered.

### A1-P1-INV-001 — The spa-split refactor is gated by a three-level parity suite

- **Statement.** Every A1 `spa-split` packet must pass an automated three-level parity threshold against committed baselines of the pre-split app: **(a)** DOM-snapshot equality for `header`, `#mainTable` (head, body digest, and full-body hash), `footer.control-footer`, `#zpdEmptyState` and the four modals across four boot states at two viewports, plus a SHA-256 per skill-modal body for all 174 skills; **(b)** pixel-equal screenshots of those surfaces at 1280×800 and 412×760, captured on a platform fixed by a pinned browser container rather than by the CI runner image; **(c)** behavioural smoke over the main tick → ZPD-recompute → persist → reload loop, the no-profile honest-degradation refusal, the activity→skill deep link and modal stack, service-worker registration / offline boot from precache / the PWA update flow, and the kb-load error path. Baselines are never written implicitly: `updateSnapshots: 'none'` makes a missing or changed baseline fail, and only an explicit `--update-snapshots` run may rewrite one. *(Namespace note: id follows the `A1-P{k}` packet namespace of the spa-split milestone, mirroring the `VDK-P3-INV-001` precedent for a non-`M{N}` namespace; the "Entry format" footer below is intentionally left unedited, matching how the decision-log treats its non-spine namespaces.)*
- **Enforced by.** `app/tests/dom-parity.spec.js`, `app/tests/visual.spec.js`, `app/tests/behavior.spec.js` and `app/tests/delivery-contract.spec.js` against the committed baselines in `app/tests/__baselines__/`, configured by `app/playwright.config.js` (`updateSnapshots: 'none'`, `retries: 0`) and run by `scripts/parity-suite.sh` — invoked as the `parity` job in `.github/workflows/ci.yml`. Determinism is held by a pinned clock (`app/tests/support/seed.js`, `page.clock.setFixedTime`) and a pinned browser container; `app/tests/delivery-contract.spec.js` additionally parses `app/nginx.conf` and fails when the delivery rules mirrored by `app/tests/server.js` diverge from it.
- **Landed in.** A1-P1.
- **Scope.** Covers refactor-equivalence of the shipped `/app` surfaces listed above, in one pinned headless Chromium, served by a mirror of `app/nginx.conf`. It does **not** cover real-device rendering, other browser engines, on-device PWA install/splash behaviour, accessibility or performance — those remain owner on-device smoke steps under the ADR-020 gate. It asserts that behaviour did not change; it does not assert that the behaviour it froze is *correct* (the fixed state it baselines is `A0-DL-001` + `A1-DL-001`). The suite is dev/CI-only and is excluded from both the production image and the Docker build context, so it constrains no runtime behaviour.

### A1-P3-INV-001 — The image ships everything the shell references, and the shell and the precache agree

- **Statement.** Two directions are machine-asserted on every CI run against the **real** `app/Dockerfile` `COPY` list: **(a)** every same-origin asset referenced by a shipped HTML shell, and every entry of `OFFLINE_URLS` in `app/sw.js`, resolves to a path the image ships **and** that exists on disk; **(b)** every same-origin asset referenced by a shipped shell is **also** precached by name in `OFFLINE_URLS`. Direction (a) exists because `app/Dockerfile` has no wildcard `COPY` while the parity server serves from the `app/` directory on disk — a forgotten `COPY` therefore passes the whole suite and 404s only in production. Direction (b) exists because module-mount bumps are copy-forward (`A1-DL-004`): both the old and the new mount version stay shipped, so (a) alone would stay green while `index.html` pointed at one version and `OFFLINE_URLS` still listed the other. The `COPY` parser fails **closed** — any form it does not fully understand throws, since a skipped line is a loud false negative while a mis-parsed destination is a silent false positive.
- **Enforced by.** `app/tests/delivery-contract.spec.js` (the `ship list — app/Dockerfile ships everything the shell needs` describe-block), run in the `contract` project of `app/playwright.config.js` via `scripts/parity-suite.sh` — invoked as the `parity` job in `.github/workflows/ci.yml`. Sources are the real `app/Dockerfile`, `app/sw.js` (`OFFLINE_URLS` read textually, with a loud throw on no-match, because `app/sw.js` calls `self.addEventListener` and cannot be `require()`d), `app/manifest.json` (`icons[].src`), and the shipped HTML shells `app/index.html` and `app/offline.html`.
- **Landed in.** A1-P3.
- **Scope.** Covers assets referenced from HTML attributes (`href=`/`src=` values beginning with `/`), manifest icon sources, and the precache list — since A1-P6 that surface includes the `<link rel="modulepreload">` delivery hints, so every hinted module is asserted shipped-and-precached as a shell reference in its own right, independently of the import walk that also reaches it. It does **not** cover: assets referenced only from JavaScript string literals — `fetch('/kb-v1.json')` in the shell is covered incidentally, via `OFFLINE_URLS`, and a future runtime-fetched asset that is not precached would be invisible; `url()` / `@import` references inside CSS (there are none today); `app/.dockerignore`, which the guard does not consult, so a future ignore pattern matching `m/` would make `COPY m/` ship nothing with this guard still green; and production `Content-Type`, which comes from the base image's `mime.types` and is observed only by the promotion-smoke curl in `docs/RUNBOOK.md`. The copy-forward / never-overwrite half of the `A1-DL-004` mount contract is a documented owner procedure, **not** a gate — nothing here can detect bytes changing at an already-published URL within one commit.

### A1-P4-INV-001 — The shipped module import graph is shipped and precached, transitively

- **Statement.** Every module the shell's `<script type="module">` entries import, transitively, resolves to a path the image ships **and** that is precached by name in `OFFLINE_URLS`. This extends `A1-P3-INV-001` past the HTML attribute surface: the walk is rooted at the shell's **execution** entries — its `<script type="module" src>` values, `/m/v1/app.js` and `/m/v1/sw-register.js` — and every other module (`core/` since A1-P4, `surfaces/` since A1-P5) is reachable only through an `import` statement, so both of that invariant's directions would stay green while a module was absent from the `app/Dockerfile` `COPY` list (a production 404 that no test sees, because the parity server serves from disk) or from `OFFLINE_URLS` (a broken import graph on the first offline boot, because `cache.addAll` never fetched it). Since A1-P6 the shell additionally **names** each non-entry module in a `<link rel="modulepreload">` hint. A hint is a delivery instruction, not an evaluation root — `modulepreload` fetches and compiles, it never evaluates — so hints are deliberately excluded from the walk's roots: rooting it at every `.js` the shell names would make `moduleDependencies()` delete the entire graph as "entries" and silently reduce this invariant to **zero** assertions with CI still green (`A1-DL-006` (g), `A1-DL-007` (d)). The walker fails **closed** at both ends: a dynamic `import()`, an indented module statement, or a bare specifier throws rather than being silently skipped, since the app is buildless and has no import map to resolve one; and a same-origin `<script src>` that is not `type="module"` throws rather than being silently dropped from the roots or silently added to them.
- **Enforced by.** `app/tests/delivery-contract.spec.js` (`htmlModuleEntries` / `moduleSpecifiers` / `resolveSpecifier` / `moduleDependencies`, asserted in the `ship list — app/Dockerfile ships everything the shell needs` describe-block), run in the `contract` project of `app/playwright.config.js` via `scripts/parity-suite.sh` — invoked as the `parity` job in `.github/workflows/ci.yml`. Sources are the real `app/Dockerfile`, `app/sw.js` and the shipped module files on disk.
- **Landed in.** A1-P4 (statement amended A1-P5: the graph reached this way now includes `surfaces/`; amended A1-P6: the walk's roots stated explicitly as the shell's `<script type="module">` entries — two of them since A1-P3, not the one the earlier wording claimed — with `modulepreload` hints excluded as roots).
- **Scope.** Covers **static** `import` / `export … from` specifiers in shipped JavaScript modules reached from a shipped HTML shell. It does **not** cover: dynamic `import()` (rejected loudly rather than followed — the parity suite's own test-seam import lives in dev/CI code, which this walker never reads); CSS `@import` / `url()` (there are none today, same gap as `A1-P3-INV-001`); assets fetched from JS string literals; import correctness beyond existence — a module that ships and is precached but throws on evaluation is the parity suite's business, not this guard's; or whether the `modulepreload` hint set agrees with the graph this walk produces, which is `A1-P6-INV-001`'s business and not this one's.

### A1-P6-INV-001 — The module delivery hints and the shipped import graph are the same set

- **Statement.** The set of `<link rel="modulepreload">` hrefs in the shipped HTML shells equals, exactly and in both directions, the transitive import graph `A1-P4-INV-001` walks from the shell's `<script type="module">` entries: **(a)** every hint names a module that walk reaches — a hint the graph no longer contains is a fetch nothing evaluates, which is what a copy-forward mount bump or a removed module would leave behind; **(b)** every module that walk reaches carries a hint — an unhinted module is discovered only after its importer parses, which costs the same-origin round trip A1-P6 removed and which nothing else in this repo would ever notice. Direction (b) is the load-bearing one: it makes the hint list a machine-checked consequence of the graph rather than a hand-maintained list that rots on the next extraction. The hints are same-origin absolute paths and are additionally asserted shipped-and-precached as shell references by `A1-P3-INV-001`, so a hint pointing at an unshipped path fails there.
- **Enforced by.** `app/tests/delivery-contract.spec.js` (`htmlPreloadHints`, plus the `PRELOAD_HINTS` ↔ `MODULE_DEPS` set-equality loops and the fail-closed classifier asserting every same-origin `.js` the shell names is either a module entry or a hint), run in the `contract` project of `app/playwright.config.js` via `scripts/parity-suite.sh` — invoked as the `parity` job in `.github/workflows/ci.yml`. Sources are the shipped HTML shells `app/index.html` and `app/offline.html` and the shipped module files on disk.
- **Landed in.** A1-P6.
- **Scope.** Covers set membership only: which modules are hinted. It does **not** cover: hint *ordering* or placement in the document (both affect fetch scheduling and neither is asserted); whether a hint actually improved anything (a waterfall measurement is a packet-time argument in `A1-DL-007`, not a gate — nothing in CI measures delivery timing); `rel="preload"` / `prefetch` / `preconnect` of any kind (there are none today, and this guard would not see them); hints for cross-origin modules (rejected loudly instead — the mount is version-in-path and a hint must track it); or whether the browser honours the hint at all, which varies by engine and is outside every level of the parity suite.

### L2-P2-INV-001 — The staging seed is fail-closed on the target-database marker

- **Statement.** The staging seed writes only to a database carrying the staging marker, asserted independently in two places: the database name parsed from `DATABASE_URL` **before any connection is opened**, and `SELECT current_database()` on the connected server **before any write**. Both compare against an allowlist of exactly one name, so a production name, a near-miss name and an absent name all refuse — the guard fails closed in both directions rather than blocking a known-bad list. The two layers exist because the first trusts a string while only the second asks the server, which is what catches a libpq `host=`/socket override or a pooler landing the connection somewhere the URL did not name. The refusal names the database only, never the connection string (§4). *(Namespace note: id follows the `L2-P{k}` packet namespace of the l2-staging milestone, mirroring the `VDK-P3-INV-001` / `A1-P1-INV-001` precedent for a non-`M{N}` namespace; the "Entry format" footer below is intentionally left unedited, matching how the decision-log treats its non-spine namespaces.)*
- **Enforced by.** `api/theygrow_api/seed_staging.py` (`ensure_staging_target` / `ensure_staging_server` against the `STAGING_DATABASE_NAME` allowlist, raising `NotStagingTarget`; the whole seed runs in one transaction so a refusal leaves the target untouched) + `api/tests/test_seed_staging.py` (`test_url_guard_refuses_anything_but_the_marker`, `test_seed_refuses_before_touching_the_corpus_when_url_is_not_staging`, `test_server_guard_refuses_when_url_and_server_disagree`, `test_url_guard_refusal_does_not_echo_the_connection_string`), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** A2-P2.
- **Scope.** Covers which database the seed may write to, and that a refusal writes nothing. It does **not** make the corpus itself safe — that staging holds no real family data is an authorship property of the corpus (see `api/corpus/staging/README.md`), unprovable by any test and deliberately not claimed here. It does not govern the other CLIs: `theygrow-import-export`, `theygrow-rederive` and `theygrow-embed` remain unguarded and will still act on whatever `DATABASE_URL` names. Nothing here constrains production; it constrains this seed.

### L2-P2-INV-002 — The clearance gate's default is egress, and the staging seed crosses no third-party boundary

- **Statement.** Two halves, the first load-bearing. **(a)** An embedding provider is treated as egressing unless it structurally declares otherwise: a missing declaration, or any value that is not exactly `True`, is gated by `embedder_privacy_cleared` exactly as before the A2-P2 narrowing — so the exemption cannot be acquired by omission, by a truthy stand-in, or by a provider that simply forgot. **(b)** Given that default, the staging seed embeds through an in-perimeter provider (`LocalDeterministicEmbeddingProvider`, no network client, no I/O) and therefore populates the dense leg while both clearance flags stay unset, making zero third-party provider calls (ADR-020). Clearance is per-egress-surface (ADR-014); an in-perimeter embedder removes the surface rather than satisfying its clearance. *(Namespace note: as `L2-P2-INV-001`.)*
- **Enforced by.** `api/theygrow_api/ports/provider.py` (`performs_no_egress`, defaulting to gated — in `embeddings_backfill.py` until A2-P3 moved it beside the ports) + `api/theygrow_api/adapters/embeddings/local_deterministic.py` (the declaring provider) + `api/theygrow_api/seed_staging.py` (which injects it) + `api/tests/test_seed_staging.py` (`test_unmarked_provider_still_gated_when_uncleared` and `test_truthy_declaration_is_not_enough` for half (a); `test_seed_populates_the_contour_with_zero_third_party_calls` and `test_in_perimeter_provider_runs_with_clearance_unset` for half (b)), run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** A2-P2 (predicate relocated A2-P3 — `L2-DL-003`; semantics byte-for-byte unchanged).
- **Scope.** Covers the gate's default disposition and the seed's provider wiring. It does **not** verify that a declaring provider truly performs no I/O — the declaration is trusted, and only code review keeps it honest; the test asserts the seed's calls all reach the injected in-perimeter provider, not that no socket exists anywhere. It says nothing about embedding **quality**: the local provider's similarity is authored from `concepts.json`, so it exercises the dense leg's plumbing and makes a semantic-only eval case authorable, and transfers no recall claim to the real embedder.

### L2-P3-INV-001 — The read-path clearance gates default to egress, and the eval crosses no third-party boundary

- **Statement.** Two halves, the first load-bearing. **(a)** On the READ path — the query embedding inside `retrieve()` and the answers egress inside `answer_query()` — a provider is treated as egressing unless it structurally declares otherwise: a missing declaration, or any value that is not exactly `True`, is gated by `embedder_privacy_cleared` / `answers_privacy_cleared` exactly as before the A2-P3 narrowing, so the exemption cannot be acquired by omission, by a truthy stand-in, or by a provider that simply forgot. **(b)** Given that default, the eval pipeline drives both seams through in-perimeter providers (`LocalDeterministicEmbeddingProvider`, `ScriptedAnswersProvider`) and therefore runs the whole labeled query set while BOTH clearance flags stay unset, making zero third-party provider calls (ADR-020). Clearance is per-egress-surface (ADR-014); an in-perimeter provider removes the surface rather than satisfying it, so **nothing here grants a clearance** — `answers_privacy_cleared` in particular remains default-false and an open owner prerequisite. *(Namespace note: as `L2-P2-INV-001`.)*
- **Enforced by.** `api/theygrow_api/ports/provider.py` (`performs_no_egress`, defaulting to gated) + `api/theygrow_api/services/retrieval.py` and `api/theygrow_api/services/query_service.py` (the two narrowed gate call sites; the answers provider is still built AFTER the grounding gate) + `api/theygrow_api/adapters/embeddings/local_deterministic.py` and `api/theygrow_api/adapters/answers/local_scripted.py` (the declaring providers) + `api/tests/test_retrieval.py` (`test_retrieve_fail_closed_when_uncleared`, `test_retrieve_truthy_declaration_is_not_enough` for half (a); `test_retrieve_in_perimeter_provider_runs_with_clearance_unset` for half (b)) + `api/tests/test_query_service.py` (`test_answers_fail_closed_when_uncleared_zero_calls`, `test_truthy_answers_declaration_is_not_enough`, `test_in_perimeter_answers_provider_runs_with_clearance_unset`) + `api/tests/test_evals.py` (`test_labeled_set_passes_on_the_seeded_contour`, which runs the whole set with both flags unset), run by `mypy api` + `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** A2-P3.
- **Scope.** Covers the two read-path gates' default disposition and the eval's provider wiring. It does **not** verify that a declaring provider truly performs no I/O — the declaration is trusted, and only code review keeps it honest. It says nothing about retrieval or answer **quality**: the local embedder's similarity is authored from `concepts.json` and the answers provider is a policy over the prompt, so both exercise plumbing and transfer no recall or faithfulness claim to the real providers.

### L2-P3-INV-002 — The eval gates on a recorded, per-case expectation and reports §4-safely

- **Statement.** Three halves. **(a)** The eval refuses to run at all unless the connected database carries the staging marker (both layers, reused from the seed), holds exactly the chunk and community counts the corpus on disk implies, and returns a stored vector that agrees with the run's own lexicon — so an empty, stale or wrong contour is a named refusal with its own exit code, never a vacuous pass on the three case classes that assert ABSENCE. **(b)** The gate is binary and structural: every labeled case must meet the expectation recorded beside it in `evals/queries.py`, and any cross-community or draft-route leak fails the run. There is no pass-rate threshold and no separately recorded baseline — the labeled set IS the baseline, and no environment variable can lower the bar. **(c)** The scorecard artifact and every log line carry case ids, bounded check names, counts and timings only: never query text, never chunk text, never a `community_id`, and never a `chunk_id` (a synthetic id still names its community by substring). *(Namespace note: as `L2-P2-INV-001`.)*
- **Enforced by.** `api/theygrow_api/evals/runner.py` (`check_contour`, raising `ContourNotReady` before any case; exit `2` for cannot-run vs `1` for case failure) + `api/theygrow_api/evals/queries.py` (the frozen set + `LABELED_SET_VERSION`) + `api/theygrow_api/evals/scorecard.py` (`Scorecard.ok`, `to_json_document`) + `api/tests/test_evals.py` (`test_eval_refuses_an_unseeded_contour_before_running_a_case`, `test_eval_refuses_when_stored_vectors_disagree_with_the_lexicon` for half (a); `test_labeled_set_passes_on_the_seeded_contour`, `test_scorecard_is_not_ok_when_a_case_failed` for half (b); `test_scorecard_carries_no_community_id_chunk_id_or_text`, `test_scorecard_uses_no_banned_metric_name` for half (c)) + `api/tests/test_seed_staging.py` (`test_corpus_created_at_is_unique_per_source_message`, the corpus property the legs' tiebreak needs for run-to-run stability), run by `pytest api` in `.github/workflows/ci.yml` and by the `eval` job in the same workflow.
- **Landed in.** A2-P3.
- **Scope.** Covers what the gate refuses, what it requires of each case, and what the artifact may carry. It covers **contract conformance on a fixed synthetic corpus** — retrieval fusion, note-only eligibility, community scoping, the grounding gate, honest degradation — and explicitly **not** Russian lexical recall, embedder semantic quality, or answer faithfulness: the corpus is authored, the embedder's similarity is authored, and the answers provider is our own policy. Those measurements are the M4-CLOSE mini-eval against the real corpus with cleared real providers, and nothing this invariant enforces bears on the `M4-DL-001` or `M4-DL-004` port-out triggers. It also does not make the `/api` promotion precondition a mechanism: `docs/RUNBOOK.md` states the eval run as an owner convention, and nothing reads its exit code on the way to promotion.

### A3-P1-INV-001 — The front end is never issued a CORS header, and the same-origin `/api` proxy carries a service identity rather than the caller's

- **Statement.** Two halves, both asserted against the real `app/nginx.conf`. **(a)** No location in the PWA's nginx configuration issues an `Access-Control-*` response header — the browser reaches `/api` through the PWA's own origin, so a CORS header is never needed and its appearance would mean the topology had been abandoned. **(b)** The `/api` proxy location sets the upstream `Authorization` header to the container-minted ID-token variable and includes the file that defines it; it does not forward whatever `Authorization` the caller sent. Half (b) is the one that can fail while still returning `200` in a smoke test: a deleted `proxy_set_header` line leaves nginx silently passing the client's header through, which looks healthy and is not the intended identity. The proxy's SNI directive is asserted alongside it, because nginx defaults `proxy_ssl_server_name` to **off** and the upstream requires it. *(Namespace note: id follows the `A3-P{k}` packet namespace of the A3 milestone, mirroring the `L2-P2-INV-001` / `A1-P1-INV-001` precedent for a non-`M{N}` namespace; the "Entry format" footer below is intentionally left unedited, matching how the decision-log treats its non-spine namespaces.)*
- **Enforced by.** `app/tests/delivery-contract.spec.js` (`same-origin /api proxy — config shape (A3-P1-INV-001)`: the no-`Access-Control-*` sweep over the whole file for half (a); the `Authorization` / token-include assertions on the proxy location for half (b); the `proxy_ssl_server_name` assertion), which parses the real `app/nginx.conf` rather than a mirror — run by `scripts/parity-suite.sh` and by the `parity` job in `.github/workflows/ci.yml`.
- **Landed in.** A3-P1.
- **Scope.** Covers the **shape of the committed configuration** and nothing else. It does **not** prove that the proxy works: the metadata server exists only on Cloud Run, so the ID-token mint, the audience↔service-URL match, the `roles/run.invoker` decision and the anonymous-`403` on the upstream's direct URL are established solely by the owner-run smoke on the tagged revision (`docs/RUNBOOK.md`, "Same-origin `/api` proxy"). It says nothing about the token's freshness at any moment, nothing about the refresh loop's behaviour under CPU throttling, and nothing about what the upstream does with the identity once it arrives. It also does not constrain the API service's own configuration — that a header is absent here is not evidence that CORS is absent there.

### A3-P2-INV-001 — Liveness is environment-free, and readiness fails closed and mute

- **Statement.** Two halves, both asserted against the real application. **(a)** `GET /api/health` constructs no `Settings` and opens no connection: with `DATABASE_URL` absent from the process environment it still answers `200` with its exact body. That is not a nicety — it is the property that makes the deploy gate legible, because a traffic rollback to a revision with no Cloud SQL attachment must still have a route that says "this revision is up". **(b)** `GET /api/health/ready` fails **closed** — any failure to construct `Settings`, to dial, or to execute `SELECT 1` yields `503` — and **mute**: the body is exactly `{"status": "ready"}` or `{"status": "unavailable"}` on the two paths, identical in shape, carrying no DSN, host, user, database name, driver text, timing or exception text; and neither does the `readiness.probe` signal, whose payload is two **closed** bounded labels (`outcome`, `failure_class` ∈ `none` | `config_invalid` | `connect_failed` | `query_failed`) plus a latency. The closed-enum half is the load-bearing one: `failure_class` is derived from which control-flow branch was taken and **never** from an exception, which is why no exception-derived string can reach a payload — psycopg and SQLAlchemy connection errors carry the host, the user and the socket path. *(Namespace note: continues the `A3-P{k}-INV-{NNN}` namespace opened by `A3-P1-INV-001`; the "Entry format" footer below is intentionally left unedited.)*
- **Enforced by.** `api/tests/test_readiness.py` (both body paths; the identical-shape assertion; the needle sweep for user/password/database/socket-path/driver over the body, the signal payload and the logging boundary; the closed-enum assertion driven by fakes whose exception text embeds the whole connection string; the pool-ceiling assertions), `api/tests/test_health.py::test_health_is_env_free_and_answers_without_database_url` for half (a), and `api/tests/test_signals.py` for the taxonomy/payload shape — run by `pytest api` in `.github/workflows/ci.yml`.
- **Landed in.** A3-P2.
- **Scope.** Covers in-process behaviour and payload shape, over TCP against CI's ephemeral pgvector service. It proves **nothing** about the production wiring: not the socket URL form, not the secret's contents, not the `--add-cloudsql-instances` attachment, not `roles/cloudsql.client`, not the `--service-account` pin — none of which exist off Cloud Run, and none of which any other contour rehearses, because no staging service exists. Their only proof is the owner-run smoke on the tagged revision (`docs/RUNBOOK.md`, "Production database enablement", step 13). It also says nothing about how long a probe takes under load, nothing about what happens above the pool ceiling beyond that the request fails rather than opening another connection, and nothing about whether the emitted signal reaches any particular log sink in production.

### LSC-P1-INV-001 — WebView storage has exactly one door

- **Statement.** Across the whole shipped web surface — both HTML shells, every `<script type="module">` entry, and every module reachable from those entries by static `import` — Web Storage is reached through **one** module, `app/m/v1/core/storage.js`, plus **two individually declared** shell accesses (`ga_debug`, the inline gtag debug flag; `iosInstallDismissed`, the install-prompt IIFE). The enforced property is the fail-closed default: **an access site that is not declared is red**, whether it is a new module, a new key in the shell, or an access whose key the guard cannot name (a `clear()`, a `length` read, a computed key) — the guard counts total accesses against declarable ones so an unnameable access cannot pass by being invisible. The mirror direction is asserted too: a declared exception that no longer appears must be dropped, so the list cannot rot into sanctioned precedent for accesses nobody makes. This is the seam L1-P2 replaces when the device-native store becomes the source of truth; it is enforced now because that replacement is only tractable if the set of call sites cannot quietly grow first. *(Namespace note: milestone L1 "local structured core" takes `LSC-` for decisions and invariants because the ladder id `L1` is already owned by the shipped **L1 Promotion Gate** track (`L1-DL-001`); packets keep `L1-P{k}`. Declared in `docs/execution-map.md`, following the `A2`/`L2` precedent. The "Entry format" footer below is intentionally left unedited.)*
- **Enforced by.** `app/tests/storage-seam.spec.js`, run in the `contract` project of `app/playwright.config.js` via `scripts/parity-suite.sh` — invoked as the `parity` job in `.github/workflows/ci.yml`. The scanned surface is computed by the shared walker in `app/tests/support/ship-list.js`, rooted at the shells' module entries, and a guard test asserts that walk did not collapse to nothing (the `A1-DL-007` (d) failure mode).
- **Landed in.** L1-P1.
- **Scope.** Covers **where** Web Storage is touched, not **what** is stored there. It deliberately does **not** assert that family data is absent from `localStorage` — today it is present, which is the state L1-P2 (the native store) and L1-P4 (the write path and the `localStorage` → native import) exist to change. *(Corrected in L1-P3: this sentence carried the pre-reorder ladder, in which P3 was the write path. `LSC-DL-002` reordered the packets — P3 is the export contour, P4 is the write path and migration — and corrected `docs/execution-map.md` but not this line.)* It matches the **property-access form** (`localStorage.` / `localStorage[`), which is why prose comments naming storage do not trip it and why an obfuscated dynamic lookup (`globalThis['local'+'Storage']`) would not be caught — the guard is a drift gate over honest code, not an adversary. Cache Storage (`caches`) is deliberately **not** scanned: it is the service worker's precache, the losable cache the platform inversion permits. **Knowingly uncovered, and named here rather than left out of frame:** inside the Capacitor WebView a registered service worker turns Cache Storage into a second copy of the shell held in WebView storage, which survives independently of the asset set the APK carries. That surface is real, it is exactly the persistence the milestone is inverting, and its disposition belonged to **L1-P4** — this packet establishes the fact (`LSC-DL-001`) and changes nothing about the registration, because L1-P1 makes zero product changes. *(Corrected in L1-P4: this sentence said **L1-P2**. `LSC-DL-002` declined the disposition as outside its approved scope and carried it to L1-P4 without editing this line, so the assignment here was false for two packets. It is now **closed** — see `LSC-P4-INV-004`.)*

### LSC-P1-INV-002 — The native channel ships the web channel's bytes unchanged, and exports nothing

- **Statement.** Two halves. **(a) Byte-identity.** The Capacitor web root (`native/www/`) is assembled from the `COPY` list in `app/Dockerfile` and is asserted to equal the shipped set **in both directions** — no missing file, no extra file — with every file's SHA-256 equal to its `app/` source. `app/nginx.conf` and `app/docker-entrypoint.sh` are asserted absent, since their `COPY` destinations fall outside the web root and the entrypoint mints the `/api` proxy's ID token. The application id (`app.theygrow`), the web root and the WebView origin (`https` / `localhost`) are pinned, and the native toolchain is asserted to invoke no bundler — so "no build step in the web path" is checkable rather than promised. **(b) No export.** `AndroidManifest.xml` sets `android:allowBackup="false"` and wires both backup-rule formats, each excluding every storage domain: `dataExtractionRules` (API 31+, the only one that also covers device-to-device transfer, which `allowBackup` does not disable) and `fullBackupContent` (API 24–30, in range at `minSdkVersion 24`). The enforced default is fail-closed — the Capacitor template ships `allowBackup="true"`, so without this the app sandbox (WebView storage today, the native store from L1-P2) would be uploaded to the user's cloud account by platform default, with no export contour, no key scope and no grant model behind it.
- **Enforced by.** `app/tests/native-shell.spec.js`, run in the `contract` project via `scripts/parity-suite.sh` — the `parity` job in `.github/workflows/ci.yml`. The ship list is parsed from the real `app/Dockerfile` by the shared fail-closed parser in `app/tests/support/ship-list.js`, the same one `app/tests/delivery-contract.spec.js` and `native/tools/stage-webdir.js` use. The spec reads `native/www/` **as it stands** and does not stage it — `scripts/parity-suite.sh` stages first — so a hand-added file is still present to be caught rather than wiped moments before the check.
- **Landed in.** L1-P1.
- **Scope.** Covers the **committed configuration and the staged asset set**. It does not prove the APK builds — that is the `android` job in CI, the one gate that cannot be reproduced on the development machine (no JDK, no Android SDK, no Gradle, and Node 18 against a CLI requiring 22). It does not prove the app runs on a device, nor that the backup exclusions take effect at the OS level: manifest attributes are asserted as text, and only an on-device check can observe Android honouring them (`docs/RUNBOOK.md`, "Android shell (Capacitor)"). It says nothing about signing — the APK this gate covers is the debug one, and release signing is `RSN-P1-INV-001` / `RSN-P2-INV-001` — and nothing about what a Play submission would require, which is gated out for the whole milestone. *(Corrected in RSN-CLOSE: this sentence read "the APK is debug-signed and no release keystore exists", false since RSN-P1. Statement-scoped; the rest of the entry stands.)*

### LSC-P2-INV-001 — The journal is append-only, and its two exceptions are named

- **Statement.** Every table of the mark journal — the `journal_entry` spine and its three detail tables (`assertion`, `confirmation`, `child_attribute`) — refuses `UPDATE` and `DELETE` at the engine, through `BEFORE` triggers that `RAISE(ABORT)`. A correction is a new entry on top; a superseded assertion stays in the journal forever, which is what makes state at any past date reconstructible (PDR-026 §4 slot 10). The invariant is stated with its **exceptions**, because an unstated exception is how an invariant rots into a slogan: the diary `record` is edited by overwrite (accompanying rule 1 — append-only binds the mark journal and derived state, not diary text) and `assertion_quote` rows are erasable (slot 15 — final deletion of a record cascades to erase the quotes copied out of it while the assertions survive with degraded provenance). Both exceptions are asserted positively, so a later packet cannot quietly extend append-only over them or quietly extend mutability past them.
- **Enforced by.** `app/tests/schema/test_store_append_only.py` (both verbs × all four tables, parameterized; plus the two exceptions asserted in the other direction) and `native/android/app/src/androidTest/java/app/theygrow/StoreEngineTest.java::the_append_only_triggers_fire_on_the_real_engine` — run by `pytest app/tests/schema` in the `quality` job and by the `android-instrumented` job of `.github/workflows/ci.yml`.
- **Landed in.** L1-P2.
- **Scope.** Covers what the ENGINE refuses, on a connection that has applied `app/m/v1/store/schema/001-core.sql`. It does not prevent a future migration from dropping the triggers (that is the migration mechanism's problem, designed with P4), and it says nothing about whether the application ever tries to update — the write path is P4. It does not cover the file: a caller with the passphrase and a raw SQLite connection can do anything, which is why the encryption key and the export contour are separate decisions.

### LSC-P2-INV-002 — A declarative marker never moves a computed number

- **Statement.** PDR-033 fixes slot 6 as **one** computing field (gestational age at birth, weeks + days, from which corrected-age policy is derived) and **three** declarative markers (bilingual environment, atypical development, unknown early history). The enforced property is structural and behavioural: flipping every marker through every value — set, cleared, changed — leaves the output of every computing view **byte-identical**. A marker changes what the product is allowed to assert; it never changes a number. The computing surface is reachable from attribute events through exactly one view (`v_child_profile`), which names the four non-marker attributes explicitly and therefore cannot reach a marker at all. All three markers carry the sensitivity flag by CHECK constraint rather than by the caller remembering.
- **Enforced by.** `app/tests/schema/test_store_markers.py` — the behavioural half (`test_markers_never_move_a_computed_number`, which snapshots the computing surface, flips all three markers through four values each, and re-compares after every write), an anti-vacuity test asserting the snapshot is non-empty and numerically correct, the textual half over the view SQL, and the CHECK-constraint half. Run by `pytest app/tests/schema` in the `quality` job.
- **Landed in.** L1-P2.
- **Scope.** Covers the SQL computing surface, enumerated in `COMPUTING_VIEWS`. A computation added in JavaScript that read `v_child_marker` directly would not be caught here — the guard is over the schema, which is the layer that freezes. `v_child_age` is deliberately clock-free (it yields a corrected baseline DATE, not an age), so the comparison is exact rather than time-sensitive; the subtraction happens at the call site.

### LSC-P2-INV-003 — The "do not collect" list is closed, and two column names are reserved

- **Statement.** Two denylists over the whole schema — every table, view, index and trigger name, and every column of every table and view. **(a)** PDR-033's closed list: no clinician-instrument score (GMFCS/MACS/CFCS, Vineland, PEDI-CAT, HINE, ASQ-like), no numeric probability of a diagnosis or risk, no syndrome curve, no severity or percentile, no multiple-birth or deprivation coefficient, and no `regression` attribute — regression is derived from the journal, not stored. The `child` row is asserted to carry `id` and `created_at_utc` and nothing else, and the child-attribute enum is asserted **closed** at exactly the eight values PDR-033 permits, so the list cannot be extended by adding a column somewhere else. **(b)** `sql_deleted` and `last_modified` are reserved: `@capacitor-community/sqlite` rewrites `DELETE FROM … WHERE …` into a soft-delete `UPDATE` when a table carries both, so a schema that grew those names would silently stop erasing quote-basis rows and slot 15's erasure would become a lie told by a third-party plugin.
- **Enforced by.** `app/tests/schema/test_store_denylist.py` — name scan, column scan, the closed-enum assertion parsed out of the live DDL, the two reserved names, and the shape assertions on `child` and `participant`. Run by `pytest app/tests/schema` in the `quality` job.
- **Landed in.** L1-P2.
- **Scope.** Matches **names**, not meaning: a forbidden quantity stored in a column called `value` would pass, which is why the child-attribute enum is asserted closed rather than relying on the name scan alone. It covers the schema, not the UI — nothing here stops a surface from asking a question the store then declines to hold. Half (b) is pinned to the wrapper this packet selected; `app/tests/store-supply-chain.spec.js` asserts the wrapper behaviour it depends on has not drifted.

### LSC-P2-INV-004 — The schema converges under both CRDT candidates, and neither library ships

- **Statement.** The frozen schema is checked against the merge semantics of **both** ADR-040 §3 candidates — Automerge and Loro — and the library itself stays **unselected** (that is L7). For each library: concurrent appends on two replicas lose nothing; merge order does not change the merged journal; the projection is identical on both replicas afterwards; diary text edited by overwrite converges to one value; quote erasure converges while the assertion survives; and an id minted at creation survives the merge as the key. The two libraries are then asserted to agree with **each other** on the merged journal and its projection, so choosing either later creates no lock-in and no migration. The containment half is enforced in the opposite direction: no shipped byte mentions either library, no shipped module imports a bare or package-path specifier, and neither is a runtime dependency of either `package.json`.
- **Enforced by.** `app/tests/merge-semantics.spec.js` (one adapter per library, the same assertions run against both, plus the cross-library agreement test and the device-local-column tests) and `app/tests/store-supply-chain.spec.js` (containment, the plugin-method allowlist, and the exact-pin assertions) — both in the `contract` project, run by `scripts/parity-suite.sh` as the `parity` job.
- **Landed in.** L1-P2.
- **Scope.** Checks the SCHEMA's merge behaviour, not a sync implementation: there is no relay, no key scope and no grant model, and none is in this milestone. The projection rule is ported into the spec rather than executed as SQL — a CRDT document is not a SQLite connection — so the rule is pinned in two places and `app/tests/schema/test_store_projections.py` holds the SQL side. It says nothing about performance, document size, or how either library behaves on a journal of realistic length.

### LSC-P2-INV-005 — No family datum exists without an author and a subject

- **Statement.** `journal_entry.author_participant_id` and `journal_entry.subject_child_id` are both `NOT NULL` and both foreign keys, enforced with `PRAGMA foreign_keys = ON`; the detail tables key on the spine, so a child attribute cannot name a different child than the entry that carries it. This is slot 1 made structural — a mark is an **attributed assertion**, never an impersonal fact — and it is also the defensible form of the "a default profile always exists" premise: the store **does not** auto-create a child (that was rejected as an owner decision in the A0 spa-polish entry — "it invents a family record the owner did not ask for, to paper over a write path that should simply report failure"), it makes an orphan impossible instead. The one row bootstrap does create is the `is_self` participant, which is an id to attribute to rather than a family record, and which carries no name column at all.
- **Enforced by.** `app/tests/schema/test_store_slots.py::test_an_assertion_cannot_be_anonymous` (both the unknown-author and the null-subject case), `test_store_denylist.py::test_the_participant_row_carries_no_name`, and the guard in `app/m/v1/store/journal.js::appendEntry` asserted by `app/tests/store-unit.spec.js`. Run by `pytest app/tests/schema` in the `quality` job and the `contract` project in the `parity` job.
- **Landed in.** L1-P2.
- **Scope.** Covers the schema's refusal, on a connection with foreign keys ON — the store turns them on at open, and a connection that did not would not be enforcing this. It does not make the WRITE path honest about failure; that behaviour (the A0 fix: revert the checkbox, open the create-profile modal, emit nothing) still lives in `app/m/v1/surfaces/skill-completion.js` and is P4's to re-express against the native store.

### LSC-P2-INV-006 — The bundled engine carries FTS5, and the shipped DDL applies to it

- **Statement.** On a real Android device, the SQLCipher engine the app links (`net.zetetic:sqlcipher-android`, bundled — **not** the Android system SQLite, which guarantees neither) compiles `ENABLE_FTS5` in, reports a SQLite version at or above the `3.37.0` floor STRICT tables need, opens in WAL, and reports a non-empty `PRAGMA cipher_version` so the database is demonstrably encrypted rather than assumed to be. The DDL read out of `assets/public/` — the same artifact the WebView fetches — applies clean, passes `integrity_check`, and its append-only triggers fire. Russian tokenization is asserted **as measured**, including what it does not do: `unicode61` folds Cyrillic case but does **not** fold `ё` to `е`, which is recorded as the L2 word-form gap rather than assumed away. The absence of `ENABLE_ICU` is asserted too, as a recorded fact about the engine — see `LSC-DL-002` on why the substance secured instead is a fully rebuildable index.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/StoreEngineTest.java` and `BridgeSmokeTest.java` — run by the `android-instrumented` job in `.github/workflows/ci.yml`.
- **Landed in.** L1-P2.
- **Scope.** **This is not a per-push gate.** The job runs on `pull_request` and `workflow_dispatch` only, by owner decision on CI spend, so a regression introduced by a later packet is caught at PR time rather than at push time — possibly several commits after its cause. It covers one emulator image (API 34, `google_apis`, x86_64) and says nothing about other API levels, other ABIs, or physical devices; the on-device smoke in `docs/RUNBOOK.md` is what covers a real handset. `BridgeSmokeTest` covers the bridge call path and that the app opens its store at boot; it does not exercise the write path, which does not exist until P4.

### LSC-P3-INV-001 — The export artifact is readable using only what it declares about itself

- **Statement.** The archive carries a verbatim copy of `app/m/v1/export/declaration.json` inside its own `index.json`, and that copy is sufficient to interpret every file in the archive: which datasets exist, which columns each carries, what every column means in plain Russian, in what order the journal is written, and how a spine row is joined to its detail row. The enforced property is proven by a reader that **imports nothing but the Python standard library** — asserted structurally, by parsing the reader's own import statements, so an import added later cannot quietly turn the proof into a statement about this repository. Every declared dataset's column list is checked against what its declared query actually returns on the real frozen schema, so the artifact cannot promise a field it does not deliver or deliver one no reader was told about; every declared file must be present and non-empty, and the zero case (`text/diary.txt`) is worded as a fact about the source rather than left as an empty file that reads as data loss. *(Corrected at DIA-P4: this read "until L2", which L2 did not make true. L2 gave the diary a write path and a search and left the ARTEFACT unchanged — `record` is in no declared dataset. The owner gate of 2026-08-17 decided the artefact will carry diary text and addressed the work to **L3**; until it ships, a parent's diary text exists in exactly one copy on one device and the archive carries marks and journal history only. The scope that work inherits is the one `LSC-P3-INV-003` already enforces: the exporting participant's own private area, never another participant's. See `DIA-DL-005`'s pointer and `DIA-DL-008`.)* This closes PDR-026 §4's fifth accompanying rule, which L1-P2 deferred to this packet. *(Namespace note: continues the `LSC-` namespace declared in `docs/execution-map.md`; packets keep `L1-P{k}`. The "Entry format" footer below is intentionally left unedited.)*
- **Enforced by.** `app/tests/export/test_artifact_selfdescribing.py` (with the reader in `app/tests/export/blind_reader.py`) and `app/tests/export/test_artifact_shape.py`, run by `pytest app/tests/export` in the `quality` job of `.github/workflows/ci.yml`. The artifact under test is built by the **shipped** builder: `app/tests/export/harness.py` runs the declared queries against a real SQLite database carrying the real `001-core.sql`, then shells out to `node` on `app/m/v1/export/build.js` — the same discipline `app/tests/schema/harness.py` uses when it applies the DDL through a port of the wrapper's own splitter. `app/tests/schema/test_store_slots.py::test_each_accompanying_rule_is_closed_or_addressed` resolves rule 5 to the blind-reader test, so the rule is closed by a live test rather than by a deferral string.
- **Landed in.** L1-P3.
- **Scope.** Covers the artifact's **structure and its self-description**, on a journal the test seeds. It does **not** prove the artifact is useful: until L1-P4 lands the write path, nothing writes family data to the journal, so on a real device the archive is correct and nearly empty. It does not cover the PDF print layer, which is the second checkpoint of this packet and carries its own invariant. It says nothing about re-importing the artifact — import is L6, and the README says so rather than implying a round trip.

### LSC-P3-INV-002 — The artifact is produced locally, on an explicit human action, and never transmitted

- **Statement.** No module under `app/m/v1/export/` contains a request primitive at all (`XMLHttpRequest`, `sendBeacon`, `WebSocket`, `EventSource`, or any `navigator.` reach), and every `fetch` call site addresses its target through a knob on the `EXPORT_CONFIG` surface rather than a literal URL — so a new off-device read has to be declared in the config surface before it can compile past the gate. The only fetch the contour makes is of its own declaration, which on the native channel is a local asset read served by Capacitor with no network involved. Nothing schedules an export: no `setInterval`, no `setTimeout`, no `requestIdleCallback` and no listener registration anywhere in the contour, and the surface that wires the button is asserted to listen for `click` and nothing else — so an unencrypted archive cannot appear on the filesystem without a person asking for it in that moment. The sink is **one method wide** and the count is asserted: `ALLOWED_SINK_METHODS` is exactly `['createDocument']`, and the Android plugin behind it uses `ACTION_CREATE_DOCUMENT`, which needs **no storage permission** — the app can write the single document the parent picked in the system file picker and nothing else, cannot list a directory, cannot read a file and cannot choose a path of its own. No export module touches WebView storage. The interface states two sentences verbatim, before the action rather than after it — that media is not in the archive, and that there is no cloud backup of this data — and both are asserted as exact strings so they cannot drift into a hedge one word at a time.
- **Enforced by.** `app/tests/export-contour.spec.js`, run in the `contract` project of `app/playwright.config.js` via `scripts/parity-suite.sh` — the `parity` job in `.github/workflows/ci.yml`. The device half is `native/android/app/src/androidTest/java/app/theygrow/ExportSinkTest.java` (the byte path, truncation, and that the first-party plugin is registered and answers through the injected bridge), run by the `android-instrumented` job.
- **Landed in.** L1-P3.
- **Scope.** Covers **shipped source text and the declared sink boundary**, plus the byte path on one emulator image. Like `LSC-P1-INV-001` it is a drift gate over honest code, not an adversary: an obfuscated dynamic lookup would not be caught. It does not prove Android honours the picker's URI grant, nor that the parent chose a sensible location — the archive is plaintext family data and where it lands is the parent's choice by design, which the interface states rather than manages. **The device half is not a per-push gate**: `android-instrumented` runs on `pull_request` and `workflow_dispatch` only, by the standing owner decision on CI spend, so a sink regression is caught at PR time rather than at push time. The system file picker's own UI is deliberately not automated — see `ExportSinkTest`'s header and the owner-run smoke in `docs/RUNBOOK.md`.

### LSC-P3-INV-003 — The archive carries the requesting participant's records, and no other participant's private ones

- **Statement.** Every dataset whose query reads a visibility column (`visibility_class`, `owner_participant_id`, `private_to_participant_id`) binds the requesting participant's id as a declared parameter, and the declaration states the rule in its own published words: the archive carries every child-shared entry plus the private entries of the participant who asked for it, and each participant exports their own. The parameter is bound by name, and an unknown name raises rather than binding `NULL` — a scope filter that silently bound `NULL` would widen the archive past its requester while every other assertion stayed green. Absence is asserted as well as presence, and asserted **in the bytes** rather than only in the parsed datasets, so a leak into a rendered text file cannot pass.
- **Enforced by.** `app/tests/export/test_artifact_selfdescribing.py::test_the_archive_holds_the_requesters_own_private_records` and `::test_the_archive_holds_no_other_participants_private_records`, over a fixture (`app/tests/export/harness.py::seed_family`) that deliberately holds a second participant with private records, a shared record, and one quote per participant — because a scope filter shown only shared data passes while doing nothing. The structural half is `app/tests/export-contour.spec.js`, which fails if a dataset reads a visibility column without binding the requester. Run by `pytest app/tests/export` and the `parity` job.
- **Landed in.** L1-P3.
- **Scope.** On this rung there is exactly one participant, so the exclusion half changes no observed behaviour today. It is enforced now because the **format is published**: a declaration that had promised "all private areas" would already have committed the product to putting a co-parent's private records into someone else's archive when a second participant arrives at L7. It covers what the declared queries select; it does not cover a future dataset added without a visibility column, which is why the structural gate keys on the column names rather than on a list of dataset names.

### LSC-P3-INV-004 — Two exports of the same journal are the same archive, and the export time lives in one file

- **Statement.** Given the same journal state and the same `exported_at_utc`, the artifact is **byte-identical**; given the same journal state and a different export time, every file is byte-identical except `MANIFEST.json`. This is achieved structurally rather than by convention: entries are STORED (zip method 0, never compressed — DEFLATE output is not byte-stable across implementations, so two devices holding the same journal would otherwise disagree), entry timestamps are pinned to the DOS epoch instead of the wall clock, entry order is the declared file order, and the journal is ordered by `(entry_at_utc, id)` — the replica-identical total order — with the device-local `seq` deliberately absent from the artifact entirely. The anti-vacuity direction is asserted too: a changed journal must change the archive, and no file outside `MANIFEST.json` may contain the export timestamp.
- **Enforced by.** `app/tests/export/test_artifact_deterministic.py` (all four directions) and `app/tests/export/test_artifact_shape.py::test_every_entry_is_stored_uncompressed_and_time_pinned`, run by `pytest app/tests/export` in the `quality` job; the declared half (`compression: stored`) is cross-checked against the config knob by `app/tests/export-contour.spec.js`.
- **Landed in.** L1-P3.
- **Scope.** Covers the archive the builder produces from a given read-out. It does not cover the read-out itself being stable under concurrent writes — there is no write path until P4 — and it makes no claim about the archive being identical across **format versions**: a `format_version` bump is exactly the event that is allowed to change these bytes. The print layer's contribution to this property is the second checkpoint's to establish.

### LSC-P3-INV-005 — The print layer is PDF/A-2b, and the claim is verified rather than asserted

- **Statement.** The archive's `print/archive.pdf` is written to PDF/A-2b and says so in two places a reader can check — the XMP packet (`pdfaid:part` 2, `pdfaid:conformance` B, in an unfiltered metadata stream) and the artifact's own `declaration.json`. The enforced property is that **the claim and the file agree**, checked from two directions at two costs. Structurally, on every push: no `/Encrypt`; every `FontDescriptor` embeds a `FontFile2` with a declared `/Length1`; an `OutputIntent` with `/S /GTS_PDFA1` and an embedded `/N 3` destination profile; XMP dates equal to the document information dictionary's; a trailer carrying `/ID`, `/Root` and `/Info`; and a cross-reference table every one of whose offsets is followed to the object it claims to start. Certifyingly, at PR or on dispatch: veraPDF `--flavour 2b` against a fixture built by the shipped builder, failing the job unless the report says `compliant="true"`. Text extraction is asserted by **round trip** rather than by presence — the CMap is walked and the first page read back, requiring Cyrillic to survive and no replacement character to appear — because an Identity-H file with a broken `ToUnicode` renders perfectly and yields nonsense when copied. The declared degradation is asserted with it: a codepoint outside the embedded font renders as U+FFFD **in the PDF only**, the text files and `index.json` always carry the exact characters, and the declaration names which copy is authoritative.
- **Enforced by.** `app/tests/export/test_pdf_structure.py`, run by `pytest app/tests/export` in the `quality` job of `.github/workflows/ci.yml`; and the `Validate the print layer against PDF/A-2b` step of the `android-instrumented` job, over a fixture produced by `app/tests/export/emit_fixture.py` through the same `harness.py` the pytest suite uses. The two vendored binaries the layer requires are pinned by SHA-256 in `app/tests/export-contour.spec.js` (`parity` job).
- **Landed in.** L1-P3.
- **Scope.** **What the free per-push half can and cannot cover, stated once.** It can assert every property expressible by reading the bytes: the absence of `/Encrypt`, an embedded `FontFile2` per descriptor, an `OutputIntent` with an embedded `/N 3` profile, XMP dates equal to the document information dictionary's, a trailer with `/ID`/`/Root`/`/Info`, an xref whose offsets resolve, and a text round trip through the `ToUnicode` CMap. It cannot cover the rest of PDF/A-2b: 144 validation rules and ~2 779 individual checks span colour spaces, transparency, annotations, actions, optional content, embedded file relationships and ICC tag internals, and re-deriving those in the standard library would be building a second validator that agrees with our reading of the specification rather than with the specification. That residue is veraPDF's, and it is why the certifying step exists. A third class sits outside both: **a defect in the gate itself** — `z-bis` — which no assertion about the PDF can catch, and which is covered instead by `app/tests/export-contour.spec.js` checking the workflow's success condition against the attribute veraPDF actually emits. **The certifying half is not a per-push gate.** `android-instrumented` runs on `pull_request` and `workflow_dispatch` only, by the standing owner decision on CI spend, so a conformance regression is caught at PR time rather than at push time — the same trade already accepted for the device layer, and the reason the cheap structural half exists at all. veraPDF is installed from upstream's unversioned "latest" URL, since no version-pinned URL is published, so the tool version is echoed into the log rather than asserted. The gate covers the artifact this repository's fixture produces; it says nothing about a PDF built from a family's real journal, which no CI job will ever see. It does not assert that any particular viewer renders the file, nor that the embedded font covers any particular language beyond the round trip's Cyrillic — coverage gaps are handled by the declared U+FFFD degradation rather than prevented.

### LSC-P4-INV-001 — A mark is an attributed assertion, and it travels with its author's confirmation

- **Statement.** Ticking a skill does not record the fact "mastered"; it appends an **attributed assertion** — an author, a subject child, an event date, an entry time and a stable id — and, in the **same transaction**, a `confirmation` of that assertion by the same author. The pairing is unconditional and nothing anywhere branches on how many participants exist, which is what makes "confirmed by one" the answer the general consensus function returns rather than a degenerate case somebody remembered to special-case (PDR-021). Un-ticking is a `skill_revoked` assertion appended on top, never a delete and never an update: the write path issues no `UPDATE` and no `DELETE` against any journal table. A skill's state is **projected** from the assertions on every read and is never stored as a fact — the in-memory set the render path consumes is a cache of that projection, rebuilt after every write, and authoritative for nothing. The projection carries the consensus columns (`confirmed_by`, `disputed_by`, `needs_refresh_by`) and the `origin` of the winning assertion, so the half-confirmed state is **expressed** at this rung even though no surface reads it until L7. With no subject, or with no author, or with a native store that did not open, the mark is **refused**: the checkbox is put back, the parent is told which of the three happened, and nothing is written (ADR-015).
- **Enforced by.** `app/tests/schema/test_write_path_projection.py`, run by `pytest app/tests/schema` in the `quality` job of `.github/workflows/ci.yml` — against a real SQLite database carrying the real frozen `001-core.sql`, with the projection query **read out of `app/m/v1/store/journal.js`** rather than retyped, so the query that is verified is the query that ships. The act's shape (one transaction, four statements, no `UPDATE`, no `DELETE`, both refusal paths) is `app/tests/write-path.spec.js` in the `contract` project via `scripts/parity-suite.sh` (`parity` job). The unconditionality of the confirmation is asserted against the shipped source, because the absence of a branch cannot be shown by reaching a branch.
- **Landed in.** L1-P4.
- **Scope.** Covers what the app **asks the store to do**, and what the SQL **means** on the real schema — deliberately in two places, by two mechanisms. `app/tests/write-path.spec.js` drives a recorder, not a database: it proves the statements, values and transaction grouping, and proves nothing about SQLite or the plugin. That the plugin and SQLCipher actually execute them is `android-instrumented`'s, and that job is **not a per-push gate** — it runs on `pull_request` and `workflow_dispatch` only. Both parity channels run without an injected Capacitor bridge, so **the parity suite exercises the localStorage backend, not the journal one**; what it proves here is that the web path is unregressed. It does not cover the surface behaviour of a half-confirmed node, which is L7's, nor prerequisite propagation, which stays a projection-time function and is never materialised.

### LSC-P4-INV-002 — The legacy import is re-runnable, and it never writes to the source

- **Statement.** Four properties, each a condition on the import being allowed to run at all. **(1)** Running it twice is running it once: every id is DERIVED — a SHA-256 over a declared namespace and the tuple it identifies, shaped as a UUID — so a second run computes the same ids, reads back which the store already holds, and appends only the remainder. There is no ledger and no "already imported" flag: the journal is the only record of what has been imported, so there is no second truth to drift. **(2)** An interrupted run leaves a state the next run **completes**: a child row and its attributes go in one transaction, marks go in batches, and an assertion is never split from its confirmation — so an interruption leaves a complete child and a prefix of their marks, and the resumption appends exactly the remainder with nothing written twice. **(3)** The import **cannot** write to `localStorage` — no module in the import path imports a writer from `app/m/v1/core/storage.js`, which is a stronger guarantee than not calling one, because a module that never imports a writer cannot become a second door by a later edit to its body. **(4)** Re-running does not undo what happened natively: a mark revoked on the device keeps its imported assertion under the same derived id, so the import appends nothing and the later revocation still wins the projection. Every imported entry carries `origin = 'migrated_legacy'`, the import date in the NOT NULL `event_date_local`, and a NULL event instant — the schema's own way of saying only the date is known — plus one confirmation by its author, which is "the author's assertion, confirmed by one" written into the data rather than inferred by a projection.
- **Enforced by.** `app/tests/import-legacy.spec.js` in the `contract` project via `scripts/parity-suite.sh` (`parity` job), including an interruption test that fails the *N*th transaction and then asserts that the interrupted run plus its resumption equals an uninterrupted run, with no id written twice. The SQL-level half — that a migrated mark and an authored one project identically but for `origin` — is `app/tests/schema/test_write_path_projection.py` (`quality` job).
- **Landed in.** L1-P4.
- **Scope.** Covers the import's control flow and the shape of what it writes, against a recorder. It does not prove SQLite enforces the transaction boundaries — that is the engine's, covered by `StoreEngineTest` and the append-only triggers — and it does not cover a *process kill* mid-transaction, only a failed call; the two leave the same state (a committed prefix) because a transaction that never commits is rolled back on reopen, but only the former is automated. **Clearing `localStorage` after a successful import is a separate, explicit owner action and is not in this packet**; nothing here removes, rewrites or marks the source consumed. The consent shape — per-profile, everything ticked by default — is a surface property, not an enforced one.

### LSC-P4-INV-003 — No signal payload can carry family text

- **Statement.** The client signal surface accepts, as a payload value, only a **number**, a **boolean**, `null`, or a member of the **closed code list declared for that field**. There is no code path that accepts a free string, so a payload cannot carry a child's name, a skill title, or anything a parent wrote — not "does not", *cannot*. An undeclared kind, an undeclared field, a nested object, an array, a function and `undefined` are each refused; a refusal is counted rather than swallowed, and `emitSignal` never throws outward, because a telemetry bug must not take the write path down with it. Structurally, every `emitSignal` call site in the shipped surface names a declared kind with literal keys, no payload value is derived from an identifier that holds family text, and **no payload computes a value inline** — a call inside a payload is refused outright, which is what makes the family-text scan sound rather than merely present. **The surface's only sink is the device console.** It reaches no network primitive, and no emitter hands a signal to the analytics shim.
- **Enforced by.** `app/tests/signal-payload.spec.js` in the `contract` project via `scripts/parity-suite.sh` (`parity` job), over every shipped JavaScript module expanded from `app/Dockerfile`'s COPY list. The taxonomy, its codes and the guard live in `app/m/v1/core/signals.js`.
- **Landed in.** L1-P4.
- **Scope.** Covers **this** surface. It says nothing about the GA4 shim inlined in `app/index.html`, which predates it, is not behind this seam, and whose payloads are governed by §4 and `GA-DL-001` — on the native channel that shim is now inert (`LSC-P4-INV-004`), and on the web channel it is untouched and its disposition remains an open owner item. Like every source-text guard in this repository it is a drift gate over honest code, not an adversary: an obfuscated dynamic emit would not be caught. **Giving this surface a network sink is a separate owner decision on the escalation list (PDR-027 §2 — egress)**, and the absence of one is asserted rather than assumed, so it cannot be added as an implementation detail of adding a signal. The guard's soundness was established by mutation rather than by inspection — an earlier form of it failed **open** on exactly the shape a violation takes, and was replaced.

### LSC-P4-INV-004 — The native channel holds no second copy of the shell, and sends nothing to analytics

- **Statement.** Inside the Capacitor WebView the app registers **no service worker**, unregisters one a previously installed APK left behind, and deletes every Cache Storage entry under its own shell prefix — so Cache Storage cannot become a second copy of the shell held in WebView storage, which is the persistence surface this milestone spent four packets inverting. What is deleted is the losable cache ADR-043 permits — shipped shell assets, reconstructible from the APK on the next launch — and **never family data**. The same channel loads no analytics: the `googletagmanager.com` loader is not injected, no `gtag` configuration is queued, and `trackEvent` returns before recording anything, so the egress stops at the request rather than only at the event. Both are **runtime branches inside the same shipped files**, so the two delivery channels still ship byte-identical bytes (`LSC-P1-INV-002`), and **neither branch runs on the web**, where the PWA keeps its worker, its offline boot and its analytics unchanged.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/WebViewStorageTest.java`, run by the `android-instrumented` job of `.github/workflows/ci.yml`. It asserts zero service-worker registrations, no cache under the shell prefix, no analytics loader in the document, an empty `dataLayer` before and after a `trackEvent` call, and — as anti-vacuity for all of it — that `Capacitor.isNativePlatform()` and `IS_NATIVE_SHELL` are actually true, since every assertion is about a branch that would otherwise not have been taken. The probe facts (`'serviceWorker' in navigator`, `caches.keys()`, `typeof trackEvent`) are **printed** as well as asserted, so the instrumented report records what the WebView does rather than only that a test passed.
- **Landed in.** L1-P4.
- **Scope.** **This closes the question `LSC-DL-001` (i) opened and `LSC-DL-002` carried**, and it closes the standing analytics item `LSC-DL-001` (o) **for the native channel only** — whether the *web* channel should carry analytics at all remains an open owner decision under the escalation list, with its own address at the milestone close. Covers one emulator image (API 34, `google_apis`, x86_64) and **is not a per-push gate**: `android-instrumented` runs on `pull_request` and `workflow_dispatch` only, by the standing owner decision on CI spend. The parity suite cannot substitute: both of its channels serve `native/www` over plain HTTP with **no Capacitor injected**, so both take the web branch, and the two analytics assertions in `behavior.spec.js` are statements about the web branch and remain so. It asserts the end state rather than the mechanism — if a future Android WebView stops exposing `serviceWorker` entirely, the guard becomes a no-op and the assertions still hold, which is why the probe facts are recorded beside them.

### EMV-P1-INV-001 — A surface the app opens by class is a surface the stylesheet can actually show

- **Statement.** Two properties, deliberately separated because one of them is what failed. **(1) Coverage:** for every `classList.add('show')` in the shipped module mount, the element it targets carries a class for which the mount's `app.css` declares a matching `.show` rule. There are six such call sites today, and before this packet three of them — the export modal, the legacy-import offer and the store-unavailable notice, all bare `.modal` elements — had no rule at all, so their handlers ran to completion and the elements stayed `display: none`. **(2) Behaviour:** clicking `#exportBtn` produces a modal that is **visible**, computing to `display: block`, and the close control returns it to `display: none`. The second property is asserted by performing the action, not by reading the source that performs it — which is the whole subject of this invariant, because every existing guard over that surface was green while the surface was dead.
- **Enforced by.** `app/tests/show-rule-coverage.spec.js` (the `contract` project) for (1) — it derives the mount from the shell, enumerates the call sites, resolves each to its element id (failing closed on any form it cannot resolve) and requires a matching rule. `app/tests/behavior.spec.js` for (2) — it boots the app, clicks the control and reads the computed style, in the `behavior` project (nginx channel, desktop) and the `native` project (staged APK web root, mobile). Both run via `scripts/parity-suite.sh` in the `parity` job of `.github/workflows/ci.yml`. Soundness was established by mutation rather than inspection: deleting the rule reds both, on exactly the three affected call sites.
- **Landed in.** EMV-P1.
- **Scope.** **The two halves are not interchangeable, and the coverage half must never be read as the behavioural one.** Emptying `openExportModal()`'s body leaves every coverage assertion green — measured, not assumed — because the call site disappears and there is nothing left to cover; only the click test reds. Conversely the click test says nothing about the other five call sites. Covers the mount the **shell references**, never `app/m/**`: a copy-forward bump leaves the frozen generation on disk without the rule, correctly, and scanning it would red on bytes that are right to leave alone. Like every source-text guard here, (1) is a drift gate over honest code — a class name assembled at runtime would not be caught, and the scanner throws rather than skipping any call-site form it does not fully understand. For `#importModal` and `#storeUnavailableModal` the suite asserts only that **the rule resolves them to `display: block`**, with the class applied in-page: their trigger paths are unreachable on the web branch (`offerImportIfPending()` returns without a native store handle; `showStoreUnavailable()` sits behind `canRecord() === false`), so **no automated test executes either trigger** — that is `android-instrumented`'s and remains open debt (`EMV-DL-001` debt 3). Both parity channels run without an injected Capacitor bridge, so (2) proves the **web** branch on both, twice, and not the native one.

### EMV-P3-INV-001 — A client installed on the previous generation ends up running the current mount, and sees the fixed surface

- **Statement.** With two mount generations shipped, a client that installed the **previously published** shell and worker — precache filled from `/m/v{N-1}/`, cache generation `theygrow-v{K-1}` — ends up evaluating `/m/v{N}/app.css` and gets a **visible** export modal (`display: block`) when `#exportBtn` is pressed, on **both** update legs: after accepting the update banner, and after simply opening the app again without touching it. No request under the previous mount participates in the outcome. The property is asserted by installing that generation and driving the update in a real browser, never by reading the worker's strategies and concluding they would work — which is what `EMV-DL-001` was left resting on.
- **Enforced by.** `app/tests/upgrade-path.spec.js` (the `behavior` project), which stages the previous generation from the bytes on disk (`app/tests/support/prev-generation.js`, served by a cookie-keyed switch in `app/tests/server.js`), pins that it really installed — a controlling worker, `theygrow-v{K-1}` present, `/m/v{N-1}/app.css` in its precache, and the previous generation's own broken modal reproduced by a click — then serves the current build and asserts the outcome. Runs via `scripts/parity-suite.sh` in the `parity` job of `.github/workflows/ci.yml`. Soundness by mutation, measured: deleting `.modal.show` from the current mount reds both legs at the post-upgrade click; never serving the current build reds both at the banner; never installing the previous generation reds both at the precache pin.
- **Landed in.** EMV-P3.
- **Scope.** **This is the mechanism, against the generation this repo published — not the bytes any live client holds.** The staged shell is byte-identical to `app/index.html` at `711b5bc` (verified at authoring time, and true only while the mount repoint stays the shell's only change since then); the staged worker is that generation's worker plus one comment block. A real client holds whatever was current when it last updated, and its HTTP-cache age is invisible from CI, so `docs/RUNBOOK.md` **Promotion + rollback** step 5 — open the app on a client installed **before** the deploy and see the modal — remains the only evidence about real bytes and is not made redundant by this invariant. Web channel only: in the Capacitor shell `/sw.js` is never re-fetched, so no waiting worker and no banner can exist and the update channel is inert (`LSC-DL-001`); APK replacement is that channel's only path and is not covered here. **When one mount version is shipped the property is vacuous by construction** — there is no previous generation to upgrade from — and the two legs skip with that reason printed rather than passing quietly; retiring `/m/v1/` is what produces that state. The leg-2 assertion records a **measured** outcome, not a designed one: the unaccepting client reaches the current mount because navigations are network-first and the new stylesheet lives at a URL it has no cached copy of. Change either half — a fix shipped at an existing mount URL, or a cache-first navigation strategy — and the assertion is expected to red, which is the point of asserting it rather than arguing it.

### EMV-P5-INV-001 — Nothing under `native/` names a module mount the shell does not run

- **Statement.** Every `m/v{N}/` token in a text file under `native/`, excluding the derived directories named below, names the mount version the **shell** references — derived from `app/index.html`'s stylesheet `<link>` via `currentMount()`, never written down in the guard. A token of that shape whose version is anything else fails; a token of that shape the guard cannot classify as a version at all also fails, rather than being skipped. **This is a static property and carries no runtime claim** (`AGENTS.md` §11): it boots nothing, starts no emulator, and reads only the tree. Which mount version is written down where *is* a property of the tree, and reading the tree is the right instrument for it.
- **Enforced by.** `app/tests/native-shell.spec.js`, third `describe` block (the `contract` project), run via `scripts/parity-suite.sh` in the `parity` job of `.github/workflows/ci.yml` — **on every push**, unlike `android-instrumented`, which is `pull_request` + `workflow_dispatch` only. It walks `native/` as a whole, skips binaries by NUL byte, and carries an anti-vacuity test that asserts the walk actually reached the two instrumented sources, the stager and `capacitor.config.json` — an exclusion typo that emptied the walk would otherwise make it pass while checking nothing. Soundness by mutation, measured, four ways: restoring the previous generation's specifier in `BridgeSmokeTest`, its asset path in `StoreEngineTest`, or its path in `ExportSinkPlugin`'s javadoc each reds on exactly that line; injecting `m/vNEXT/` into `capacitor.config.json` reds on the unclassifiable-token direction. All four were reverted and the tree verified green again.
- **Landed in.** EMV-P5.
- **Scope.** **The property is the INVERSE of "the staged root carries the current mount and only it", and the difference is load-bearing.** A mount bump is copy-forward, so both generations stay shipped on purpose — an already-installed client is still holding the previous one, and `EMV-P3-INV-001` is what proves it can get off it. What must not exist is a reference **outside** the mount root that still points at the frozen generation, because such a reference does not 404: it resolves, against bytes nobody runs. Covers `native/` only. It says nothing about `app/**`, where `currentMount()`'s own single-version check governs the shell and the delivery guard governs `OFFLINE_URLS` and the preload hints; and it deliberately never scans `app/m/**`, where the frozen generation legitimately names itself. **Excluded, each because the directory is derived and its content is nobody's reference to maintain:** `node_modules/` (third-party trees), `www/` (the gitignored staged web root — a byte copy of `app/m/`, so it carries **both** generations by design, governed by `LSC-P1-INV-002` instead), `android/app/src/main/assets/` (the gitignored `cap sync` output, the same both-generation copy), `build/` and `.gradle/` (Gradle output), and `capacitor-cordova-android-plugins/` (generated by `cap sync`). **A comment counts.** The guard does not strip comments and is not meant to: `ExportSinkPlugin`'s javadoc named the frozen generation's `export/config.js`, and prose that misdirects the next reader to bytes nobody runs is the same drift as code that imports them. The consequence, accepted deliberately, is that prose describing the historical defect must say "the previous generation" in words rather than encode a version number — which is the better sentence anyway, since a written-down number goes stale at the next bump exactly like the references this invariant exists to catch. **It does not carry the store claim.** That the app opens its encrypted store at boot on the native channel is executed only by `BridgeSmokeTest` on an emulator in `android-instrumented`; this guard would stay green against a shipped mount that was thoroughly broken, and is worth having only because it runs on every push, which is the window `LSC-DL-005` debt 13 leaves open.

### RSN-P1-INV-001 — Release packaging is fail-closed without a usable signing environment

- **Statement.** **An incomplete or unusable signing environment produces no release APK — ever, and never one signed by the debug key.** The enforced property is the default, not the exemption: release **packaging** tasks (`package*Release` / `sign*Release` of the app module) abort at `gradle.taskGraph.whenReady`, before any task executes, unless all three of `ANDROID_KEYSTORE_PATH` / `ANDROID_KEYSTORE_PASSWORD` / `ANDROID_KEY_ALIAS` are present, the path names a readable file, that file opens as PKCS12 under the password, it holds an entry under the alias, and `versionCode` resolved from `git rev-list --count HEAD` in a complete clone. Each abort carries a bare `THEYGROW_RELEASE_*` token and prints no secret value — the keystore-open failure names the exception class only, and the alias failure names neither the alias asked for nor the aliases present. Debug builds and `lint*Release` are deliberately outside the matcher and are unaffected.
- **Enforced by.** `native/android/app/build.gradle` (the guard) + `.github/workflows/ci.yml`, the `android` job's step *Release packaging fails closed without a usable signing environment*, **on every push**. It **executes** the real packaging task twice — once with no signing environment, once against a keystore minted in-run whose password is deliberately wrong — and asserts the specific token in the build log **and** that no APK exists under `native/android/app/build/outputs/apk/release` afterwards (the step runs with `working-directory: native/android`, so its own `find` names that path relatively); any other failure prints the log tail rather than passing. Both passwords are random at runtime, so the leg needs no real secret. It is also the guard's **only** detector: delete the guard from `build.gradle` and this step reds, where a source scan for the same property would go quietly green.
- **Observed.** CI run **31896998977** is the first run in which this step's own verdict is on the record: both executed legs printed `ok: THEYGROW_RELEASE_SIGNING_MISSING` and `ok: THEYGROW_RELEASE_KEYSTORE_UNREADABLE`. Until RSN-P3 the step carried no `if:`, so it inherited `if: success()` and was **skipped on every run where the comparator step ahead of it went red** — which, from RSN-P2 until RSN-P4, was every run. The claim "on every push" above was therefore true of the workflow's intent and not of its traffic for that window; `if: ${{ !cancelled() }}` (RSN-P3, `RSN-DL-004` (g)) is what makes it true of both. What moved here is evidential, not behavioural: the guard is unchanged, and its central claim moves from implemented to **observed**. *Amended 2026-08-16 (XPT-P2, `XPT-DL-002`).* On the `android` job of CI run **31898815923** (`pull_request`, commit 637660b) — and on its push-event sibling **31898813639**, the same commit — this step ran with the comparator step **ahead of it green**, so it would have run under the old `if: success()` as well: the window described above is closed, and both legs printed the same two `ok:` lines there and again on run **31900363700** (push on `main`, fa744a2). What did **not** move: 31898815923 is the address of a job verdict, not of a green run — that run's `quality` job failed at *Ruff format (check)*. And the Scope below is re-affirmed unchanged rather than weakened: both run logs print exactly two `ok:` lines and no others, so the three unexecuted legs are still unexecuted.
- **Landed in.** RSN-P1.
- **Scope.** **Two of the guard's four legs are executed; the other three are not, and that is stated rather than implied.** Unexecuted: the "path does not name a readable file" branch of `THEYGROW_RELEASE_KEYSTORE_UNREADABLE`, `THEYGROW_RELEASE_KEY_ALIAS_ABSENT`, and **`THEYGROW_RELEASE_VERSIONCODE_UNRESOLVED`** — the last being the leg the release workflow's `fetch-depth: 0` contract rests on, so "a shallow checkout fails the release closed" is implemented and unobserved. What would close all three is worked out in `RSN-DL-003`: additional legs in this same step passing a **correct** password and alias, where the `android` job's default depth-1 checkout already supplies the shallow tree. This invariant is about **packaging refusing to proceed**; it says nothing about the identity of an APK that *is* produced (`RSN-P2-INV-001`), nothing about key custody, and nothing about what the owner publishes. The guard runs at configuration time, so an AGP change that packaged release output under a differently-named task would fall outside the matcher — the pattern is `^(package|sign)[A-Za-z]*Release[A-Za-z]*$`, scoped to this module's task paths.

### RSN-P2-INV-001 — A release run only produces an APK whose signing certificate is the committed one

- **Statement.** A release run fails unless the certificate SHA-256 that `apksigner` reports for the built APK equals the fingerprint committed at `native/android/release-signing/cert-sha256.txt` — with an unsigned APK, output the parser cannot read, more than one signing identity, a missing or malformed baseline, and an APK `apksigner` refuses to verify all failing too, each with its own exit code and `THEYGROW_RELEASE_*` token. The comparison reads the **certificate** digest specifically, never the public-key digest `apksigner` prints beside it. The baseline is public and committed on purpose; moving it into the secret store that holds the key would void the check.
- **Enforced by.** Two gates with **different cadences, which are not interchangeable.** (1) `.github/workflows/release.yml` — the steps *The signature comparator is armed* and *Verify the APK is signed by our key*, running `scripts/verify_release_signature.py`. **This runs only on owner-triggered release runs (tag push / `workflow_dispatch`) and has never executed** — it is a release-time gate, not a per-push one, and nothing in ordinary CI traffic exercises it. (2) What runs **on every push**, and is what makes that verdict worth anything: `pytest app/tests/release` in the `quality` job (the comparator's logic, including the certificate-vs-public-key trap, the labelled mismatch print, and the pins that the committed baseline parses and still declares itself public), and the `android` job's step *The signature comparator rejects a foreign certificate*, which runs the **real** `apksigner` against the **real** debug APK and requires exit **3** — a green there would mean the detector cannot tell keys apart. Each release run additionally **arms** the comparator in-run against a fingerprint derived from the live baseline, so a green run carries its own evidence that the detector was live in that run rather than borrowing it from a test that ran elsewhere. *(Touched up in RSN-P4, per `RSN-DL-004`'s deferral: gate (2) also includes the stub-driven `--apk` suite `app/tests/release/test_apksigner_invocation.py` — which binary is resolved out of several, which stream is parsed, and that the diagnostic block appears on the blind verdicts and on no other — and the `android` job's fail-closed step now carries `if: ${{ !cancelled() }}`, so this step's red no longer suppresses the next detector's verdict. Since RSN-P4, `app/tests/release` also pins the parse against **recorded** apksigner bytes (`fixtures/apksigner-buildtools-37.0.0.txt`) and refuses each of the five near-miss digest lines that stand beside the answer in that same output.)*
- **The real-tool leg has never returned the 3 it asserts.** It returned **4** on runs 31894850072 and 31896998977, for a pattern defect diagnosed in the second and repaired in RSN-P4 (`RSN-DL-005`). "The comparator returns exactly 3 against a foreign certificate, on a real APK, with the real tool" is executed by that step and by nothing else — the per-push suite proves the parse reads a **recorded** shape, not that the tool still prints it — so it stays **unproven until a run shows it green**. That the leg failed **closed** throughout is the property this invariant is about; that it failed at all is what RSN-P4 addressed.
- **Amended 2026-08-16 (XPT-P2, `XPT-DL-002`) — the real-tool leg has now returned it.** The `android` job of CI run **31898815923** (`pull_request`, commit 637660b, 2026-08-15) is the first in which the step *The signature comparator rejects a foreign certificate* passed; its push-event sibling **31898813639** on the same commit passed too, so "first" has two addresses. The step never prints the number — its own wrapper is `if [ "${status}" -ne 3 ]; then … exit 1`, so a green step **is** exit 3, and that is an inference from the guard rather than a value read out of the log. What the log does carry is the verdict with both sides labelled, `THEYGROW_RELEASE_CERT_MISMATCH` followed by `apksigner reported:` and `baseline expects:` on their own lines, and the tool that produced it: `apksigner` from **build-tools 37.0.0**, named by the resolution line RSN-P4 added. No digest value is transcribed here, deliberately — the APK is debug-signed with a key minted per run, and the reported digest is a different 64-hex value on every run that prints one. **31898815923 is not a green run and must not be cited as one:** its `quality` job failed at *Ruff format (check)* and `pytest app/tests/release` was **skipped** there, so that run discharges the real-tool leg and says nothing about the per-push suite. Both have since been green **in the same run** — **31899851227** (`pull_request`, 7d2ff2c) and **31900363700** (push on `main`, fa744a2), the second of which is the property holding on the default branch. **Not discharged, and not softened by any of the above:** `.github/workflows/release.yml` has **never executed on any trigger** — it is registered and active, and its run list is empty — and the comparator's **positive** verdict against a real release-signed APK has never been observed. Every executed leg to date is a negative one, by construction: the positive path needs the real key. The Scope's sentence to that effect stands as written.
- **Landed in.** RSN-P2.
- **Scope.** Covers the **certificate identity of what a release run produces**, at the moment it produces it. It does not cover what the owner publishes afterwards: publication is by hand, outside CI (`permissions: contents: read`), so nothing machine-checks that the file uploaded to a download page is the file this gate passed — the `sha256` sidecar and the RUNBOOK's `sha256sum -c` step are what the owner has instead. It says nothing about key custody, about the keystore backup being restorable (`docs/RUNBOOK.md` → **Keystore backup and the restorability check**, owner-run and unautomated), or about the APK's contents. **The positive verdict has never been observed** — every executed leg to date is a negative one, by construction: the positive path needs the real key. Correcting the baseline is a two-file change (the fingerprint file and the `EXPECTED` constant in `app/tests/release/test_verify_release_signature.py`), which is deliberate — a baseline edited alone reds the per-push suite.

### XPT-P1-INV-001 — No archive payload can ride the plugin call that opens the file picker

- **Statement.** The call that starts `ACTION_CREATE_DOCUMENT` carries a reference to a staged transfer and nothing else. The enforced property is the **fail-closed default**, not the happy path: `ExportSinkPlugin.createDocument` refuses any option key outside the four it declares (`transferId`, `filename`, `mimeType`, `totalBytes`) and any options object above `LAUNCH_OPTIONS_MAX_BYTES`, and it refuses **before** the intent is built, so an undeclared key means no picker and no document rather than a large parcel. The consequence asserted alongside it is the one that killed the process: the state Capacitor persists for that call — which it writes twice, at `Bridge.saveInstanceState` — stays under 8 KiB for an archive of any size.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/ExportTransferTest.java`, run by `./gradlew connectedDebugAndroidTest` in the `android-instrumented` job of `.github/workflows/ci.yml` (`pull_request` + `workflow_dispatch`). Two legs, deliberately separate. **(1) The refusal is fired against an input the test builds in-run** — a payload larger than the binder limit, put on the launching call exactly as the pre-repair code did — and the plugin must reject it by name with no activity started (`Intents.assertNoUnverifiedIntents()`). **(2) The persisted state is measured at the instant of launch**, through an espresso-intents `respondWithFunction` hook, and **carries its own control**: the same measuring function is run against a synthetic bundle in the OLD shape, carrying a payload the size of the one in the 2026-08-15 crash, and that number must EXCEED the bound the real one must stay under. Without the control, a measurement that silently returned zero would read as a pass. Both numbers are logged. On every push, `app/tests/export-contour.spec.js` additionally asserts that the ceiling and the declared key set are the same on both sides of the bridge (`EXPORT_CONFIG.sinkLaunchOptionsMaxBytes` ↔ `LAUNCH_OPTIONS_MAX_BYTES`), and `app/tests/export-sink-unit.spec.js` asserts the app builds that call with exactly those four keys — both static, and neither carries the runtime claim.
- **Observed.** The `android-instrumented` job of CI run **31912350123**, on the pull request for **#24**, is the first in which both legs above executed, and both passed: the refusal fired against the payload the test builds in-run, with no activity started, and the persisted state was measured at the instant of launch beside its old-shape control. That discharges the **Not yet observed.** field this entry carried from XPT-P2 (`XPT-DL-002`) until DIA-P1, and it is the run that field named as its discharge condition — the replacement was a scheduled pre-merge step of the XPT milestone and is made here. What moved is evidential, not behavioural: the guard is unchanged, and its central claim moves from implemented to **observed**. The two per-push assertions named in **Enforced by** remain static and still carry neither leg. *(Amended 2026-08-16, DIA-P1, `DIA-DL-001`.)*
- **Landed in.** XPT-P1.
- **Scope.** Covers the options of the picker-launching call and the state Capacitor persists for it. It does **not** cover the system file picker: no test here opens DocumentsUI, and the stub means the activity is never really stopped, so what is asserted is that the payload is absent from the state — not that a real SAF round trip was survived. That remains the owner-run smoke in `docs/RUNBOOK.md`. It says nothing about the other two sink methods, whose calls are never persisted because they start no activity, and nothing about how Capacitor may choose to persist plugin calls in a future version — which is precisely why the chunk size is bounded well below the limit rather than merely below it.

### XPT-P1-INV-002 — An archive several times past the binder limit reaches the parent's document whole

- **Statement.** Pressing the export control in the app writes a **complete, valid** archive to the document the picker returned, for a payload at least five times the ~1 MB binder limit and about three times the archive that crashed the family device: non-zero, `PK\x03\x04` at offset 0, an end-of-central-directory record present, every entry readable to EOF with its CRC verified by `java.util.zip`, and `README.txt`, `index.json`, `MANIFEST.json` and `print/archive.pdf` all present.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/ExportTransferTest.java`, in the `android-instrumented` job. It boots `MainActivity`, seeds the journal through the **shipped** write path (`appendEntries()` out of the mount the APK carries), presses `#exportBtn` and then `#exportRunBtn`, and waits on the surface's own status line — so what executes is `surfaces/export.js` → `export/run.js` → `export/sink.js` → the injected bridge → the plugin, with only the picker stubbed. The fixture size is asserted, not assumed: an archive below the bound fails the test by name and names the constants to raise, because the size of the fixture is where this defect hid for four packets.
- **Observed.** The `android-instrumented` job of CI run **31912350123**, on the pull request for **#24**, is the first in which this end-to-end export executed, and it passed: the app booted, the journal was seeded through the shipped write path, the export control was pressed, and the archive that reached the document was complete and valid past the bound the test refuses to be satisfied below. That discharges the **Not yet observed.** field this entry carried from XPT-P2 (`XPT-DL-002`) until DIA-P1, on exactly the run that field named, as the scheduled pre-merge step of the XPT milestone. The claim moves from implemented to **observed**; nothing about the code or the fixture changed with it. *(Amended 2026-08-16, DIA-P1, `DIA-DL-001`.)*
- **Landed in.** XPT-P1.
- **Scope.** Covers the transfer and the write, on an emulator, with a stubbed picker. It does **not** cover the real picker, the real device, or the archive's *content* — that the bytes are the family's history correctly projected is `pytest app/tests/export` and the PDF/A gate, which run against the shipped builder off-device. It is a claim about one export of one fixture, not about determinism (that is the RUNBOOK's two-exports step) and not about any archive larger than `MAX_STAGED_BYTES`, which the plugin refuses outright. Because it runs on `pull_request` and dispatch only, a regression here can arrive several commits after its cause — the same accepted trade as every other claim in that job.

### DIA-P1-INV-001 — The browser source is read and never written, and each leg can go red alone

- **Statement.** The browser's `localStorage` under the production origin holds the **only** copy of this family's history — not the freshest copy, the only one — and nothing in the transfer writes to it. Two legs, deliberately separate because they fail for different reasons and neither substitutes for the other. **(a) STATIC:** no module in the handoff page's graph imports a WRITER binding from the mount's `core/storage.js` (the import form, not the call form: a module that never imports a writer cannot become a door by a later edit to its body), none calls `setItem` / `removeItem` / `clear`, the page names neither the app entry — whose boot calls `removeOrphanedAgeFilter()`, the one existing write in the graph — nor a service-worker registrar, and every Web Storage access in the graph is one the guard could **name**, counted so an access it cannot name cannot pass by being invisible. **(b) EXECUTING:** a real browser loads `/transfer.html` over a seeded source and the button is **pressed**, with the run observed twice independently — every mutating `Storage.prototype` call recorded in-page, and the whole of `localStorage` (every key, in index order, with every value) compared before and after. The two observations are not redundant: a write that sets a key to the value it already holds is invisible to the state comparison, which was **measured** by mutation. On the native side the same property is asserted for the WebView's own origin during a real import. *(Namespace note: milestone L2 "local diary" takes `DIA-` for decisions and invariants because `L2-DL` is already owned by the A2 staging milestone; packets are `DIA-P{k}`. Declared in `docs/execution-map.md`, following the `LSC-` / `A2`-`L2` precedent. The "Entry format" footer below is intentionally left unedited.)*
- **Enforced by.** `app/tests/handoff-source.spec.js` for leg (a) and `app/tests/handoff-transfer.spec.js` for leg (b), run by `scripts/parity-suite.sh` in the `parity` job of `.github/workflows/ci.yml` — leg (a) in the `contract` project, leg (b) in `behavior`, **on every push**. `app/tests/storage-seam.spec.js` covers the same graph under `LSC-P1-INV-001` since `transfer.html` joined its shell list. The native half is `native/android/app/src/androidTest/java/app/theygrow/HistoryTransferTest.java::the_transfer_writes_nothing_to_web_storage`, in `android-instrumented`. Soundness by mutation, measured: a one-line `writeOnboardingDismissed()` in the page reds leg (a) immediately, and red leg (b) only after the call recorder was added — before it, that mutation passed, which is why both observations exist.
- **Observed — the device half.** The band's browser halves (a) and (b) have been observed on the host since DIA-P1, by the `parity` job on every push, and **their wording is unchanged**. What was not observed until now is the native side — the same property asserted for the WebView's own origin during a real import — and it is, by `the_transfer_writes_nothing_to_web_storage` in the `android-instrumented` job of CI run **31956524510** (`workflow_dispatch`, branch `feat/l2-local-diary`; 26 tests, 0 failures). The leg establishes the transfer LANDED before it says anything about writes: the recorder is installed and the untouched world is read first as an explicit control (`[]` before anything happens), the surface reports the import and the marks are read back from the journal through the shipped store, the write set **sampled during the transfer** is `[]`, and a probe then drives `setItem`/`removeItem` through the recorder so that the empty list cannot mean a recorder that never ran. **This leg was red in the two dispatches before it and green for the wrong reason in the one before those** — it shared a fixture with another leg, then pressed a button that was not offered — so what discharges this half is a leg that only became able to see its own subject in `DIA-DL-003`. *(Added 2026-08-16, DIA-P1 closing evidence step; reasoning in `DIA-DL-002` and `DIA-DL-003`.)*
- **Landed in.** DIA-P1.
- **Scope.** Covers the handoff page's module graph and what it does to storage when it runs, on the production-origin channel. **Leg (a) alone does not carry this invariant and must never be read as carrying it** — it proves a property of a set of files, not a behaviour (`AGENTS.md` §11), and the export guard that asserted markup for a surface no user could reach (`EMV-DL-001`) is the precedent it exists against. Like every source-text guard here, leg (a) is a drift gate over honest code: an obfuscated dynamic lookup would not be caught. It does **not** cover the browser's storage on a real device — no emulator in CI holds the family's origin — so "the family's actual history survived" is the owner-run smoke in `docs/RUNBOOK.md`, and nothing here makes that step redundant. **Clearing the browser after a confirmed transfer remains a separate, explicit owner action; no packet in this milestone performs it**, and this invariant asserts the absence of the write rather than promising the parent will never make it. **What the device half's green run does NOT reach**, so the band cannot be read as "the transfer is proven end to end": that Chrome resolves `intent://…;package=app.theygrow;end` to this package — the leg receives a **synthetic Intent** fired at the activity, standing in for the browser and for nothing else; the system document picker's UI and fallback A on a real device; any link longer than the fixture; and the real chain through the production handoff page over the family's own origin, which needs that page deployed and is the owner smoke in `docs/RUNBOOK.md` **after promotion** — the step this invariant has said from the start that nothing here makes redundant.

### DIA-P1-INV-002 — No payload rides a cross-process call, and every refusal happens before anything is staged

- **Statement.** The family's history crosses two process boundaries on its way into the app, and neither carries it as a parcel that could approach the ~1 MB binder limit this repository already died at (4 630 924 bytes, 2026-08-15, `XPT-DL-001`). **The bridge hop carries a reference:** the Intent is consumed natively in `MainActivity` — on `onCreate` **and** on `onNewIntent`, since the activity is `singleTask` and a returning parent takes the second path — and the plugin decodes, verifies and stages the payload before the WebView is told anything; `pendingTransfer()` then answers with `{transferId, totalBytes, sha256, formatVersion}` and the app **drains** the staged buffer in pieces bounded by `TRANSFER_CONFIG.transferChunkBytes`. **The browser hop is bounded instead**, because a web origin has no handle to pass: the handoff page decides link-versus-file against `linkMaxBytes` **before the link is built**, and the receiver re-applies the same ceiling rather than trusting the sender to have applied its own. **Every guard refuses before a byte is retained** — a foreign query or option key by name, options or payload past their ceiling, a declared byte count that is not the decoded one, a digest that does not match, a format version in either direction — and each refusal prints bounded evidence (scheme and host, the offending key, declared beside actual, the ceiling in force) and never the payload, anything decoded from it, or either digest. A refusal is reported as a member of the closed `refusal` code list and the app falls to the file path automatically, with one line of instruction and no fork shown to the parent.
- **Enforced by.** `native/android/app/src/main/java/app/theygrow/HistoryTransferPlugin.java` (the guards) + `native/.../androidTest/.../HistoryTransferTest.java` (`the_receiver_refuses_a_truncated_link`, `..._a_foreign_query_key`, `..._a_payload_past_the_ceiling`, `..._an_unknown_format_version`, `a_deep_link_delivers_the_history`, `no_bridge_call_carries_the_payload`), run by `./gradlew connectedDebugAndroidTest` in the `android-instrumented` job (`pull_request` + `workflow_dispatch`). Each refusal leg builds its own violating input in-run. The parcel leg **carries its own control**: the same measuring function is run against a synthetic bundle in the payload-on-the-call shape, and that number must exceed the bound the real one must stay under — both are logged. On every push, `app/tests/transfer-seam.spec.js` asserts the ceilings, the declared key sets, the method allowlist in **both** directions (a `@PluginMethod` the app never calls is still callable from the WebView) and that every refusal code the plugin can emit is declared in the taxonomy; `app/tests/transfer-drain-unit.spec.js` drives the shipped drain against a fake bridge. **Both are static or fake-driven and neither carries the runtime claim.**
- **Observed.** The `android-instrumented` job of CI run **31956524510** — `workflow_dispatch`, branch `feat/l2-local-diary` — is the first in which any of this executed, and it passed: **26 tests, 0 failures**, `HistoryTransferTest` in 21.576 s. Which leg observed what, named rather than summarised. **The transport:** `a_deep_link_delivers_the_history` — a link built by the mount's own `transfer/format.js` inside the app's WebView is delivered to `MainActivity`, decoded and staged before the WebView is told anything, drained through the bridge, and the unchanged importer writes the journal, with the marks read back from the store through the shipped module rather than from the surface. **The reference-only property:** `no_bridge_call_carries_the_payload` — completion read from the shipped seam's own last act, at least one chunk actually read, no `base64` / `payload` / `archive` / `profiles` option on any recorded call, and the persisted launch state measured beside a synthetic old-shape control that must exceed the binder limit for the measurement to mean anything. **The refusals, each against an input built in-run and each before a byte is retained:** `the_receiver_refuses_a_truncated_link`, `..._a_foreign_query_key`, `..._a_payload_past_the_ceiling`, `..._an_unknown_format_version`. That discharges the **Not yet observed.** field this entry carried from DIA-P1, on exactly the condition it named — a green `android-instrumented` job on this branch by owner dispatch. **Provenance, because "green" alone would mislead:** this was the **third** dispatch. Runs 31950709031 and 31954630121 failed for **harness faults only, never a product one** — seven legs and then one — and are recorded in `DIA-DL-002` and `DIA-DL-003`; the product code under test is unchanged across all three. The per-push guards named in **Enforced by** remain static or fake-driven and stand in for none of this. *(Amended 2026-08-16, DIA-P1 closing evidence step; reasoning in `DIA-DL-001`, `DIA-DL-002` and `DIA-DL-003`.)*
- **Landed in.** DIA-P1.
- **Scope.** Covers what crosses each boundary and what the receiver refuses. It does **not** cover the browser: no test here drives Chrome, so whether `intent://…;package=app.theygrow;end` is resolved to this package, and how a real browser behaves on a URI between the fixture size and `linkMaxBytes`, are the owner-run smoke's — **the 16 KiB ceiling is measured on the payload side only, against the shipped builder, and no test exercises a link above the fixture size.** That gap is bounded rather than open: a truncating browser produces a payload whose length or digest disagrees with the declared values, the receiver refuses it as `size_mismatch` or `checksum_mismatch` before staging, and `the_receiver_refuses_a_truncated_link` is the leg that executes exactly that refusal. It says nothing about the system document picker's UI (deliberately not automated since L1-P3), nothing about how a future Capacitor version may choose to persist a plugin call — which is why the chunk size is bounded well below the limit rather than merely below it — and nothing about the importer, whose contract this packet reuses unchanged (`LSC-P4-INV-002`). **Because the job runs on `pull_request` and dispatch only, a regression here can arrive several commits after its cause** — the standing trade accepted for every claim in that job. **The green run above narrows none of the gaps named in this paragraph, and "observed" must not be read as "the transfer is proven end to end."** The legs fire a **synthetic Intent** at the activity, which stands in for the browser and for nothing else — so Chrome's resolution of `intent://…;package=app.theygrow;end`, the document picker's UI with fallback A on a real device, and any link longer than the fixture are exactly as unobserved after the run as before it. Nor does the run reach the real chain through the production handoff page over the family's own origin: that needs the page deployed, and it is the owner smoke in `docs/RUNBOOK.md` **after promotion**.

### DIA-P1-INV-003 — The mount is addressed by derivation, and nothing under `app/tests/` names a generation the shell does not run

- **Statement.** Two halves. **(a) STATIC:** no reference under `app/tests/` names a `m/v{N}` mount generation other than the one the shell runs — derived from `app/index.html`'s stylesheet `<link>`, never written down in the guard — and neither do the running mount's own config modules, whose four self-referencing knobs (`STORE_CONFIG.schemaUrl`, `EXPORT_CONFIG.declarationUrl` / `fontUrl` / `iccUrl`) are computed from `import.meta.url` instead of repointed by hand at every bump. A mount token the guard cannot classify as a version fails rather than being skipped. **(b) EXECUTING:** a real browser evaluates those shipped config modules at a real origin, on **both** delivery channels, and every derived URL both equals the running generation's asset path and `fetch`es it with a `200` and a non-empty body.
- **Enforced by.** `app/tests/mount-reference.spec.js` for half (a) — the tree walk plus the second describe block over the running mount's config modules — and `app/tests/mount-derivation.spec.js` for half (b), in the `behavior` project (nginx mirror) and the `native` project (staged Capacitor web root). Both run via `scripts/parity-suite.sh` in the `parity` job, **on every push**. Half (a) carries a **self-proving arm-check** that assembles a stale, a current, a malformed and a placeholder token from the current mount at run time and asserts the classifier's verdict on each — necessary because there are **zero** live mount literals under `app/tests/` today, so the assertion is green against an empty set and would stay green if the classifier were broken. Soundness by mutation, measured both ways: a derivation pointed at a missing asset reds half (b) only; a literal that is **correct today** reds half (a) only.
- **Landed in.** DIA-P1.
- **Scope.** **This guard scans STRING LITERALS where `EMV-P5-INV-001` counts a comment, and the divergence is deliberate rather than an oversight.** Under `native/` that invariant reds on prose, on the argument that a javadoc line misdirecting the next reader to bytes nobody runs is the same drift as code importing them. Here the subject is narrower: a reference that **resolves**. The failure class named in the handoff is `build-artifact.mjs` and `test_write_path_projection.py`, which stayed pinned across two bumps and stayed green because a copy-forward bump leaves the frozen generation shipped — prose does not resolve and cannot do that. The reason for the narrowing is the corpus: every `m/v{N}` token under `app/tests/` today is a historical statement whose value **is** the number ("EMV-P1 moved the shell to `/m/v2/`"), and adopting "a comment counts" would mean rewriting eleven such lines into version-free prose in files this packet otherwise does not touch. **What that costs, stated rather than left to be found: a comment under `app/tests/` may name a stale generation and this guard will not say so.** If such a comment ever describes CURRENT behaviour rather than history it is wrong and nothing here catches it. One further form is ignored wherever it sits — the placeholder `m/v{N}`, which names no generation, cannot resolve, and is the version-free phrasing `EMV-P5-INV-001`'s own Scope recommends; the arm-check pins that as a decision rather than an accident of the pattern. Half (a) excludes `__baselines__` and `__pycache__` as derived directories, and skips binaries by NUL byte. Half (b) cannot tell a derivation from a literal that is correct today, and half (a) cannot tell a derivation from one that addresses nothing — **neither substitutes for the other**, which was measured rather than argued. Neither half scans `app/m/**` beyond the running mount: the frozen generations legitimately name themselves, on purpose, and stay byte-untouched.

### DIA-P2-INV-001 — The archive is offered only on the channel that can produce it

- **Statement.** Two halves. **(a) STATIC:** the archive control lives in the header, ships carrying `hidden`, and no shipped module reveals it except the channel gate; the community-chat control it used to sit beside exists nowhere in the shell or the stylesheet. **(b) EXECUTING:** a real browser loads the shipped shell and the control is present in the document and **not visible**, while a control this channel does offer is visible in the same shot; on a page that has taken the native branch the same control **is** visible, and pressing it opens `#exportModal` with a computed `display: block` that closes again on the close control. The web channel cannot produce an archive at all — there is no web branch in `export/sink.js` and `runExport()` refuses before reaching it — so offering the action was offering something that does not happen.
- **Enforced by.** `app/tests/export-contour.spec.js` for half (a) — *the export control lives in the header, and ships unrevealed* and *the chat control is gone from the shell entirely* — and `app/tests/channel-composition.spec.js` for half (b), in the `behavior` project, run by `scripts/parity-suite.sh` in the `parity` job **on every push**. The native branch is reached by an init script installing the smallest stand-in the channel probe reads (`window.Capacitor.isNativePlatform`), and the block asserts `window.IS_NATIVE_SHELL === true` before anything else, so a leg whose init script silently failed cannot pass by testing the web branch twice. Soundness was measured, not argued: the first run of half (b) went red on the shipped code, because `.header-action { display: inline-flex }` beat the `hidden` attribute and both controls rendered — the guard caught the defect the packet had just introduced, and `.header-action[hidden]` is the repair.
- **Landed in.** DIA-P2.
- **Scope.** Covers **which control the shell offers**, not what happens behind it. It says nothing about whether an archive can actually be written: the native leg installs no plugin, so the store does not open and the sink is unavailable there — that is `XPT-P1-INV-002` and `ExportTransferTest` on the emulator. It says nothing about the app's own header on a real device either; the parity suite serves both channels over plain HTTP, so the native branch here is a simulated probe result rather than a Capacitor shell. The static half would stay green against a shell that revealed everything at boot, and the executing half would stay green against a stylesheet whose archive modal was empty — neither substitutes for the other, and neither substitutes for the device.

### DIA-P2-INV-002 — The public download offer never points at a release that does not exist

- **Statement.** The download control is revealed only when the shell **declares** a published release and only off the native channel, and its address exists exactly once in the shipped tree. Concretely: the control ships `hidden` and carries no `href` in the markup; the address is `CHANNEL_CONFIG.apkReleaseUrl` and no other shipped `.js` or `.html` names a `/releases/` path; the decision function `shouldOfferApk(state, native)` returns true only for the declared token on the web channel, and treats a missing tag, an empty value and any near-miss token as "no release". With the state this build ships (`content="none"`), a real browser shows no download control; with a shell declaring the published token, the same real browser shows it, addressed at the declared URL.
- **Enforced by.** `app/tests/export-contour.spec.js` — *the download control ships unrevealed and carries no address of its own*, and *the download control name is one string in three places* — for the static half; `app/tests/channel-composition.spec.js` for the executing half, both branches: the shipped state on the real shell, and the reveal branch on the same real shell with the one attribute an owner flips changed before boot (the test asserts the mutation took, so the reveal leg cannot pass by re-testing the shipped state). Its truth table, including the fail-closed tokens, is driven against the **shipped** module in the same file. All of it runs in the `parity` job **on every push**.
- **Landed in.** DIA-P2.
- **Scope.** Covers **what this build offers and where the address is declared**. It cannot know whether a release actually exists — the declaration is an owner statement, and a tag flipped before publication would satisfy every assertion here while a visitor met an empty page. That ordering is a numbered step in `docs/RUNBOOK.md`, not a property of the code, and it is stated as such. It says nothing about the repository being public: that was checked once, by hand, at authoring time (`gh api repos/{owner}/{repo} --jq .private` → `false`), and a repository turned private later would break the link with nothing here going red. It says nothing about what is on the page the link opens, nor about the integrity of any APK — that is `RSN-P2-INV-001` and the RUNBOOK's distribution procedure.

### DIA-P2-INV-003 — A successful export leaves the dialog closed and its confirmation standing outside it

- **Statement.** When an export completes, the archive dialog closes and the confirmation the parent reads lives outside it, so it survives that close: the run is polled through the surface a parent watches, and the success verdict is read from `#exportDoneBanner` rather than from the status line inside the dialog, after which `#exportModal` no longer carries `show`. A failure keeps the dialog and its message.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/ExportTransferTest.java::the_export_writes_a_complete_archive_past_the_binder_limit`, in `android-instrumented` — the real controls pressed in the real app, against the real store and the real plugin, with the system picker stubbed. It is the only observer that reaches a *successful* save: producing one off-device would need a fake store and a fake sink, and a fake would prove the fake.
- **Observed.** `android-instrumented` run **31963730354** (`workflow_dispatch`, branch `feat/l2-local-diary`) — success, with `ExportTransferTest` green in **7.701 s** across both legs (5.937 s and 1.764 s). The leg that carries this invariant is `the_export_writes_a_complete_archive_past_the_binder_limit`: a real export of a real journal through the real store and the real sink, with only the system picker stubbed, after which the run asserts that `#exportModal` no longer carries `show` and that the success verdict it polled came from `#exportDoneBanner`. **`DIA-DL-004`'s falsifiable prediction held, and is recorded as checked rather than left to be assumed:** it predicted the leg would reach `saved` by reading the banner rather than the status line and then find the dialog `closed`, and that is what the run did — had the confirmation not been raised, the poll would have expired instead, which is the outcome that would have falsified decision (g). That dispatch was also the **first compilation of the amended `ExportTransferTest.java` anywhere** — the development host has no Android SDK and no Gradle, so it type-checked and dexed for the first time in that run, and it compiled clean.
- **Landed in.** DIA-P2.
- **Scope.** Covers the **transition after a successful save**, on one emulator image, with the picker stubbed. It says nothing about the system file picker's own UI, which this repository has declined to automate since L1-P3 and which the owner smoke exercises. It says nothing about the failure and cancellation paths beyond leaving them unchanged — no test presses a failing export — and nothing about the banner being legible or noticed, which is a human judgement the owner smoke makes. Because the job is not a per-push gate, a regression here can arrive several commits after its cause. **What the green above does NOT reach, named here so this entry cannot be read as "the export flow is proven for a parent":** the emulator is not the family's phone — it is a fresh API-34 image with a stubbed picker, and the observation that matters to a parent is the owner-run smoke after promotion (`docs/RUNBOOK.md`, *On-device smoke*), which is where the confirmation is read by the person it is for. The banner's **dismiss control**, the **failure path** and the **cancellation path** are unchanged by DIA-P2 and this leg exercises none of them: it presses one successful export and nothing else. And the **web channel has no export contour at all**, so nothing in this run says anything about it — what the web channel offers is `DIA-P2-INV-001`, on every push, and it is a different claim about a different surface.

### DIA-P3-INV-001 — An entry a parent writes lands in the store, with its two times apart and nothing declared for them

- **Statement.** Three halves, and none of them substitutes for another. **(a) MEANING:** the statements `store/records.js` ships, executed against the real frozen DDL, put the parent's day in `event_date_local` and the moment of writing in `entry_at_utc`, leave `event_at_utc`/`event_utc_offset_min` NULL **together** because the surface collects a day and not a moment, leave `sensitivity` NULL because nobody was asked, and — on an edit — overwrite the row in place, moving `updated_at_utc`, leaving `entry_at_utc` alone and appending nothing to `journal_entry`. **(b) SHAPE:** the app asks for the area and the first record in ONE transaction, for only the record on the second entry, for an `UPDATE` and never a second `INSERT` on an edit, and for no retry after a refusal. **(c) EXECUTING, ON THE DEVICE:** a parent presses the diary control in the real app, types an entry about a past day and saves it; the row is then read back **through the shipped module, out of the mount the APK carries**, and carries that day, that text, an unknown event instant, a real entry instant and no sensitivity — after which the same entry is edited through the same surface and the store holds one row, with the same id and an untouched journal.
- **Enforced by.** `app/tests/schema/test_diary_write_path.py` for (a) — sixteen tests running the shipped statements, read out of `store/records.js` rather than retyped, against the real frozen DDL in the `quality` job **on every push**; `app/tests/diary-write.spec.js` for (b), in the `contract` project, driving the shipped store modules against the recorder in `app/tests/support/fake-bridge.js`, which executes no SQL and says so about itself; and `native/android/app/src/androidTest/java/app/theygrow/DiaryEntryTest.java::an_entry_a_parent_writes_lands_in_the_store` plus `::an_edit_overwrites_the_entry_and_appends_nothing` for (c), in `android-instrumented`. **Soundness measured, not argued:** flipping `diaryAreaVisibility` to `child_shared` reds (a) and (b); writing `'not_sensitive'` into the insert reds both as well — the first mutation initially reded only (b), and `test_the_declared_visibility_is_the_private_one` was added because of it.
- **Observed.** `android-instrumented` run **31982061125** (`workflow_dispatch`, branch `feat/l2-local-diary`) — success, with `DiaryEntryTest` green **4/4** in 21.349 s. Half (c) is carried by two legs. `an_entry_a_parent_writes_lands_in_the_store` (4.401 s) read the entry back out of the real encrypted store through the shipped module: `count=1;id=49b7c221-2a12-4167-954b-1591798dd65d;body=match;eventDate=2026-02-01;eventAt=null;eventOffset=null;entryAt=set;entryAtValue=1786926707501;updatedAtValue=1786926707501;sensitivity=null` — the event pair NULL, the entry pair real, sensitivity undeclared. **Both halves of the event pair are read through a statement the leg issues itself, which is what no earlier field here could have said:** until DIA-P3R2 this leg asked `loadRecords` for `event_at_utc`, a column `RECORDS_SQL` does not select, so it compared `undefined` to `null` and had never once observed slot 11 (`DIA-DL-007` (f), (h)). The `absent` sentinel that now distinguishes an unselected column from a NULL one did **not** fire anywhere in this run. `an_edit_overwrites_the_entry_and_appends_nothing` (5.229 s) then overwrote it in place: `count=1;id=7a0ea67e-1678-4954-9e57-814ff8edaeae;body=match;eventDate=2026-01-31;eventAt=null;eventOffset=null;entryAt=set;entryAtValue=1786926716832;updatedAtValue=1786926718059;sensitivity=null` — one row, the same id before and after, `entry_at_utc` unmoved while `updated_at_utc` advanced, and the journal count unchanged. Reached green after two repair rounds (`DIA-DL-006`, `DIA-DL-007`).
- **Landed in.** DIA-P3.
- **Scope.** Covers **one entry, written and corrected**. It says nothing about many entries, about the list's ordering under real volume, or about `diaryListLimit` being the right bound — the limit is asserted only against a five-row fixture. It says nothing about search, which is P4. Half (a) is about SQL meaning on desktop SQLite and half (b) about what the app asks for, and **neither of them can see the plugin or SQLCipher**: only (c) does, and only on one emulator image. Because `android-instrumented` is not a per-push gate, a regression in (c) can arrive several commits after its cause. **What a green in (c) does not reach:** the emulator is not the family's phone, and the parity suite's native branch is a *simulated probe* — an init script installing `isNativePlatform()` with no plugin behind it — so nothing off-device says anything about a store that opens. That limit is unchanged by the discharge above: run 31982061125 is one API-34 emulator image, not a family's device.

### DIA-P3-INV-002 — A write the store refuses reaches the parent as itself, and never takes their text with it

- **Statement.** Two halves. **(a) EXECUTING, OFF-DEVICE:** when the diary cannot be written, the surface says which of the two reasons it is — the store did not open, or there is no child to attribute the entry to — offers no compose form it knows will refuse, and, when a save is refused, keeps the window open, keeps the text in the textarea character for character, keeps the chosen day, and leaves the save control enabled; and the sentence it shows names none of the words that would read as the parent's fault. **(b) EXECUTING, ON THE DEVICE:** with the engine genuinely out of pages, a save is refused with the DISK-FULL sentence and no row is written, the parent's text survives, and releasing the ceiling makes the same press succeed; and the same failure on the MARK path withdraws the tick, shows the disk-full explanation and not the store-did-not-open one, and sticks once the ceiling is released. The engine's own wording for a full database is one the shipped classifier recognises.
- **Enforced by.** `app/tests/diary-surface.spec.js` for (a), in the `behavior` project, run by `scripts/parity-suite.sh` in the `parity` job **on every push**; `native/.../DiaryEntryTest.java::a_full_disk_refuses_the_entry_and_keeps_the_text` and `::a_full_disk_withdraws_the_tick_and_says_why` for (b); and `native/.../StoreEngineTest.java::a_full_database_says_so_in_words_the_app_classifies`, which reads `DISK_FULL_MARKERS` out of the shipped `store/errors.js` in the APK's own assets rather than retyping them. **The arming proves itself:** SQLITE_FULL is produced by lowering `max_page_count` to the database's current size and then writing filler rows until one is REFUSED — the refusal of that filler is what says the store is full before the act under test — and each disk-full leg ends by releasing the ceiling and repeating the same act, which must then succeed. `app/tests/store-seam.spec.js` covers the SEAM half of (b) only — that when a statement fails and the wrapper's rollback fails too, the caller is handed the statement's failure, so a full disk still classifies as `StoreDiskFullError`; it starts no product and executes no SQL, and the parent-facing claim stays the device's (DIA-DL-007). **Soundness measured:** making the refusal clear the textarea reds (a); reverting `bridge.js`'s envelope reds three of `store-seam.spec.js`'s five legs.
- **Observed.** `android-instrumented` run **31982061125** (`workflow_dispatch`, branch `feat/l2-local-diary`) — success. Half (a) is green on every push; half (b) is carried by three legs, all green. **The arming was genuine in BOTH shapes, which is the precondition the earlier runs failed:** `arming: state=full;rows=1;ceiling=43;refusal=full:StoreDiskFullError;journalRows=19;journalRefusal=full:StoreDiskFullError` for the diary leg and `state=full;rows=7;ceiling=41;refusal=full:StoreDiskFullError;journalRows=24;journalRefusal=full:StoreDiskFullError` for the mark leg — the fixture refuses to call anything but a `StoreDiskFullError` full, so those two strings are themselves the classification evidence. `a_full_disk_refuses_the_entry_and_keeps_the_text` (5.705 s): `refusal verdict: refused-disk-full`, then `surface after refusal: text=kept;date=kept;modal=open;form=visible;save=enabled`, no row written, and the same press saved once the ceiling was released. `a_full_disk_withdraws_the_tick_and_says_why` (6.014 s): `tick verdict: checkbox=unchecked;modal=show;diskFull=visible;unavailable=hidden` — the tick withdrawn and the disk-full paragraph shown rather than the store-did-not-open one. `StoreEngineTest::a_full_database_says_so_in_words_the_app_classifies` (0.628 s) green within `StoreEngineTest` **9/9**.
- **What this run establishes that no earlier one did.** Two things, both about the seam under `DIA-DL-007`. **(1) The disk-full class survives the transactional seam.** Before the repair, a write through `executeSet` with a wrapper-driven transaction surfaced the ROLLBACK's failure instead of the statement's, carrying none of `DISK_FULL_MARKERS`. In this run **no `executeSet` error carries rollback text at all** (zero occurrences), and the engine's own words cross intact — `{"methodName":"executeSet",…,"error":{"message":"ExecuteSet: database or disk is full (code 13)"}}` at 00:31:41.827. **(2) The connection is not wedged after the seam's own rollback.** The wrapper's `ROLLBACK` still fails — twelve times in this run, now under `methodName: rollbackTransaction` (00:31:41.834, `cannot rollback - no transaction is active`), each swallowed by the seam so the statement's failure is what reaches the parent. The risk that swallowing it might leave the connection unusable was named in the DIA-P3R2 plan **with the post-release control as its detector**, and the detector is read rather than assumed: `tick after release: checkbox=checked;modal=hidden;diskFull=visible;unavailable=hidden`, and the diary leg's control press saved and read back `count=1`. A leg that refuses correctly and then cannot write again would have passed every assertion before the release.
- **Amended 2026-08-17 (DIA-P4R, `DIA-DL-009`) — the DIARY half of (b) is withdrawn: its green above came from an arming that has since produced a red on the same string.** The arming line quoted in **Observed** is evidence that the fixture reached *a* refusal, and of nothing beyond it. **A refusal proves the store full for the write that was performed and for no other.** `max_page_count` bounds the pages of the FILE, not its occupancy, and a statement that is refused leaves the file no larger than it found it — so the slack in partially-filled leaves is still there afterwards for a shorter row. The filler stopped at a 200-character row; the entry the leg then writes is 26 characters, and it fitted. On `android-instrumented` run **32012897363** (`workflow_dispatch`, this branch) `a_full_disk_refuses_the_entry_and_keeps_the_text` therefore went **red** — `expected:<refused-disk-full> but was:<listed>` — with an arming string **byte-identical to the one quoted above**. The run's logcat carries both halves fifty-eight milliseconds apart: `arming: state=full;…` at 09:03:45.287, and the app's own signal `[signal] diary.write outcome=complete failure_class=none chars=26 write_ms=44` at 09:03:45.345, on that same store. (The leg's `refusal verdict: listed` at .716 is the fixture reporting, not the store answering.) Two runs, one string, opposite verdicts: 31982061125's diary green was a page layout rather than the property, and this entry reported it as evidence. **What the repair changes is the fixture, not the product** — the app was right, and the store was not full for the write it was asked to make. `fillToCeiling` now runs a third record stage at the ACT's own size and returns `actRefusal`, which is what the leg now asserts; `refusal` stays in the line, asserted by nobody, because it is what made those two runs comparable at all. `app/tests/schema/test_store_corruption.py::test_a_full_store_still_takes_a_row_of_the_next_size_down` measures the same thing off the device on every push — after the 200-character refusal, four more rows of the act's size land, and only then does that size converge. **The MARK half of (b) is untouched, and the reason is the sharp form of the same rule: a refusal bounds every write no WIDER than the one refused, and errs only downward.** The record filler failed that test by being wider than the act — 200 characters against 26. The journal probe is on the safe side of it: one transaction carrying a journal entry and its detail, where the mark act (`markSkill`) commits two such entries and their details in one transaction. The probe's write is therefore no larger than the act's, so a store that refused the probe refuses the act too, and `journalRefusal` was always sound for the leg it arms — `a_full_disk_withdraws_the_tick_and_says_why` stands as observed on 31982061125. **RE-ESTABLISHED, and the run number goes in here now that the run exists: `android-instrumented` run 32044006357** (`workflow_dispatch`, this branch) — success, `DiaryEntryTest` **5/5**, `StoreEngineTest` **11/11**, `a_full_disk_refuses_the_entry_and_keeps_the_text` green with `refusal verdict: refused-disk-full`. **What makes this green different from the withdrawn one is readable in the arming line itself**, which is why it is quoted rather than summarised: `state=full;rows=1;ceiling=43;refusal=full:StoreDiskFullError;actRows=1;actRefusal=full:StoreDiskFullError;journalRows=19;journalRefusal=full:StoreDiskFullError`. The first four fields are byte-identical to 31982061125's and to 32012897363's, and they license nothing. **`actRefusal=full:StoreDiskFullError` is the field that licenses this leg** — the store refused a record of the act's own size — and `actRows=1` is the row that used to be the parent's entry getting through. A reader comparing this line with the one in **Observed** can see the difference without leaving the page, which is the whole reason `refusal` was kept in the answer. This field was written in after the run, not before it.
- **Landed in.** DIA-P3.
- **Scope.** Covers **a store that refuses a write**. It does not cover a device whose storage is actually exhausted: what is produced is an engine-level `SQLITE_FULL` from the real SQLCipher build, and a real low-storage phone also fails in ways SQLite never sees — the WebView failing to load, the system killing the process — none of which is exercised anywhere. Half (a) reaches only the two refusals a browser can produce; the disk-full and corrupt sentences are **not asserted off-device at all**, deliberately, since reaching them needs a store that opens and faking one would prove the fake. The mark half of (b) covers the classification and the sentence and nothing else about the mark surface. Nothing here says the wording is *good*, only that it is the right one of the set and that the text survives; whether a parent finds it clear is the owner smoke's judgement. **What the green of run 31982061125 does not reach, stated so the discharge is not read as more than it is.** The emulator is not the family's phone: what was produced is an engine-level `SQLITE_FULL` from the real SQLCipher build, by lowering `max_page_count` and filling to it. **Nothing anywhere exercises a device whose storage is actually exhausted** — such a phone also fails in ways SQLite never sees, the WebView failing to load or the system killing the process, and none of that is touched. The parity suite's native branch is a *simulated probe*, an init script installing `isNativePlatform()` with no plugin behind it, so nothing off-device says anything about a store that opens. And `app/tests/store-seam.spec.js` proves the seam's **call order and error precedence against a fake** — it starts no product and executes no SQL; the wrapper's own masking happens inside Java, below the bridge, and only the device run reaches it.

### DIA-P3-INV-003 — The diary is offered only on the channel that has a store, and the window it opens is really open

- **Statement.** Two halves. **(a) STATIC:** the diary control lives in the header, ships carrying `hidden`, and no shipped module reveals it except the channel gate. **(b) EXECUTING:** a real browser loads the shipped shell and the control is present in the document and **not visible**, while a control this channel does offer is visible in the same shot; on a page that has taken the native branch the same control **is** visible, and pressing it opens `#diaryModal` with a computed `display: block` that closes again on the close control. The web channel has no store at all — `store/bridge.js` is inert there by construction (`LSC-P1-INV-001`) — so offering the diary would be offering an action that cannot happen.
- **Enforced by.** `app/tests/show-rule-coverage.spec.js` and the ship-list directions in `app/tests/delivery-contract.spec.js` for the static half; `app/tests/diary-surface.spec.js` and the `.show`-rule loop in `app/tests/behavior.spec.js` (which now carries `diaryModal`) for the executing half — all in the `parity` job **on every push**. The native branch is reached by an init script installing the smallest stand-in the channel probe reads, and the block asserts `window.IS_NATIVE_SHELL === true` before anything else, so a leg whose init script silently failed cannot pass by testing the web branch twice. **Soundness measured:** revealing the control unconditionally reds the executing half.
- **Landed in.** DIA-P3.
- **Scope.** Covers **which control the shell offers and whether its window opens**, not what happens behind it. The parity suite serves both channels over plain HTTP and injects no plugin, so the native branch here is a **simulated probe**: the store does not open, and every diary surface reached in it is in its refusing state. The static half would stay green against a shell that revealed everything at boot, and the executing half would stay green against a diary that saved nothing — neither substitutes for the other, and neither substitutes for the device.

### DIA-P4-INV-001 — A search returns the searching participant's own diary, for the child on screen, and no one else's

- **Statement.** `record_fts` indexes **every** record in the store — every area, every participant, every child — so a `MATCH` alone would return the other parent's private diary. The shipped search statement therefore carries the SAME three scope predicates as the read path (`a.owner_participant_id`, `ac.child_id`, `a.visibility_class`) plus `r.kind`, all bound, and joins the index to `record` on the rowid that `content='record'` makes meaningful. The surface asks with the id the store minted and the child the parent is looking at, and with nothing else.
- **Enforced by.** `app/tests/schema/test_diary_search.py::test_a_search_returns_only_this_parents_records_for_this_child` and `::test_the_count_is_scoped_the_same_way`, against the real frozen DDL, over a fixture holding **two participants and two children** — the leg first asserts that the bare index really matches three rows across that boundary, so a scope filter that returned everything cannot pass a single-family fixture. The structural half is `::test_the_search_reads_the_index_and_scopes_through_the_area`, which reads the statement rather than running it and is labelled as such. That the SURFACE passes the right author and child is `app/tests/diary-search.spec.js::the store is asked with the shipped expression, this author and this child`, which reads the transcript of what crossed the seam. All in the `quality` and `parity` jobs, **on every push**.
- **Observed.** `android-instrumented` run **32044006357** (`workflow_dispatch`, branch `feat/l2-local-diary`) — success, with `DiaryEntryTest` **5/5**. The device half is carried by `::a_parent_searches_the_diary_and_a_lost_index_is_repaired_under_them`, and that leg searched the real encrypted store through the real plugin, with the shipped statement and its three bound scope predicates, from the document that wrote the entries: `села` returned **one** of the two entries (`found=1;empty=false;failed=false`), which is the narrowing this invariant is about — a scope filter that had dropped its predicates would have returned both. What moved is evidential, not behavioural: the statement is unchanged and its **Enforced by** suite has been green on every push since DIA-P4. This discharges the **Not yet observed.** field this entry carried from DIA-P4 (`DIA-DL-008`), and it is the dispatch that field named. *(Recorded 2026-08-17, DIA-P4-CLOSE — the discharge pass, whose decision-log address is `DIA-DL-010`.)*
- **What this run does NOT reach, stated so the discharge is not read as more than it is.** The leg runs on a fixture of **one** participant and **one** child — the two-participant, two-children boundary that makes "and no one else's" a real test is `test_a_search_returns_only_this_parents_records_for_this_child`, off-device, on every push. So the device half establishes that the scoped statement executes and narrows correctly **through the plugin and SQLCipher**; it does not re-prove the boundary. The emulator is not the family's phone, and `android-instrumented` is not a per-push gate, so a regression here can arrive several commits after its cause.
- **Landed in.** DIA-P4.
- **Scope.** Covers **which records a search may return**. It does not cover what an expression matches (`DIA-P4-INV-003`'s suite carries the word-form table) and it says nothing about deletion, sharing or L7 sync — `v_shared_journal` carries no record text at all, which is a different property enforced elsewhere.

### DIA-P4-INV-002 — No search term reaches a signal payload, a log line, or any telemetry

- **Statement.** What a parent types into the search box is the most family-identifying string this app holds: their own words about their own child, in memory at the moment a signal fires. It reaches exactly **two** places — the bound parameter of a parameterised statement, and the input where they left it. It reaches no payload, no statement text, and no console line. `diary.search` carries `outcome`, `failure_class`, `tokens`, `results`, `search_ms`, `rebuilt` and nothing else; `tokens` is a COUNT of words, in the same sense `diary.write`'s `chars` is a length. On a failed search the console line names the error class and the closed failure code and **never the engine message** — deliberately unlike the write path, where the message is printed because the body is a bound value the engine does not echo, while an FTS5 parse error would carry the whole expression built from what the parent typed.
- **Enforced by.** Structurally by `app/m/v6/core/signals.js`, which accepts numbers, booleans, null and declared codes and has no path that takes a free string, with `app/tests/signal-payload.spec.js` asserting the taxonomy, the provenance, the literal keys and the absence of any inline call in a payload. At runtime by `app/tests/diary-search.spec.js`: three legs — a search that found something, one that found nothing, and one the store refused — each watching every console line the page emits and asserting the term appears on none of them, each with an in-leg anti-vacuity check that the `diary.search` signal actually fired; plus `::the emitted signal carries counts, a timing, a boolean and nothing else`, which compares the emitted field set exactly, and `::the term reaches the store only as a bound value`, which asserts no statement text carries it. All in the `parity` job, **on every push**.
- **Amended 2026-08-18 (DIA-P5, `DIA-DL-010`) — the Statement was TRUE about this app's own code and FALSE about the device, and both halves have to be said.** **What holds, and was never in doubt:** nothing in `app/m/v6/` puts the term in a payload, in statement text or on a console line. That is structural in `signals.js`, it is asserted by the **Enforced by** suite above, that suite has been green on every push since DIA-P4, and this packet changes not one byte of it. **What was false, and the precise clause matters here of all places:** the sentence *"It reaches exactly **two** places"*. The expression built from the term reached a third — a logcat line — while the enumerated three (payload, statement text, console line) each remained true of it, since it crossed as a bound value on a Java `Logger.verbose` line and on no browser console line at all. Read "console line" loosely as "line in the device log" and the enumeration is false too; that slack is exactly what this packet exists to remove. `Bridge.callPluginMethod` (`Bridge.java:826-837` of `@capacitor/android@8.5.0`) logs `", methodData: " + call.getData().toString()` — the whole argument object, bound values included — whenever `Logger.shouldLog()` is true, which `CapConfig` resolves to *"whenever the build is debuggable"* by default. **So on every build the owner has ever installed, the expression built from what a parent typed went to logcat.** On run **32044006357** one such line exists per search — **5 of the 5** that run performed — and a reader who greps for the typed word finds **nothing**: the shipped prefix ceiling is 3 (`DIA-DL-008`), so «села» crosses as `("сел"* OR "сёл"*)` and «сесть» as `("сес"* OR "сёс"*)`. Beside them, the same trace carried the store's passphrase once and the child's name five times; the name's other 26 appearances that run came from a **second** emitter this entry never contemplated — Capacitor's JavaScript echoing plugin RESULTS to the console, whole diary rows included, forwarded to logcat by the same gate. **Why the named enforcer could not see it, which is the part worth generalising:** `diary-search.spec.js` watches a Playwright *page* console against an in-page fake bridge. The emitter is Java, below that bridge, in a process no browser test starts — the leg was not weak, it was pointed somewhere else, and no amount of care inside it would ever have reached this. **What makes the Statement true now:** `native/capacitor.config.json` sets `android.loggingBehavior` to `none`, which shuts that gate in every build, and `DIA-P5-INV-001` carries the device half with an executor that can fail. **This entry keeps its own subject — the app's own emissions — and defers the log surface to that entry rather than absorbing it.** The record is not that L2 broke privacy: the trace has been running since L1-P1, and what L2 added was diary text, search expressions, and this sentence.
- **Landed in.** DIA-P4.
- **Scope.** Covers the search path's own emissions and the statement it issues. It does not cover the DOM, where the term is present by design in the input the parent typed it into, nor the rendered results, which are the family's own text shown back to them through `textContent`. It is a property of this path, not a general proof about the app: the structural half of the signal surface is general, the runtime legs are about `diary.search`.

### DIA-P4-INV-003 — The retrieval index is derived: destroying it costs a rebuild, never a migration and never a row

- **Statement.** `record_fts` holds no normalisation decision and no data of its own (PDR-026 §4 rule 4), so losing it entirely is repairable from `record` alone. The product repairs it without being asked: a search that returns nothing over a diary that HAS entries runs FTS5's `rebuild` once per app session and re-runs the same query, and the parent is shown their entry rather than an explanation. The rebuild touches no journal row and no record. Two probes are asserted **dead** in the same suite so the next reader does not re-derive them: `count(*)` over an external-content index counts the content table and cannot see an emptied index, and the argument-less `integrity-check` passes over one.
- **Enforced by.** `app/tests/schema/test_diary_search.py::test_a_destroyed_index_is_repaired_by_the_shipped_rebuild` (FTS5's own `delete-all` for a real loss, the shipped `FTS_REBUILD_SQL` for the repair, and the record count asserted unchanged), `::test_counting_the_index_cannot_detect_that_it_is_empty` and `::test_the_argumentless_integrity_check_cannot_detect_it_either`, in the `quality` job. The app's control flow — that the repair fires on the observable symptom and **at most once per session** — is `app/tests/diary-search.spec.js::a search that found nothing over a diary with entries rebuilds and answers` and `::a second empty search does not rebuild again`, in the `parity` job, **on every push**.
- **Observed.** `android-instrumented` run **32044006357** (`workflow_dispatch`, branch `feat/l2-local-diary`) — success, `DiaryEntryTest` **5/5** and `StoreEngineTest` **11/11**. Both device claims executed. **(1) The ENGINE half:** `StoreEngineTest::the_index_repairs_itself_after_delete_all` emptied the index with FTS5's own `delete-all` on the SQLCipher build and repaired it with the shipped `FTS_REBUILD_SQL` — `fts rebuild: records=5 ms=2` — and `::a_query_of_the_shipped_shape_behaves_as_measured_on_the_device` ran the shipped query shape against that engine. **(2) The PARENT-FACING half:** `DiaryEntryTest::a_parent_searches_the_diary_and_a_lost_index_is_repaired_under_them` destroyed the index through the shipped seam and pressed the same search control, and the parent was shown their entry — `search after the index was destroyed: found=1;empty=false;failed=false;said=listed`. Not that a rebuild statement was issued, which would be this repository asserting its own mechanism back to itself. This discharges the **Not yet observed.** field this entry carried from DIA-P4 (`DIA-DL-008`), and it is the dispatch that field named. *(Recorded 2026-08-17, DIA-P4-CLOSE — the discharge pass, whose decision-log address is `DIA-DL-010`.)*
- **The numbers, and the shape of claim each one can bear.** **The rebuild:** `records=5 ms=2` — one run, one figure, and it is small enough that the interesting quantity is the corpus rather than the milliseconds. **The parent-visible wait is NOT one number, and writing it as one would be a false precision this milestone has already paid for three times.** Across the two runs whose suite contained this leg it is a RANGE: repairing **22 ms** on run 32012897363 and **41 ms** on run 32044006357 — the same corpus, the same emulator image, an 86% spread — while the ordinary wait beside it was **21 ms in both**. So what is established is that the repair costs a parent something on the order of tens of milliseconds here, and that the ordinary search is the stable term; the repairing figure is not reproducible to better than its own doubling, and any single value quoted from either run is that run's sample and not the measurement. **The corpus, attached because the numbers are meaningless without it:** five short sentences for the rebuild, two short entries for the search waits, on a fresh API-34 emulator. A real diary is a different measurement and none of this predicts it — a rebuild re-tokenises every record, so a parent with three years of entries is the case nothing here reaches.
- **Landed in.** DIA-P4.
- **Scope.** Covers **repair of the index**, not maintenance of it: nothing here detects staleness in advance, and by decision nothing tries (`DIA-DL-008` (e)). The measured numbers are five short sentences and two short entries on an emulator; a rebuild re-tokenises every record, so a real diary is a different measurement and nothing in this repository reaches it. The parent is never told a repair happened, which is a decision recorded rather than a gap.

### DIA-P5-INV-001 — Nothing a family wrote reaches the device log, and the diagnostic line that stayed is one the app decided to write

- **Statement.** Two halves, and the second is not a courtesy — it is what makes the first checkable. **(a) THE ABSENCE:** on a running device, the app's own process writes **no** child name, **no** birthdate, **no** diary text and **no** search expression to logcat, and emits **no** plugin-argument trace at all. The mechanism is one config VALUE reaching two independent mechanisms, which is worth stating precisely because a reader who thinks it is one mechanism will look in the wrong place when it breaks. `native/capacitor.config.json` sets `android.loggingBehavior` to `none`; `cap sync` mints that into the APK's own `assets/capacitor.config.json`, which is what the runtime reads. From there it makes `CapConfig.isLoggingEnabled()` false, and **(i)** `Logger.shouldLog()` — which every Java emitter consults, including `Bridge.callPluginMethod`'s `methodData` line and `BridgeWebChromeClient`'s console forwarding — returns false; **(ii)** separately, `JSExport.getGlobalJS` injects the same value as `window.Capacitor.isLoggingEnabled`, which `native-bridge.js:943` tests before calling `logFromNative`, so the JavaScript stops echoing every plugin RESULT — whole rows of diary text — through `c.dir(JSON.stringify(result.data))`. **(b) THE PRESENCE:** the `[signal]` channel survives that, carried by `app/theygrow/SignalConsoleClient`, which forwards a console line to `android.util.Log` **only** when it begins with `[signal] ` and drops every other line, including this app's own `console.error` sites. The allowlist is stricter than the gate it replaces: a signal payload cannot hold a free string by construction (`core/signals.js`), so the surviving channel is §4-safe by structure rather than by inspection.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/DeviceLogTest.java`, in `android-instrumented`, in **two legs that fail for different reasons**. `::the_app_writes_no_family_text_to_the_device_log` boots the app, puts a name, a birthdate, a diary entry and a search expression across the real bridge through the shipped modules, then reads **this whole process's** log with `UiAutomation.executeShellCommand("logcat -d …--pid=")` and counts six needles over all of it — not only over its own leg's window, because the Statement is about the process and a run holds seven other suites' traffic in the same one. `::the_installed_build_declares_the_knob_that_closes_the_trace` reads `capacitor.config.json` **out of the APK's own assets**, the way `MountAddress` reads the shell. That second leg exists because the first two guards share a blind spot: the static one reads the repository, the behavioural one runs on whatever the local sync produced, and the file the runtime actually reads is generated and gitignored — an APK assembled where `cap sync` was skipped or failed carries the old config, resumes tracing everything, and would pass both. Statically, `app/tests/native-shell.spec.js::the bridge does not trace plugin arguments to the device log`, in the `contract` project **on every push**, pins the source key — the device legs run on dispatch only, so without it a lost key could sit undetected for many commits. **Soundness measured, not argued, and in both directions.** Flipping the source key to `"debug"` reds the static leg and only it (1 failed / 1148 passed in `contract`). Deleting the key from the APK's generated asset — the fail-open above, reproduced by hand — reds **both** device legs together: the declaration leg names the missing key, and the behaviour leg returns `child-name=1;birthdate=1;diary-body=1;search-expression=1;trace-channel=25;store-passphrase=1`. The behaviour leg was also written before the fix and red against the unfixed build.
- **Three controls, because an absence assertion goes green for three different reasons and only one of them is the truth.** **(1) The matcher arms itself in-run:** before scanning anything, the same counter is run over a synthetic line built in the exact shape run 32044006357 produced, carrying this suite's own needles, and every needle must be found in it. **(2) The capture proves itself:** a Cyrillic control marker is written before the app launches and must be found in the scan — it proves the reader reaches this process, proves Cyrillic survives the read (the failure that would zero every needle at once), and marks where this leg's own traffic begins without ever calling `logcat -c`, which would destroy the CI job's artefact for every other suite. The needle scan itself deliberately covers the whole process rather than that window; the marker scopes the channel assertion. **(3) The reader's scope proves itself:** a second unfiltered dump must carry a line from another process. **And the needle is the EXPRESSION, not the typed word** — the prefix ceiling is 3, so «села» crosses as `("сел"* OR "сёл"*)` and a needle taken from the input box returns zero against a log that is leaking. **It also carries the JSON escaping**, because `JSObject.toString()` escapes the quotes; an unescaped needle was measured counting zero against a leaking log while this suite was written.
- **Observed.** `android-instrumented` run **32074105863** (`workflow_dispatch`, branch `feat/l2-local-diary`) — success, 36 tests, 0 failures, 1m29.07s, with `DeviceLogTest` **2/2**, `DiaryEntryTest` **5/5** and `StoreEngineTest` **11/11**. Both legs executed: `::the_app_writes_no_family_text_to_the_device_log` drove a child's name, a birthdate, a diary entry and a search across the real bridge and then read this process's own log, and `::the_installed_build_declares_the_knob_that_closes_the_trace` read `loggingBehavior` out of the APK's own assets. **The negative side, over the whole 79,144-line device-wide capture:** `methodData:` **0**, `V Capacitor` **0**, `Capacitor/Console` **0**, `passphrase` **0**, `setEncryptionSecret` **0**, and **0** occurrences of every fixture string this repository's suites write — the diary bodies, the child names, both birthdates and both MATCH expressions. *(Recorded 2026-08-18, DIA-P5.)*
- **THE ZERO THAT MATTERS MOST WAS ARMED, and the arming is readable in the run's own signal line rather than argued.** `[signal] store.open outcome=opened failure_class=none freshly_created=true previous_run_clean=true schema_version=1 open_ms=1761` — **`freshly_created=true`**, once, on a fresh CI emulator. The store did not exist before this run, so `store/store.js` found `isSecretStored()` false and minted the passphrase, and that mint crossed the bridge as a plugin argument exactly as it did on run 32044006357, where it printed in cleartext at line 63944. **`passphrase` and `setEncryptionSecret` therefore read zero on a run that performed the very call that used to leak them** — an absence with the act behind it, not an absence of the act. This is the one needle a warm device cannot arm, and it is why the dispatch was not substitutable by the local emulator.
- **What the controls showed, because an absence claim is worth exactly what its controls are worth.** **(1) The channel is alive, through the same reader:** **42** `[signal]` lines, all under tag `TheyGrowSignal` and none anywhere else — 20 `store.open`, 7 `diary.write`, 7 `history.handoff`, 4 `diary.search`, 2 `history.import`, 1 `export.run`, 1 `write.refused` — carrying counts, timings, booleans and closed codes and nothing else (`[signal] diary.write outcome=complete failure_class=none chars=26 write_ms=46`). The leg's own summary line is `the diagnostic channel after the gate closed: signals=1 storeOpen=1 taggedLines=1`. **(2) The reader was not blind to Cyrillic:** the entire capture holds exactly **two** lines bearing any Cyrillic character — Android's own `ContactLocale` address-book alphabet list, and this leg's control marker `log-scan control контроль-кодировки-225085437518`. A capture that could not carry Cyrillic would have shown zero of both, and the needle counts would have been meaningless. **(3) The reader was not filtered to what we wrote:** **219** distinct process ids appear in the capture, of which the app's own (5926) contributes **825** lines. *(Counted from the threadtime pid column. The naive `awk '{print $3}' | sort -u | wc -l` reports 220 because the artefact's three `--------- beginning of` header lines contribute the token `of`, and `grep -c ' 5926 '` reports 826 because Zygote's `Process 5926 exited due to signal 9` names the app from another pid. Both variants are right about the property and off by one about the count; the method is stated so the next reader does not have to guess which.)*
- **AND THE CAPTURE IS NOT EMPTY OF APP-AUTHORED LINES, which a field reading "zero everything" would overclaim past.** Seven lines carry tag **`TheyGrowTransfer`** — `HistoryTransferPlugin`'s own `android.util.Log` calls, which never passed through `com.getcapacitor.Logger` and which this packet's knob therefore does not gate. On this run they carry byte counts, a ceiling, an envelope version, the scheme and host `theygrow://transfer`, and — the one worth naming — the **externally supplied query key** `"note"` echoed verbatim from a refused handoff link. No family text, and the whole capture's two Cyrillic lines are both accounted for above, so that is measured rather than assumed. But the channel takes a **free string** where `emitSignal` structurally cannot, and no executor covers it: `DIA-DL-010` debt 13.
- **Landed in.** DIA-P5.
- **Scope, and read it as a bound on the green above rather than as boilerplate.** Covers **this app's process, on the DEBUG build, on ONE fresh API-34 emulator image**. It does not cover the release build: that build is silent for a second and independent reason — `loggingBehavior: debug` would already resolve to false with `FLAG_DEBUGGABLE` clear — but that reason is **derived from AGP's defaults and the absent manifest attribute, not observed**, because no release artefact exists to observe and none will until ADR-047's workflow produces one. It is about **logcat** and no other out-of-process sink: the saved-instance-state parcel, the handoff link's journey through the activity manager, and `webContentsDebuggingEnabled` — which defaults to on in the same debug build and exposes the DOM and the store over `chrome://inspect` — are all untouched (`DIA-DL-010` debt 12). **It says nothing about the sinks that never passed through Capacitor's gate, and they are named rather than waved at, because the knob does nothing to any of them.** In run 32074105863, and identically in the local run before it: `SQLiteLog` (**36** lines, the native SQLite callback — the only one of these that carries **statement text**), `RetHandler`'s `*** ERROR` lines (**13** — an operation name and an engine message, no statement text), `TheyGrowTransfer` (**7**, this app's own), plus `…sqlite.SQLite.Database` and `SQLiteConnection`. No family text in any of them, measured — and on the dispatch that is a whole-capture measurement rather than a per-tag one, since the entire 79,144-line file holds two Cyrillic lines and both are accounted for. **But two of those safeties are conventions rather than structure, and a reader who inherits this should know which.** (1) Statement text is family-free only because every shipped statement is parameterised — and the plugin has a branch that removes the parameters: `Database.multipleRowsStatement` inlines values into the SQL with `sqlEscapeString` whenever an `executeSet` row's `values` is an array of arrays, which is exactly the shape a future batch-the-inserts optimisation would produce. One `SQLITE_FULL` after that and a parent's sentence is in `SQLiteLog`, past this gate. (2) `HistoryTransferPlugin.refuse(code, evidence)` takes a **free string** where `emitSignal` structurally cannot; nothing today passes family text to it, and nothing stops it (`DIA-DL-010` debt 13). That the search expression is family-free in statement text likewise rests on every term being quoted, which makes the existing escaping tests load-bearing for §4 rather than merely for correctness. **And one gated path escapes the gate on the way out:** `Bridge.java:848-851` logs through `Logger` and then rethrows, so an exception raised outside a plugin method's own try reaches `AndroidRuntime` with its whole cause chain in any build — thin, and not observed in any of the four captures this packet read, but it is the one route by which a bound value could reach logcat without consulting `Logger`. And one emulator image is not the family's handset — run 32074105863 is a fresh `google_apis` x86_64 API-34 image in CI, not a phone with three years of diary in it. Whether a console message consumed by a client returning `true` produces no second copy elsewhere is measured on that image and nowhere else, and the same is true of every count in the Observed field above.

- **Amended 2026-08-21 (L3-P1, `FIU-DL-001`) — one item in that Scope enumeration is no longer untouched, and the enumeration is what this note corrects.** The list of sinks "this app's process" writes to that the `loggingBehavior` knob does nothing about names `webContentsDebuggingEnabled` among them, "which defaults to on in the same debug build and exposes the DOM and the store over `chrome://inspect`". **That door is now closed** — `native/capacitor.config.json` sets the key to `false` explicitly, and the value is asserted in three places: statically on every push, and on a device in two legs, one reading the running bridge's parsed config and one reading the APK's own asset. The invariant with the claim is `FIU-P1-INV-002`; this entry's Statement, its **Enforced by** and the rest of its Scope stand **byte-untouched**, because nothing about the logcat claim changed and the two knobs are genuinely independent — a reader who takes them for one mechanism will look in the wrong place when either breaks. What remains true of that sentence is everything else it names: the saved-instance-state parcel, the handoff link's journey through the activity manager, and the ungated tags below Capacitor are all still untouched, and `DIA-DL-010` debt 13 is still open.

### FIU-P1-INV-001 — The store closes when the app goes to the background, and the next open finds a clean marker

- **Statement.** Two halves, and the second is what makes the first worth having. **(a) THE CLOSE HAPPENS.** When the page goes hidden — which on Android is `Activity.onStop`, i.e. a home-button press or a screen lock — the app closes its store: it waits out the plugin calls already in flight, writes `clean_shutdown = 1`, and closes the connection, in that order. `closeStore()` had been defined since L1-P2 and called from nowhere. **(b) THE NEXT OPEN THEREFORE FINDS THE MARKER SET**, so `integrityCheckPolicy: 'after-unclean-shutdown'` finally means what it says and a launch after an orderly background does **not** pay a full `PRAGMA integrity_check` over the family's history. **And a return costs a parent nothing they did not ask for:** the store is reopened by the first call that needs it, not by the resume, so an unlock that touches no data touches no store. **A third clause, on the same event and from the other direction:** a return to visibility no longer re-opens the transfer offer over whatever the parent was doing — `offerImportIfPending` distinguishes the boot call from the resume call, and the resume call does everything the listener was written for (take what arrived, explain a refusal) and nothing the parent did not ask for.
- **Enforced by.** **On a device** — `native/android/app/src/androidTest/java/app/theygrow/StoreLifecycleTest.java::a_backgrounded_app_closes_its_store_and_the_next_open_finds_a_clean_marker`, in `android-instrumented`. It launches the real activity, drives it to `Lifecycle.State.CREATED` (STOPPED), and reads this process's own logcat through `UiAutomation.executeShellCommand`, scoped by a control marker written before the launch. **Off device, on every push** — `app/tests/store-lifecycle.spec.js` in the `parity` job's `behavior` project drives a real page and asserts what a parent sees and what the shipped modules asked for; `app/tests/store-seam.spec.js::the park gate` (three legs) asserts the gate's control flow against the recorder; `app/tests/schema` is untouched because no statement changed. **The engine half** — `StoreEngineTest::the_clean_shutdown_marker_round_trips_and_refuses_anything_else` pins what the frozen DDL guarantees about the one column the whole design rests on, including that it comes back as an **integer** (the app compares it with `Number(...) === 1`, and a column that had become TEXT would be misread in one direction only).
- **Soundness measured, not argued, in three mutations that were run and reverted.** (1) Removing the `unsolicited` guard from `surfaces/import.js` reds `store-lifecycle.spec.js::a return to visibility does not put the transfer screen back over the app` and **leaves its arm green** — the arm asserts a refused handoff still reaches the parent, so the two legs fail in opposite directions and neither can stand in for the other. (2) Removing `wireStoreLifecycle()` from `app.js` reds both browser park legs. (3) Making the hidden branch return early reds the **device** leg, with the message that names what to do about it: `waited 60000 ms for 1 × "[signal] store.close" and saw 0`.
- **Observed.** Local emulator (`theygrow-api34`, API 34), 2026-08-21 — `StoreLifecycleTest` **1/1**, and the whole instrumented suite **40 tests, 0 failures, 236.4 s** with every pre-existing suite still green (`DeviceLogTest` 2/2, `DiaryEntryTest` 5/5, `StoreEngineTest` 12/12, `HistoryTransferTest` 8/8). The run's own signal lines, which are the evidence: `[signal] store.open outcome=opened failure_class=none freshly_created=true previous_run_clean=true schema_version=1 open_ms=1940` at boot, then `[signal] store.close outcome=complete failure_class=none close_ms=51` when the activity stopped, then `[signal] store.open outcome=opened failure_class=none freshly_created=false previous_run_clean=true schema_version=1 open_ms=396` when a shipped read reopened it. **A LOCAL GREEN IS NOT A DISPATCH, and this field says so:** the `android-instrumented` run that discharges it has not been performed, so this entry is **Not yet observed on CI**.
- **THE OPEN QUESTION DIA-DL-008 DEBT 8 LEFT, ANSWERED BY MEASUREMENT.** That debt said it closes "when there is evidence on whether the Android WebView reliably fires `pagehide` before process death, and a disposition that does not depend on it if it does not". The disposition shipped depends on `pagehide` not at all — it is `visibilitychange` going hidden — and the evidence is the run above: a real WebView delivered that event at `Activity.onStop` and the park completed in **51 ms**, with the process alive throughout. `pagehide` is deliberately not used even as a belt, so that no later reader mistakes it for load-bearing.
- **The numbers, and the shape of claim each can bear.** **The park: 51 ms and 66 ms** on the two stops in that run — small, and it happens while the parent is walking away from the phone. **The reopen: 396 ms**, against **1940 ms** for the cold open that also created the schema. That 396 ms is what a parent pays on the first store touch after unlocking, and it is why the reopen is lazy: an unlock that does not touch the store pays nothing. **The corpus is one fresh emulator with an empty store, and a real diary is a different measurement that nothing here reaches** — a warm open re-derives the SQLCipher key regardless of size, but nothing in this run says what an open costs beside three years of entries.
- **Landed in.** L3-P1.
- **Scope.** Covers the close on `onStop`, the marker the next open reads, the lazy reopen, and the resume path's transfer offer. It does **not** cover a process killed between `onStop` and the marker write: the marker then stays clear and the next open pays the check, which is the correct answer for a run that really died and is exactly what every run did before this packet — no path is worse than it was. It does not cover the release build, which no device has ever held. And the browser legs execute the shipped chain against a seam that runs no SQL: what the statements MEAN is `app/tests/schema`, and that a real SQLCipher connection closes and reopens is the device leg alone.

### FIU-P1-INV-002 — The installed build does not answer `chrome://inspect`

- **Statement.** `native/capacitor.config.json` sets `android.webContentsDebuggingEnabled` to `false`, explicitly, and the value reaches the running bridge. Absent, Capacitor resolves the key from `FLAG_DEBUGGABLE` (`CapConfig.java:286`) and `Bridge.java:618` hands `true` to `WebView.setWebContentsDebuggingEnabled` — so on the debug build `docs/RUNBOOK.md` tells the owner to install on the phone holding the family's real history, an authorised adb connection reaches the DOM, the JS heap and WebView storage. `false` **explicitly** rather than "absent and therefore off": absent is precisely the state that produced the exposure. This is a **different sink from `DIA-P5-INV-001`'s and a different knob** — `loggingBehavior` does nothing to it.
- **Enforced by.** `native/android/app/src/androidTest/java/app/theygrow/WebInspectionTest.java`, in `android-instrumented`, in **two legs that fail for different reasons**. `::the_running_bridge_was_handed_a_false` boots the app and reads the value off the live `CapConfig` the bridge parsed, one call before it is passed on. `::the_installed_build_declares_the_knob_that_closes_the_inspector` reads the key out of the **APK's own** `assets/capacitor.config.json`, which is gitignored and minted by `cap sync` — the fail-open `DeviceLogTest`'s declaration leg exists for. Statically, `app/tests/native-shell.spec.js::the build does not expose the WebView to chrome://inspect`, in the `contract` project **on every push**, pins the source key, because the device legs run on dispatch only.
- **WHAT NO LEG EXECUTES, AND IT IS NAMED RATHER THAN LEFT TO BE ASSUMED.** "chrome://inspect finds nothing" is asserted **nowhere**. `android.webkit.WebView` declares a static setter and **no getter**, so the flag's effect is not readable by any test this repository can write. What is checkable is the value that reaches the setter, and all three legs check exactly that. Anyone who wants the effect itself has to attach a debugger by hand and look.
- **The device leg carries its own arm.** A default-constructed `CapConfig` would answer `false` here too, for the wrong reason, so the same object is first asked for `isLoggingEnabled()` — whose `false` can only have come from this repository's `loggingBehavior: "none"`, Capacitor's own default being `debug`, which is true in a debuggable build. One assertion proves the subject; the other proves the object was read from our config at all.
- **Soundness measured, not argued.** Flipping the source key to `true` was run: it reds the static leg (1 failed in `contract`) **and both device legs**, each with its own message — the declaration leg printing the APK's actual config, the runtime leg naming the exposure. Reverted, and both re-run green.
- **Observed.** Local emulator (`theygrow-api34`, API 34), 2026-08-21 — `WebInspectionTest` **2/2**, inside the 40-test suite above. **Not yet observed on CI**: the `android-instrumented` dispatch that discharges this has not been performed.
- **Landed in.** L3-P1.
- **Scope.** Covers the DEBUG build, which is the only build any device has ever held. The release build closes the same door for a second and independent reason (`FLAG_DEBUGGABLE` clear), and that reason is **derived from AGP's defaults, not observed**, because no release artefact exists. It covers this one knob and no other sink: the saved-instance-state parcel, the handoff link's journey through the activity manager, and the ungated log tags `DIA-P5-INV-001`'s Scope enumerates are all untouched. Closes `DIA-DL-010` debt 12.

## Entry format (for future entries)

- **Id.** `M{N}-P{k}-INV-{NNN}` — `N` is the milestone number, `k` is the packet number within the milestone, `NNN` is zero-padded sequence within the packet (`001`, `002`, …).
- **Required fields.**
  - **Statement.** The invariant in one sentence.
  - **Enforced by.** File path(s) of the enforcing artifact — test, lint rule, schema constraint, runtime guard, or CI workflow.
  - **Landed in.** Packet id + commit SHA where enforcement was introduced.
  - **Scope.** What the invariant covers, and explicitly what it does not.
