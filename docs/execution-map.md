# Execution map — theygrow-app

This file is the living "where are we / what's next" state map. It is the index, not the spec. `AGENTS.md` §6 is the milestone source of truth; this file tracks progress against it.

## Milestone ladder

- **M1** — Repository preparation for agentic development (enablement harness).
- **M2** — `/api` skeleton (Python / FastAPI lands; monorepo split `/app` + `/api` lands).
- **M3** — Core store (one managed PostgreSQL as SoT; pgvector-derived vector port, ADR-008) + `/export` importer from `diary-memory-service`.
- **M4** — Retrieval lift from the engine (`diary-memory-service` → `theygrow-app`).
- **fix-export-empty-archive (XPT) — Done** (P1–P2 landed; merged as **#24** at 9ba19dd, which is where `feat/l2-local-diary` is cut from). *(This line read "In progress, first packet landed" through the merge; corrected at DIA-P1, which is the packet that discharged its two open evidence fields.)* The downloaded export archive was **0 bytes on the native channel, every time**, and the cause is a measurement rather than a hypothesis: the owner captured `TransactionTooLargeException: data parcel size 4630924 bytes` on the family device on 2026-08-15. The archive rode as an option of the plugin call that opens the file picker; Capacitor persists that call into the activity's saved instance state — twice — and the process was killed at the binder limit **after** the system had created the empty document and **before** a byte was written. Small fixtures stayed under the limit, which is why every test of the sink had been green since L1-P3. Cross-cutting correctness track on the L1 export contour, **outside** the M1–M5 engine spine. Branch `fix/export-empty-archive`; decisions and invariants take `XPT-DL-{NNN}` / `XPT-P{k}-INV-{NNN}`.
  - **P1 `export-payload-transfer-fix` — Landed this packet.** The archive is staged to the plugin in bounded chunks first and the picker call carries `{transferId, filename, mimeType, totalBytes}` — with the plugin refusing, **before** it builds the intent, any option key it does not declare, any options object over 4 KiB, and any transfer whose staged byte count is not the one the app declared, so a document is never created for an archive that is not fully in hand. Delivered by copy-forward bump `/m/v2/` → `/m/v3/` (`CACHE_VERSION` v12 → v13), because bytes at a published mount URL are never rewritten. Two knobs, one signal (`export.run`, counts and timings, device console only), and two invariants whose evidence is an **executed** export of an archive several times past the binder limit plus a saved-state measurement that carries its own control (`XPT-P1-INV-001`, `XPT-P1-INV-002`, `XPT-DL-001`). Both run in `android-instrumented` only, so the claim is as good as that job's next run; the **real system file picker** is still owner-run and covered by nothing in CI. Side-repair carried in: two test files had stayed pinned to `m/v1/` for two bumps (`build-artifact.mjs`, `test_write_path_projection.py`) and now derive the mount, and `docs/RUNBOOK.md`'s "no test edit is needed at bump time" is corrected to say what a bump does still need by hand.
  - **P2 `docs-green-run-references` — Landed this packet.** Docs-only, and the packet's own subject caught it out twice: the run this was opened to cite is **not** a green run. The `android` job of CI run **31898815923** is where the comparator's real-tool leg first passed — passing being exit **3** by the step's own `-ne 3` guard, since the log prints no status code — with both sides labelled and `apksigner` from **build-tools 37.0.0** named by the resolution line RSN-P4 added; but that **run** failed, at `quality`'s *Ruff format (check)*, which also skipped `pytest app/tests/release`. The property held with everything else green on **31899851227** and, on the default branch, on **31900363700**. `RSN-DL-005` keeps every word and gains a pointer bullet (this log's amendment-by-reference convention); `RSN-P2-INV-001` and `RSN-P1-INV-001` are corrected in place, the latter's three-unexecuted-legs Scope **re-affirmed unchanged**. The still-open half rides in the same paragraph as the discharge: `release.yml` has **never executed on any trigger**, the **positive** verdict against a real release-signed APK has never been observed, and three fail-closed legs remain unexecuted. No digest value enters any amended text — the debug key is minted per run and the two runs read print two different fingerprints. Prospectively, `XPT-P1-INV-001` / `XPT-P1-INV-002` gain a **Not yet observed.** field naming what discharges them (a green `android-instrumented` job on this milestone's PR against the branch tip) and when it is due (**before merge**), so the milestone does not open the debt it is closing. One scope extension, taken deliberately: `docs/RUNBOOK.md`'s release table said the step *"has returned **4**, not 3, on every run since it landed"* — false since 637660b, in the document the owner reads immediately before a release run — and that sentence, and only it, is corrected. Decision `XPT-DL-002`.

- **local diary (L2 / DIA) — Done, in eight packets** (P1, P2, P3 with two repair rounds P3R and P3R2, P4 with one repair round P4R, and P5). *(Read "eight" as the honest count: the ladder was four, three repairs were needed, and a fifth packet closed a privacy leak the milestone's own invariant denied. P3 took two — the first shipped a diary that never saved, the second a full disk that asked a parent to try again instead of to free space. P4 took one, and it is a different KIND of repair: nothing shipped was wrong, and what was wrong was the fixture that had been certifying it — an arming that proved the store full for a 200-character row and then licensed a leg that writes 26, which had already produced one green that was luck and had it quoted back as evidence. All three were found by dispatches rather than by review, and each is its own decision — `DIA-DL-006`, `DIA-DL-007`, `DIA-DL-009`. P5 is a fourth kind again: nothing shipped was wrong and no fixture was wrong either — the FRAMEWORK beneath both was writing the family's words to the device log, and had been since L1, while an invariant added in P4 said it did not. Found by reading a dispatch's artefact rather than by any test, because no test could see it: `DIA-DL-010`.)* **The milestone PR opens after this packet; the merge and the promotion are owner acts.** The diary becomes native: the family's history moves out of the browser and into the device journal, the web channel becomes a showcase, and a diary entry and a derived FTS index land on top. It opens on a structural fact that had been invisible because the code for it already existed — the L1-P4 importer is complete and idempotent and had **never imported anything**, because the Capacitor WebView's origin is `https://localhost` while the family's history lives in `localStorage` under the **production** origin, a different storage partition. Continues the PDR-026 ladder after L1. Branch `feat/l2-local-diary`, cut from `main` at 9ba19dd; ladder P1 history-transfer-bridge → P2 web-channel-and-export-ux → P3 diary-entry-core → P4 diary-fts-and-close → P5 family-text-out-of-the-device-log, with the milestone PR at close (`AGENTS.md` §9).

  **Id split, stated because this is the second genuine collision in this map rather than a style choice:** the ladder id `L2` is already owned by the shipped **A2 l2-staging** track, whose decisions are `L2-DL-{NNN}` and whose invariants are `L2-P{k}-INV-{NNN}`. This milestone therefore takes `DIA-` — decisions `DIA-DL-{NNN}`, invariants `DIA-P{k}-INV-{NNN}` — while packets keep `DIA-P{k}`. Same shape as the `LSC-` answer to the `L1` clash, and the `A2`/`L2` precedent applied a third time.
  - **P1 `history-transfer-bridge` — Landed this packet.** The browser→native transfer per ADR-048: the app opens a handoff page under the production origin, one button there builds an `intent://…;package=app.theygrow;end` link, and the parent is returned to the app where the **unchanged** L1-P4 importer takes the profiles it has always accepted as a parameter. **Staged transport, both hops.** The Intent is consumed natively in `MainActivity` — on `onCreate` and on `onNewIntent`, since the activity is `singleTask` — decoded and staged before the WebView is told anything; the bridge then carries `{transferId, totalBytes, sha256, formatVersion}` and the app drains the buffer in 256 KiB pieces. The browser hop has no handle to pass and is **bounded** instead, at a measured 16 KiB ceiling decided before the link is built. Every guard refuses before a byte is retained — foreign key, ceiling, declared-versus-actual bytes, digest, format version — prints bounded evidence and never the payload or either digest, and falls automatically to fallback A: the page emits a transitional file and the app takes it through `ACTION_OPEN_DOCUMENT`, with one line of instruction and no fork shown to the parent. Delivered by copy-forward bump `/m/v3/` → `/m/v4/` (`CACHE_VERSION` v13 → v14), which **discharges the standing `import.meta.url` debt**: the four mount self-references derive instead of being repointed by hand, as they had been three times. Four knobs, one signal (`history.handoff`, counts / timings / closed codes, device console only), three invariants (`DIA-P1-INV-001` the two-legged band invariant, `DIA-P1-INV-002` the transport and its refusals, `DIA-P1-INV-003` mount addressing). `XPT-P1-INV-001` and `XPT-P1-INV-002` are amended in place from **Not yet observed.** to **Observed.** on `android-instrumented` run **31912350123** (the PR for #24) — the scheduled pre-merge discharge XPT-P2 wrote down. Repairs carried in because this packet made them reachable: `app/sw.js` mirrored **every** navigation into the app shell's offline cache entry, which `/transfer.html` would have poisoned permanently (latent, never reached a user, the page did not exist in production); and the upgrade-path fixture had to learn to prune what the previous generation never shipped, in both its worker and its shell. **`DIA-P1-INV-002` landed explicitly unobserved** — `android-instrumented` is `pull_request` + dispatch only and the Java in this packet had never been compiled anywhere — **and was discharged at P1's close, together with the device half of `DIA-P1-INV-001`, on `android-instrumented` run 31956524510** (`workflow_dispatch`, this branch; 26 tests, 0 failures). It took three dispatches to get there, the first two failing for harness faults only: `DIA-DL-002` and `DIA-DL-003`. Decision `DIA-DL-001`.
  - **P2 `web-channel-and-export-ux` — Landed this packet.** The role split of PDR-034 §1 made visible: the web channel stops offering what it cannot perform and starts offering the app, and a saved archive says so where a parent can see it. **One control per channel, one build.** `#exportBtn` and the new public `#apkBtn` both ship carrying `hidden`, and `surfaces/channel.js` reveals exactly one from `window.Capacitor.isNativePlatform()` — a runtime branch inside byte-identical bytes (`LSC-P1-INV-002`). The archive goes from the web because the browser cannot produce one at all (`LSC-DL-003` (m)); the Telegram link goes entirely with the capture bridge this milestone retires, taking the one GA4 call site that hung on it and nothing else about GA4 (owner act 2026-08-16, gate D). **The download offer is fail-closed against a release that does not exist**: no tag, no release, no asset, so the control is revealed only when `<meta name="theygrow-apk-release">` declares one — a **shell**-side token, deliberately not a boolean in the immutable mount, where flipping it after an owner act would have cost a whole generation; the address and the vocabulary stay declared in `channel/config.js`. **The sentence the archive modal used to carry did not go with it**: `#webChannelNote` says above the table, on the web only, that this browser holds the only copy and it has no backup — true until a transfer is confirmed (ADR-048 §5). **The export flow's three owner findings**: a successful save now closes the dialog and raises `#exportDoneBanner`, which outlives it and waits to be dismissed (the two findings were one defect — a confirmation inside a surface the parent expects to disappear); failure and cancellation keep the dialog; and the shared `.modal-buttons` row is right-aligned at content width, because the defect was in the rule, not in the dialog it was noticed in. Delivered by copy-forward bump `/m/v4/` → `/m/v5/` (`CACHE_VERSION` v14 → v15), forced by the stylesheet and surface edits. Two knobs plus the shell token, **no new signal and no analytics of any kind**, two invariants enforced on every push (`DIA-P2-INV-001` the archive is offered only where it can be produced, `DIA-P2-INV-002` no download offer without a declared release) and one landing **Not yet observed** (`DIA-P2-INV-003` the dialog closes and the confirmation stands outside it — its only executor is `ExportTransferTest`, and `android-instrumented` is `pull_request` + dispatch only) — **discharged at P2's close on `android-instrumented` run 31963730354** (`workflow_dispatch`, this branch), where the amended test compiled for the first time anywhere and both its legs passed, and where `DIA-DL-004`'s prediction held: the leg reached success by reading the banner and found the dialog closed. A guard earned its place on its first run: half (b) of `DIA-P2-INV-001` went red on the shipped code because a class rule beat the `hidden` attribute and both controls rendered. Twenty baselines moved, listed file by file in the packet reports. Decision `DIA-DL-004`.
  - **P3 `diary-entry-core` — Landed this packet.** The milestone's product appears: a parent writes in the app. `#diaryBtn` in the header opens one window with two panes — the entries for the current child, and a form with a day and the app's first `<textarea>` — and a save lands one `record` row through `store/records.js` → `store/boot.js` → the existing `executeSet` seam, creating the entry's area in the SAME transaction the first time, because a record cannot exist without one — **as shipped it did not: the save handler spread an identifier that did not exist, so every create threw before reaching the store, and run 31971968427 is where that surfaced. Repaired in P3R; `DIA-DL-006`.** **The rules PDR-026 §4 owes are encoded where a reader meets them, not only in the log.** A diary entry is edited by **OVERWRITE** — `record` is deliberately absent from the eight append-only triggers, an edit moves `updated_at_utc` and leaves `entry_at_utc` alone, and the DDL, the module header and `DIA-DL-005` all say why, because the confusion is one refactor away in both destructive directions. **Event time and entry time do not collapse:** the form collects a DAY, the instant of the event stays NULL (the paired CHECK is what makes "unknown" expressible), and the moment of writing is always real. **Sensitivity is written NULL** — nobody was asked, and a default would make an accumulated corpus indistinguishable from a reviewed one. Deletion is **not surfaced**; its cascade (quotes erased, marks standing with degraded provenance, ADR-015) was already implemented and already executed, and a parent-facing delete needs a warning that needs L5. The area is **`participant_private`**, and the `[verify vs vault]` tag came off at plan review: PDR-026's 2026-08-11 annotation keeps the grounding quote in the author's private area precisely so a shared assertion cannot leak diary text, and a quote is lifted out of a record. **The two ADR-046 items assigned here close.** §1.1: the classifier existed since L1-P2 and terminated in a dialog that said the store had not opened — now a full disk reaches the parent as itself, on the diary AND (bounded, at owner direction) on the mark path, where a tick is withdrawn with its own explanation — **as shipped it did not reach them: both paths write through `executeSet` with a wrapper-driven transaction, and on `SQLITE_FULL` the wrapper's own ROLLBACK fails and is thrown from a `finally`, discarding the disk-full error, so what the classifier received said "cannot rollback" and the parent was told to try again rather than to free space. Run 31979084821 measured the mechanism; the seam now drives the transaction itself and reports the statement's failure over the rollback's. Repaired in P3R2; `DIA-DL-007`.**; and the refusal keeps the parent's text in the field, which is executed rather than promised. §1.3: satisfied for the journal spine, and answered for the diary as `(entry_at_utc, id)` in `schema_meta` — with `record.rowid` demonstrated unusable, since it comes back around after a delete. Delivered by copy-forward bump `/m/v5/` → `/m/v6/` (`CACHE_VERSION` v15 → v16), forced by nine changed files and costing a **sixth** shipped generation, stated rather than absorbed. Three knobs, one new signal (`diary.write` — counts, timings and closed codes; `chars` is a length) and one existing kind extended (`write.refused` gains `failure_class`), three invariants: `DIA-P3-INV-003` green on every push, and `DIA-P3-INV-001` / `DIA-P3-INV-002` green per-push in their off-device halves and, as shipped, landing **Not yet observed** in their device halves — `DiaryEntryTest` and the new `StoreEngineTest` leg produce a genuine `SQLITE_FULL` by lowering `max_page_count` and filling to it, and `android-instrumented` is `pull_request` + dispatch only — **both device halves are now OBSERVED and both invariants discharged: `android-instrumented` run 31982061125 (`workflow_dispatch`, `feat/l2-local-diary`), `DiaryEntryTest` 4/4 and `StoreEngineTest` 9/9. **One of those halves was withdrawn and has since been re-established** — `DIA-P3-INV-002`'s diary half, because that run's green came from an arming that later produced a red on a byte-identical string; it was repaired in P4R and re-observed on run **32044006357**, where the arming line carries the field that licenses the leg (`actRefusal=full:StoreDiskFullError`). The mark half and `DIA-P3-INV-001` were never in doubt (`DIA-DL-009`, and the amended field in `docs/INVARIANTS.md`). P3 reached green after two repair rounds (`DIA-DL-006`, `DIA-DL-007`); the evidence is recorded in `docs/INVARIANTS.md`.** Corrections carried in: the release-address guard learned that a frozen generation is not a second declaration; `required` came off the diary fields so the refusal speaks in the product's voice and not the browser's; and the corrupt-store sentence stopped advising an archive that does not carry diary text. Two DOM baselines moved, zero visual — the header gains a child that ships `hidden`. **Named here because it must not be discovered late: the archive does not contain diary text**, so from this packet a parent's diary lives in exactly one place. *(Corrected at DIA-P4: this clause ended "what an owner decision would weigh is in `DIA-DL-005`", and that decision has since been taken — the owner gate of 2026-08-17 chose that the artefact WILL carry diary text and addressed the work to **L3**. The state is therefore temporary and has an address: until L3 ships, the archive carries marks and journal history only.)* Decision `DIA-DL-005`.
  - **P4 `diary-fts-and-close` — Landed this packet.** The milestone's last object: a parent can **find** what they wrote, and the question ADR-046 §2.5 left open gets an answer that is measured rather than argued. **The word-form strategy lives in the QUERY, not in the index**, and that is what makes it cheap: the tokenizer is frozen with the schema, FTS5 is compiled without ICU (`LSC-DL-002`) and ICU would have folded nothing and lemmatised nothing anyway — so every word a parent types is searched as a **prefix**, in **every ё/е spelling of itself** (the index folds neither, measured on the device since L1-P2), with all words required. Because the normalisation is in the query, changing it later needs neither a migration **nor a rebuild**. **The ceiling was measured, and the measurement overruled the plan**: forty realistic queries against fifteen diary sentences at four settings — 3 → 37/40 found with 4 extra documents, 4 → 31/40 with 1, 5 → 26/40, 6 → 22/40 — because a prefix bridges the END of a word, so a query longer than what was written cannot reach it. The plan proposed 4; the knob shipped at **3**, and what it still gets wrong is asserted rather than described (`сесть` does not reach `сел`, `пойти` does not reach `пошла`, `спит` does not reach `спал` — stem mutations no prefix scheme reaches). **Every term is quoted**, so the `"`, `*`, `-` and `(` a parent writes about their child cannot become an operator or a syntax error. **The rebuild path gets an owner:** PDR-026 §4 rule 3 says the index is derived and rebuilt and does not say who asks, so the APP asks — at the one moment staleness is observable (an empty result over a diary that has entries), at most once per session, and the parent is told nothing because they were not asked and there is nothing for them to do. Two cheaper probes are recorded as **measured dead** rather than left for the next reader to try: `count(*)` over an external-content index cannot see an emptied one, and the argument-less `integrity-check` passes over it. **Three empty cases are three sentences, and a refusal is a fourth thing** — no entries at all, nothing matched (with the honest-degradation wording naming what the search cannot do and what to try instead), and a search that did not RUN, because saying "nothing matched" when the store refused tells a parent they never wrote what they did write. **The scheduled parse guard lands as a gate**: `app/tests/embedded-js-parse.spec.js` reconstructs and `node --check`s **110 embedded scripts across 8 files** on every push (103 across 6 when P4 built it; P5's suite added the seventh file, and L3-P1's `StoreLifecycleTest` the eighth), strips comments before scanning literals (the false-positive mode measured at C3), declares what it cannot read in a skip table with reasons, and keeps two legs that must never be switched off — an arm-check that mutates a real snippet, and a census that reds if any script of the standard shape is invisible to it. That census exists because the guard **failed its own arm-check once** and was briefly narrower than the ad-hoc tool it replaces. Three knobs plus a policy knob, one new signal (`diary.search` — counts, a timing, a boolean and closed codes; the search term reaches only a bound parameter and the input the parent typed it into), three invariants: `DIA-P4-INV-002` green on every push, `DIA-P4-INV-001` and `DIA-P4-INV-003` green per-push in their off-device halves and landing **Not yet observed** in their device halves — **both since discharged on `android-instrumented` run 32044006357** (`DiaryEntryTest` 5/5, `StoreEngineTest` 11/11), the dispatch those fields named. **No schema change, no mount bump** — `/m/v6/` is unpublished, so it is edited in place and `CACHE_VERSION` stays v16. Zero baselines moved, checked rather than argued. A defect was found inside the packet by its own new spec, and it is the milestone's second instance of one class: a class rule beat the `hidden` attribute and offered a search over an empty diary. Decision `DIA-DL-008`, which also carries the milestone's debt list in one place and the three classes it met more than once. **One repair round followed, and it is about EVIDENCE rather than product — `DIA-P4R`.** Run 32012897363 red the disk-full leg, and the app was right: the arming had proved the store full for a 200-character filler and the entry under test is 26 characters, which still fitted. The same arming string had already carried a green (31982061125) that `docs/INVARIANTS.md` quoted as proof, so what the red exposed was not the failing run but the passing one. The fixture now fills to a refusal at the ACT's own size and the leg asserts THAT refusal; the arithmetic under it gained an off-device executor that runs on every push. `DIA-DL-009`.
  - **P5 `family-text-out-of-the-device-log` — Landed this packet.** The milestone's privacy invariant was measurably false about a device, and merging would have fixed that falsehood into the enforced-only file. **Capacitor traces every plugin call's whole argument object to logcat** (`Bridge.java:826-837`), and every family value this app holds crosses that bridge as a bound value — so run 32044006357's own artefact carries **707** trace lines — the store's **passphrase in cleartext** among them — and, across that trace and a second emitter beside it, the child's name on 31 lines, the birthdate on 31 and a diary body on 13. A second emitter doubles it from the JavaScript side, printing every plugin RESULT — whole rows of diary text — to the console, which the same gate forwards to logcat. **The leak predates L2**: the trace has run since L1-P1 and a child's name has gone out with every mark since L1-P4; what L2 added is diary text, search expressions, and the sentence denying it. **The measurement that corrects the obvious reading:** a grep for the word the parent typed returns ZERO, because the shipped prefix ceiling is 3 and «села» crosses as `("сел"* OR "сёл"*)` — one traced expression per search, **5 of the 5** that run performed. A needle taken from the input box would have certified the leak closed, which is AGENTS.md §11's over-matching class one turn further in. **The fix is one key and one class:** `loggingBehavior: "none"` shuts both emitters in every build, and because the SAME gate carried the `[signal]` channel — measured: all 41 signal lines under `Capacitor/Console`, zero `chromium` console lines, nothing else logging them — `SignalConsoleClient` carries it instead, forwarding a console line only when it begins with `[signal] ` and dropping every other, which is a **stricter** allowlist than the gate it replaces because a signal payload cannot hold a free string by construction. **The claim gets an executor that can fail** (`DeviceLogTest`) with three controls, because an absence goes green for three reasons and only one is the truth: the matcher arms itself against a synthetic line in the shape the leak produced, a Cyrillic marker proves the capture and the encoding, and an unfiltered dump proves the reader's scope — plus the channel asserted ALIVE through the same reader, since a suite that only asserted an absence on a channel it had just silenced could only ever go green. `DIA-P4-INV-002` is amended in both directions: what it said about this app's own code was true and stays, what it said about the framework underneath was false for a whole milestone, and its named enforcer — a Playwright page console against an in-page fake — was structurally incapable of seeing the Java that wrote it. **The passphrase disclosure is decided, not deferred:** the store is not re-keyed now, with two named triggers (immediately if logs ever left the owner's hands; at L4 regardless, as hygiene). **Zero bytes under `/m/v6/` change**, so no mount copy-forward and `CACHE_VERSION` stays v16. One new invariant `DIA-P5-INV-001`, landed **Not yet observed** on green 36/36 from the local emulator and **since discharged on `android-instrumented` run 32074105863** — 36/36 in CI, `DeviceLogTest` 2/2, and the whole 79,144-line device-wide capture carrying zero trace lines, zero family text and exactly two Cyrillic lines, one of them the leg's own encoding control. Three debts handed on (the two `surfaces/diary.js` defects, one of which tells a parent their saved entry was not saved; and `webContentsDebuggingEnabled`, the same default on the same build through a different door). Decision `DIA-DL-010`, which is also the address the dangling `DIA-P4-CLOSE` citations now resolve to.

- **first live-install UX (L3 / FIU) — Done, FIVE PACKETS.** *(This read "ALL FOUR PACKETS LANDED" until L3-P5, and the correction is the packet: the closing dispatch it points at below was RUN, and it came back red — the print layer's declared degradation was drawing `.notdef`, so the artifact was not PDF/A and the job never reached the emulator. P5 is the repair; `FIU-DL-005`.)* *(Status corrected at PPR-P3: this line read "Current … the milestone is at its close" and stayed there through the merge.)* The owner-run close named in `docs/RUNBOOK.md` **Closing a milestone** was performed: the `android-instrumented` dispatch that discharges every `Not yet observed on CI` field is run **32535042631** — run 32530473473 before it discharged none of them, because it failed at step 7 before the emulator started — and the PR merged as #26 @ `cccff4b`. The ten fields were written up at PPR-P3, two milestones later, which is the gap this line is now explicit about. The twelve things the owner met on the first live install, the store-lifecycle debt `DIA-DL-008` marked for a milestone's OPENING rather than for a list, and the artefact work the 2026-08-17 owner gate addressed here.

  **Id split, for the same reason L2's is:** the ladder id `L3` is the rung; the decision and invariant ids are `FIU-DL-{NNN}` and `FIU-P{k}-INV-{NNN}`, so nothing collides with the shipped **L2 staging** track's `L2-*` or with L2 local-diary's `DIA-*`.
  - **P1 `store-close-and-first-run` — Landed this packet.** Three things the first install made visible, and one generation they force. **The store had no way to close.** `closeStore()` was defined at L1-P2 and called from nowhere, so `openStore()`'s unconditional `clean_shutdown = 0` was never undone, `integrityCheckPolicy: 'after-unclean-shutdown'` meant ALWAYS, and every launch paid a full `PRAGMA integrity_check` over the family's history — the very cost the knob was declared to avoid. It is called now, when the page goes hidden, which on Android is `Activity.onStop`; the store is reopened by the **first call that needs it**, so an unlock that touches no data costs nothing. **The condition `DIA-DL-008` debt 8 set is met by measurement, not by argument:** the disposition does not rest on `pagehide` at all, and a real activity driven to STOPPED on an emulator parked in **51 ms** and reopened in **396 ms**, against **1940 ms** for the cold open that also creates the schema. **The resume defect reproduced with no reload, no process death and no manifest fault** — `surfaces/import.js`'s `visibilitychange` listener called `offerImportIfPending()` unconditionally, and its terminal branch opens a `100vw/100dvh` modal, so the transfer screen came back over the parent's work on every unlock. One parameter fixes it and keeps the listener's real job: what actually arrived is still taken, a refusal still reaches the parent, and an invitation to start over does not. **`DIA-DL-010` debt 12 closes with the one key it said it would** — `webContentsDebuggingEnabled: false`, explicitly rather than by absence, with three legs, and with what NO leg can execute written into the invariant: `WebView` has a static setter and no getter, so "chrome://inspect finds nothing" is asserted nowhere. Delivered by copy-forward bump `/m/v6/` → `/m/v7/` (`CACHE_VERSION` v16 → v17), forced because `/m/v6/` has been published since the L2 merge, and costing a **seventh** shipped generation, stated rather than absorbed. One new signal (`store.close` — a closed code, a failure class and a timing), no new knob, no new closed code, no schema change, no new plugin method, no new dependency. Two invariants, `FIU-P1-INV-001` and `FIU-P1-INV-002`, both green on a **local** emulator (40 tests, 0 failures) and both landing **Not yet observed on CI**. Three false statements corrected in files the packet already had open, and two DOM baselines re-blessed whose diff carries one cause that is **not** this packet's — `a8b2ec2` revealed the download button and did not re-bless. Decision `FIU-DL-001`.

  - **P2 `fresh-install-self-sufficiency` — Landed this packet.** **The first screen after an install offered to move a history the app could not see, and it was removed outright** — owner act 2026-08-21, not gated and not made conditional. `app.js` called `offerImportIfPending()` at the end of boot and its terminal branch opened a `100vw/100dvh` modal whenever nothing was staged; what that modal offered had never been reachable on the native channel, because the WebView lives at `https://localhost` and the family's browser history is on the production origin — which the surface's own header already said. `surfaces/import.js` and `#importModal` are deleted; the plugin, the deep link, the envelope, the drain, the importer and the standalone handoff page all STAY as insurance, hanging off `store/boot.js`'s re-export block — which is what keeps them inside the walked graph and therefore inside the storage-seam, supply-chain and ship-list guards. **The two signal kinds that lost their emitter are flipped to `emittedNow: false` rather than deleted**, and `emitSignal` refuses a kind flagged false, so the state is enforced rather than noted. **With the offer gone a fresh install has exactly one path to a profile, and it had no executor anywhere on any channel** — measured by grep, not assumed — so the app now ASKS: on the native channel, with a store that opened and no child, the create-profile window opens in the slot the offer vacated, and its whole path is executed on the web channel, where it needs no store. **The diary's refusal offers the act instead of naming a menu**, and closes itself before opening the create window, because every `.modal` shares one z-index and `#createProfileModal` precedes `#diaryModal` in the markup — the button would otherwise have opened a window behind a full-screen panel. **`DIA-DL-010` debts 10 and 11 close, and they were one failure with two addresses.** A store refusal during a list render used to leave the parent looking at an open diary with an empty list and not one word — a read failure rendered as *you have never written anything* — and the same rejection, arriving inside `saveEntry`'s try after a successful write, told a parent «Запись НЕ сохранена» about an entry that was saved and put two contradictory `diary.write` lines on the wire. Both are fixed, and the direction that must NOT change is asserted beside them: a save the store really refused still says so, still names the disk, and still leaves the parent's text in the field. **The plugin's own log tag moves inside the gate** (`DIA-DL-010` debt 13, exposure half): 14 `android.util.Log` sites become `com.getcapacitor.Logger`, which keeps the tag and consults `shouldLog()` — false in every build — with a self-proving `DeviceLogTest` leg and its three controls; `refuse()`'s free string stays, and stays inventoried as `FIU-DL-002` debt 17. **Four device legs that drove the removed modal follow the mechanism down a layer** rather than going out with it, and **two of those four were not predicted by the plan — the suite found them**. One escalation, resolved by the owner as branch A: the handoff page keeps its button and loses the three sentences promising the app would finish the transfer. Zero visual baselines run (container-only, owner-run, already red on `main` from `a8b2ec2`); two DOM baselines moved, one line each, and the line is the onboarding modal's storage claim — false on the native channel since L1-P4 and the first sentence a fresh install shows. Decision `FIU-DL-002`.
  - **P3 `header-install-and-policy-link` — Landed this packet.** **The install channel's face.** The owner's first live install produced a list, and the part of it this packet answers is what a stranger meets before they have installed anything. Measured rather than assumed, in a booted headless browser: at 412×760 the download control ended at x 283 in a 412 px header with 12 px of padding — **117 px of dead space to its right** — because `.header-right { width: 100% }` at ≤767 px leaves `margin-left: auto` nothing to absorb and sets no `justify-content`; on the desktop the same header was already correct, which is why it had never been noticed. At ≤768 px the same control collapsed to a 40×40 unlabelled glyph, and it was an `<a target="_blank">`: what made it read as a stray link was the markup — the *ссылка* announcement, Enter-not-Space, the status-bar URL, *copy link address* — not the paint, which `app.css` already went out of its way to make identical to its `<button>` neighbours. **The fix is one declaration and one element change:** `margin-right: auto` on the profile chip pushes everything after it to the edge for one, two, three or four controls alike, and the control becomes a `<button>` because it no longer navigates — it opens a window. Measured after: every revealed control's right edge lands exactly on the header padding, on both channel compositions, at 360, 412 and 1280 px, with the native header holding four controls at 360 px without wrapping. **The 4 px the mobile header gained is the project's own 44 px touch-target rule finally applying to the web channel**, whose header until now held an `<a>` that escaped it, and a `.header-help { width: 40px }` override written before that was measured was deleted rather than shipped as a rule that does nothing. **What the window says is the packet's real subject.** Until now a visitor who decided to install got a bare download: no statement of what the app is, and none of what happens to their child's data — the only such sentence in the product lived inside the intro modal and had been false on the native channel since L1-P4. The window says four things, each checkable against a mechanism rather than a promise: what the app is, with months named as a way to find things and «не оценка и не диагноз»; where the data lives, in an encrypted store on the phone with no account, no sync and no cloud copy including Android Auto Backup; the honest downside, that the copy is one and deleting the app deletes it; and how to install. A test opens it, reads the rendered text and requires those promises by substring while forbidding ranking, deadline and price framing — because this is the surface where such framing would sell, and the first version of that guard was **wrong in an instructive way**: it banned the stem `сравн`, which the honest copy uses to DENY comparison, so the ban would have forbidden the sentence doing the work. **The page also stopped offering itself as an app.** The install prompt, its iOS twin, its rules, its four GA4 events and its declared storage door are gone — and so is `<link rel="manifest">`, which was escalated because deleting it exceeds the literal scope item and was resolved by the owner on branch (i): `beforeinstallprompt` fires only for a page whose manifest declares `display: standalone`, so removing the banner alone would have handed the same offer to the browser, uncontrolled. `manifest.json` stays served and precached so an already-installed PWA client keeps a working one, and the file the shell no longer names is carried as debt. **The privacy policy got a home, and the home got a door.** The address is fixed and declared once (`CHANNEL_CONFIG.policyUrl`), its publication state is a fail-closed `<meta>` in the shell on the APK precedent, and `shouldOfferPolicy(state)` deliberately takes **no channel argument** — the download is web-only because the native channel already has the app, but the policy is for whoever is entering data, and that is both channels. The plan put the link in the pre-install screen and the create-profile form; the owner replaced both with the intro window, on the argument that at the download moment a visitor is entering no data, so a link adds a reading obligation without adding a choice and reads as a warning beside a download button. **That decision has a consequence the packet had to pay in the same motion:** deciding that ✕ means "read it" — closing `FIU-DL-001` debt 14 — makes the intro close forever, and a document reachable exactly once is a promise kept once. Hence the round «?» in the header, on both channels, behind no declaration: it offers no action a channel cannot perform, it opens text that ships in the same bytes, and its name is «О приложении» rather than «Инструкция» because the owner recorded that the window may later become several sections. There is **no consent gate**, and its absence is guarded by a static assertion: making the document reachable is the obligation, a tick-box nobody can decline is not consent, and a consent record has nowhere to live in this milestone. **And the guard `DIA-DL-008` designed as debt 9 finally exists**, built to that entry's design rather than re-derived — a booted page, every `[hidden]` element, `getComputedStyle(el).display === 'none'`, running on both channel compositions at both viewports. Its bite was measured before anything else landed on top of it: deleting `.header-action[hidden]` reds it with exactly two violations. It was landed FIRST for that reason, and the packet then wrote the project's fourth `[hidden]` rule proactively, under a guard, instead of after a third incident. `#createProfileModal` joined the guarded `show` idiom in the same checkpoint (`FIU-DL-002` debt 19), which required extending the show-rule scanner to understand the injected `doc.getElementById` — a **named** alias, not "any receiver", because the scanner had failed closed exactly as its header promises it would. Decision `FIU-DL-003`.
  - **P4 `diary-text-in-artifact-and-close` — Landed this packet; the milestone's closing packet.** *(It was not: the dispatch that would have closed the milestone came back red on this packet's own print layer, and **P5** below is the repair. See `FIU-DL-005`.)* **The premise the packet was opened on turned out to be false, and that is the finding.** The scope item read "the artefact starts carrying diary text"; it already did. `export/declaration.json` has declared the `record` dataset **with its `body` column** since L1-P3 — written ahead of the table having rows — so from DIA-P3, the day the diary shipped, every archive carried the parent's entries in `index.json`, in `text/diary.txt` and in the print layer. Reproduced before anything was changed, on an archive built by the shipped builder. Meanwhile three places said the opposite: the diary's own corrupt-store message («…текста записей дневника в нём пока нет»), which a parent reads in the minute they are deciding whether to copy their text out by hand; the artefact's zero-case sentence ("the diary appears in a later version"); and `docs/RUNBOOK.md`'s diary smoke step 8. **What was actually missing was the boundary.** `record` was scoped through its AREA — `visibility_class = 'child_shared' OR owner_participant_id = ?` — so another participant's entry written into the child-shared area travelled: measured on a seeded row, present in the text file, in `index.json`, in the decoded print layer, and counted in `MANIFEST.json` (`counts.record` 4 against 3 of the exporter's own). It had never fired on a device (one participant per store; every diary entry goes to that participant's own private area), which is why it had to close before a second participant exists rather than after. **The packet:** the query re-scoped to `record.author_participant_id = ?` with the area join deleted rather than tightened; the rule stated in the archive's own words (`scope.diary`, with its predicate) and in `README.txt`; **format version 1 → 2**, because what an archive PROMISES narrowed; `text/diary.txt` rendered as a diary — each entry under the day it is about, in the parent's words, fields beneath it — which the print layer inherits because it is built from the text files; and the text layer's own defect class swept: values are wrapped and a token longer than the line is broken, after a 300-character token was measured running off the printed page. Two invariants, `FIU-P4-INV-001` (the scope rule, both halves) and `FIU-P4-INV-002` (nothing runs off the page), each with a mutation run and reverted. **Debt 20 closed by RECORD**, not by removal: `app/manifest.json` stays, because retiring it means editing the live delivery path this packet may not touch. No new knob (`formatVersion` is a VALUE change with `changed_in`), no new signal, no schema change, no mount bump — `/m/v7/` edited in place, `CACHE_VERSION` stays v17. Closing docs: this map, the README (the showcase's front page, two ladders behind), the `AGENTS.md` §6 ladder, and the RUNBOOK's export section, diary smoke and close sequence — which now carries the visual re-bless as its own numbered owner step. Decision `FIU-DL-004`.
  - **P5 `print-layer-notdef-repair` (repair) — Landed this packet.** **The closing dispatch was run and it came back red, and the artefact was right to be red.** Run **32530473473** on `f7ee031` failed `android-instrumented` at step 7, *Validate the print layer against PDF/A-2b*, **before the emulator started** — so steps 9 to 13 were skipped and every `Not yet observed on CI` field in this milestone stayed exactly where it was. veraPDF: ISO 19005-2:2011 clause **6.2.11.8**, 143 rules passed and one failed — *a PDF/A-2 compliant document shall not contain a reference to the .notdef glyph from any of the text showing operators … in any content stream*. **The cause is the declared degradation itself.** `export/pdf.js` substituted a codepoint the font cannot draw with `font.glyph(0xFFFD)`, and **PT Sans does not cover U+FFFD** — measured through the shipped `ttf.js`, which returns 0 for it — so the branch written to avoid drawing `.notdef` drew `.notdef`, once, at index **17** of one `Tj` run, which is the position veraPDF names. It had been latent since L1-P3 and could not fire until **P4's own fixture** seeded 🙂; `FIU-DL-004`'s falsifiable prediction reads "veraPDF still green on a print layer that now carries … an uncovered codepoint", and it was the uncovered codepoint that broke it. **The mark is now one the font demonstrably has** — `◊` U+25CA, glyph 606 with a real outline, coverage MEASURED rather than assumed, which is the whole lesson — and it is **one** glyph, because `pdf.js` emits one glyph per codepoint and `text.js` bounds a line by code points, so a two-character marker would have red `FIU-P4-INV-002`. The artefact says so in its own words, and a drift guard reads the knob out of `config.js` and requires `declaration.json` to name that mark and no other — the sentence and the writer had disagreed for a whole milestone. **The defect behind the defect is the one that mattered:** `pytest app/tests/export` was **50 passed** on a tree whose print layer is not PDF/A. The printed layer had no local executor for a conformance-relevant property of its own bytes, so the class could only surface as a red job at PR time. It has one now, on every push, with four anti-vacuity legs and an arm-check that builds its own failing content stream in-run — and a mutation that reds it with the offending run and position named. **And the red job printed a verdict without evidence:** `failedRules=1` and stop, with the clause dug out of the artefact by hand afterwards — `--format text` is 49 bytes and says `FAIL <path> 2b`, so it was not one `tee` away. The failing branch now prints clause, test number, description and first context, from a printer that decides nothing, exits 0 whatever it reads, and is executed off a **recorded copy of that run's own report** — the repository's first veraPDF bytes. One new knob, no new signal, no schema change, no mount bump, no format bump, no new asset; zero baselines, and none possible. One invariant, `FIU-P5-INV-001`, green locally and landing **Not yet observed on CI** — the verdict is veraPDF's and the dispatch is the owner's. Decision `FIU-DL-005`.

- **M5** — Closed-corpus family-memory chat.

## Current state

- **M1 — Done.**
  - **P1** — Contract redirect. **Done** @ f04d2ee (`AGENTS.md` rewritten, `CLAUDE.md` added, `.cursor/rules/masterplan.mdc` neutralized, `data/mvp_masterplan.md` reframed, `docs/decision-log.md` opened with `M1-DL-001`).
  - **P2** — Docs spine. **Done** @ 2886d0f (`INVARIANTS.md`, `RUNTIME-INVARIANTS.md`, `execution-map.md`, `RUNBOOK.md`, `product/BuildPlan.md`, `product/TechSpec.md`; cursor stub extended with the execution-map pointer).
  - **P3** — Quality harness. **Done** @ d67c70d (`pyproject.toml` / Ruff / mypy / `.editorconfig` / `.pre-commit-config.yaml` / GitHub Actions CI / gitleaks secret-scan / `scripts/check-contract-integrity.sh`; first enforced `INVARIANTS.md` entries `M1-P3-INV-001` + `M1-P3-INV-002`; decision `M1-DL-002`).
  - **P4** — Naming / `.gitignore` / `README.md` cleanup. **Done** @ 9088762 (`README.md` overview, full `.gitignore` pass, residual naming sweep, contract-integrity gate tightened to a strict ban with `INV-002` updated to match). Branch-convention reconcile @ ca98842; milestone merged via PR #1 @ 56facb5.
- **M2 — Done.**
  - **P1** — Monorepo `/app` split (PWA migrated from repo root into `/app`; `Dockerfile` COPY sources repointed; RUNBOOK/TechSpec reconciled; served `/` byte-identical). **Done** @ a77dfef.
  - **P2** — FastAPI `/api` skeleton: `GET /api/health`, env-driven read-only config (pydantic-settings), provider-port interface stub, PII-redaction forward guard, quality harness teeth on `api/`. Decision `M2-DL-001`; invariants `M2-P2-INV-001` + `M2-P2-INV-002`. **Done** @ 4be3860.
  - **P3** — Runtime + deploy path: dev-only docker-compose (Postgres 16 / pgvector), build-config relocated into `/app` + `/api`, and the `/api` deploy path — `/api` deploys as its own Cloud Run service so `/api/health` is green deployed. No real DB connection or schema yet (M3). The nginx same-origin `/api` proxy is not P3 (corrected from the original P3 recording to **M5** by `M2-DL-002`, and reassigned once more when it actually landed in **A3-P1** — `A3-DL-001`). Decision `M2-DL-002`. **Done** @ 6249590; milestone merged via PR #2 @ db4c1f3.
- **M3 — Done.**
  - **Pre-execution gate** — `/export` v1 schema confirmed (`SCHEMA_VERSION=1`, D-029) + `memory_rag` lift map. Decision `M3-DL-001`. **Done** @ ac441de.
  - **P1** — Episodic source schema + migration: the `source_messages` table (v1 wire mirror; engine-faithful TEXT ids; BIGINT `edit_seq`; PK + composite assertion-key UNIQUE; defensive `detected_route` CHECK; ADR-004 dual-timestamp; persona stub; reserved unindexed `vector(≤1536)` shell), Alembic migration tooling, the first real DB connection from product code, and DB-backed constraint tests run against a pgvector Postgres in CI. Decision `M3-DL-002`. **Done** @ 415a5a2.
  - **P2** — `/export` v1 importer: an offline module/CLI (`theygrow_api.importer`; no `/api` HTTP endpoint) that reads an `/export` file and idempotently upserts live `{note, draft}` rows on the composite assertion key (`recorded_at` + PK excluded from the update; `valid_at := created_at`; persona/embedding NULL), fail-closed on malformed/wrong-version input, with non-`{note, draft}` rows quarantined to a minimized §4-safe sidecar report. DB-backed importer tests. Decision `M3-DL-003`. **Done** @ d2bfb35; milestone merged via PR #3 @ 9a3c21a.
- **M4 — Done.**
  - **P1** — Re-derivation lift + sparse (lexical) leg: lift the engine note parser (`core/domain/parser.py`) to re-derive a normalized derived layer (`notes` + `event_chunks`) from `source_messages.raw_text` (event date from the first ISO line; one event line = one chunk; fallback to `created_at` when no date-led line, so every live `{note, draft}` message stays searchable), and lift the sparse FTS leg (`websearch_to_tsquery('simple')` over a generated `chunk_text_tsv` + GIN; `ts_rank_cd` order). New Alembic `0002` migration; an offline idempotent re-derivation CLI (`theygrow_api.derivation`; no HTTP endpoint). Folds in two operability surfaces (data discipline only): a typed, versioned **parameters-as-data** config surface (`theygrow_api.parameters`; P1 knobs `fts_config`, `sparse_candidate_limit`; render-ready, decision-log-traced) and a typed **signal taxonomy + injectable emission seam** (`theygrow_api.signals`; P1 emits derivation counters + sparse candidate-count/latency; downstream signals defined-not-emitted). No embeddings, no vector column/index, no RRF, no chat surface, no UI/visibility surface — those stay in P2–P5. Decisions `M4-DL-001` + `M4-DL-002`. **Done** @ 9a130e3.
  - **P2** — Embeddings activation: lift the donor embeddings adapter (`adapters/embeddings/openai_client.py`) behind an `EmbeddingProvider` provider-port; land the per-chunk `event_chunks.embedding` `vector(1536)` (Alembic `0003`) and drop the M3 dormant per-`source_message` shell (per-chunk granularity, ADR-011 §3); add the model/dimension knobs to the config surface (`embedding_model` runtime, `embedding_dimension` schema-bound + drift guard) and an `EMBEDDING_COUNTERS` signal. An offline idempotent backfill CLI (`theygrow_api.embeddings_backfill` / `theygrow-embed`; no HTTP endpoint) embeds `pending`/`failed` chunks (`ready` skipped), then builds the HNSW index AFTER population. Fail-closed structural §4 gate: no `chunk_text` leaves the perimeter unless the `embedder_privacy_cleared` ZDR+DPA+EU clearance flag is set. Both `note` and `draft` chunks stay embeddable (fork b open). No RRF, no grounded-ask, no chat surface. Decision `M4-DL-003` (cites ADR-011). **Done** @ 5a7fe6f.
  - **P3** — Retrieval fusion + note-only episodic eligibility: lift the dense (pgvector) leg (`dense_candidates` — cosine over the P2 HNSW index, `ready`-only, community-scoped) alongside the sparse leg in `retrieval/search_repository.py`, and Reciprocal Rank Fusion of the two legs in a new `services/retrieval.py` (`reciprocal_rank_fusion` + a `retrieve` orchestrator). Episodic eligibility (ADR-012, fork b): a binary, always-on `detected_route='note'` FK-join filter on BOTH legs — `draft` chunks stay embedded/indexed but are unreachable, so the fused result is note-only by construction. The query-embedding path honours the SAME fail-closed ZDR gate as the P2 backfill (reused `_ensure_embedder_cleared` / `_build_provider`): an uncleared process makes zero provider calls. P3 knobs (`candidate_k`, `top_k`, `rrf_k`, `rrf_dense_weight`, `rrf_sparse_weight`) land in the config surface (`changed_in=M4-DL-004`, `PARAMETERS_SCHEMA_VERSION` → 3); `RETRIEVAL_LATENCY` is extended with a §4-safe `leg` label (`sparse`/`dense`/`fused`). Folds in the operability structural enforcement (owner-directed, closes ADR-013 OQ#1): `M4-P3-INV-002` (every parameter carries `changed_in`) + `M4-P3-INV-003` (emitted_now kinds are wired to producers), plus `M4-P3-INV-001` (note-only in both legs + the fused path). No schema change, no migration; `ef_search` deferred (metric-gated). No grounded-ask, no chat surface. Decision `M4-DL-004` (cites ADR-011/ADR-012/ADR-013). **Done** @ db9f830.
  - **P4** — Grounded-ask assembly: lift the engine's grounded-ask seam (`services/context_assembler.py` + `services/query_service.py` + the answer-prompt / answer-schema contract + the OpenAI answers adapter) into a callable library seam — no `/api` HTTP endpoint, no chat UI (M5/ADR-007). `answer_query` orchestrates `retrieve()` (P3 fused, note-only) → `assemble_context` → the grounding gate → the answers-LLM → parse + grade → a `GroundedAnswer` with per-segment family-observation provenance and an honest degradation mode. Closed corpus on three structural edges (retrieved-only prompt; fabricated-citation rejection in the parser; pre-provider `grounding_min_segments` gate). Second-egress §4 gate (ADR-014): `answers_privacy_cleared` fail-closed before any answers-LLM egress — zero provider calls when uncleared, even with a provider injected. Honest degradation for grounded-but-uncertain (ADR-015): `uncertain` / `ambiguous` answers RETURNED WITH an honesty flag, still cited to retrieved family context (never parametric). Activates the two deferred signals (`GROUNDING_COVERAGE`, `DEGRADATION_EVENT`; both `emitted_now=True`); new knobs `answers_model`, `grounding_min_segments` (`PARAMETERS_SCHEMA_VERSION` → 4). No schema change, no migration. Decision `M4-DL-005` (introduces ADR-014 + ADR-015); invariants `M4-P4-INV-001` + `M4-P4-INV-002`. **Done** @ 5f01d02 (ruff-format drift fixup @ fddfc27).
  - **P5 (M4-CLOSE)** — Repo-bridge lift-fidelity reconciliation + docs reconciliation: diff all seven lifted seams (P1 parser; P2 embeddings adapter; P3 dense leg + RRF; P4 context-assembler + query-service + answers adapter) against the now-on-disk donor (`memory_rag`, read out-of-perimeter; ADR-005 §7) and record each as confirmed-faithful or noted-deliberate-divergence — every divergence covered by a prior decision (ADR-011 / ADR-012 / ADR-014 / ADR-015 / `M4-DL-001`…`005`); and reconcile this state map to milestone close. Docs + verification only — no product-code, schema, or migration; the lifted seams are frozen as shipped. The M4-CLOSE recall / grounding-quality mini-eval is recorded-not-run (owner-run against the real corpus with cleared real providers). Decision `M4-DL-006`. **Landed this packet** (milestone PR opens at close, AGENTS.md §9).
- **Б1 — Done.** ZPD filter in the skills tracker (`/app`), frontend-only.
  - **P1** — ZPD readiness predicate (a skill is ready iff not-completed ∧ all prerequisites completed; **no** age gate) + presentational row markers (the presentational core). Decision `B1-DL-018` (= vault ADR-018). **Done** @ 62a07d6.
  - **P2** — ZPD filter replaces the active age filter (toggle + `applyFilter`); age demoted to a passive display reference. **Done** @ e2ecf60.
  - **P3** — Honest empty-state for the empty ready-zone: a message, not an empty table, when the ZPD filter is on and no skill is ready (ADR-018 / контракт §2). **Done** @ 51cc9e2.
  - **P4** — Onboarding copy reconciled to the ZPD filter. **Done** @ 79b6b28.
  - **B1-CLOSE** — decision-log (`B1-DL-018`, = vault ADR-018) + execution-map reconciliation; the frozen P1–P4 ladder recorded in the repo ledger. **Landed this packet.**
- **PWA Update-Delivery — Done.** Network-first SW delivery + prompt-driven update UX in the PWA (`/app`), frontend-only. Cross-cutting front delivery track (roadmap v3 §5 contour 7) — sits alongside Б1, **outside** the M1–M5 engine spine; not naively "next" in that ladder.
  - **P1** — Network-first SW delivery engine: versioned cache (`CACHE_VERSION`) + `activate()` purge of non-current caches + the `SKIP_WAITING` message handler; network-first navigation / app-shell + `/api/`, cache-first for static assets. Decision `PWA-DL-001` (cross-refs vault ADR-019). **Done** @ 107de2a.
  - **P2** — Prompt-driven update UX: the `#updateBanner` "Обновить" prompt drives `skipWaiting`; first-install-silent (`clients.claim()`), reload double-gated (`updateAccepted` + one-shot `refreshing`). **Done** @ 273c9ab.
  - **PWA-CLOSE** — decision-log (`PWA-DL-001`, cross-refs vault ADR-019) + execution-map reconciliation; the frozen P1–P2 ladder recorded in the repo ledger. The `app/sw.js` `changed_in` placeholder is authoritatively resolved to `PWA-DL-001` here (physical comment backfill deferred to the next `CACHE_VERSION` bump). **Landed this packet.**
- **L1 Promotion Gate — Done.** Deploy-safety promotion gate for the PWA (`app/cloudbuild.yaml` Step 3 + the `docs/RUNBOOK.md` procedure). Cross-cutting deploy/ops delivery-safety track (vault ADR-020) — sits alongside Б1 and PWA Update-Delivery, **outside** the M1–M5 engine spine; not naively "next" in that ladder. Live-infra identifiers (the PWA service / region) stay confined to `docs/RUNBOOK.md` (`M1-P3-INV-002`); this map names only the build-config path and links there.
  - **P1** — No-traffic + sha-tag deploy: `app/cloudbuild.yaml` Step 3 deploys the PWA with `--no-traffic --tag sha-$SHORT_SHA`, so each push lands a fresh **0%-traffic**, per-revision sha-tagged revision and the live revision keeps serving until the owner smoke-tests and promotes. Decision `L1-DL-001` (cross-refs vault ADR-020). **Done** @ f0cf7c9.
  - **P2** — Promotion-gate procedure + owner smoke checklist in `docs/RUNBOOK.md` ("Promotion + rollback"): get the per-revision tagged URL → curl-only smoke (health + app-shell DOM + `#updateBanner` + the `/sw.js` `no-cache` header) → promote 100% traffic → abort (withhold) / rollback (pin the previous revision) → out-of-band `sha-` tag pruning. Owner-run `gcloud`; the gate is owner smoke, not CI. **Done** @ e76de87.
  - **L1-CLOSE** — decision-log (`L1-DL-001`, cross-refs vault ADR-020) + execution-map reconciliation; the frozen P1–P2 ladder recorded in the repo ledger. Piggybacks the owed M2-P3 trigger-continuity caveat removal from `docs/RUNBOOK.md` (gated on the applied two-trigger steady state, now live; `M2-DL-002` left untouched). **Landed this packet.**
- **Б2 (ZPD activities-list correctness) — Done.** Relabel + de-age + impact-sort of the activities modal in the skills tracker (`/app`), frontend-only. Sibling track-Б milestone (after Б1), **outside** the M1–M5 engine spine; not naively "next" in that ladder.
  - **P1** — Read-only diagnosis of **Defect A** (suspected age-coupling in the ready-zone / honest-empty-state surface): confirmed already correct per ADR-018 (ready-zone visibility = pure prerequisite graph; age consumed only at the current-month highlight; honest-empty-state already holds) → owner **no-fix, no divergence, no new ADR**. No edit, no commit. Decision `B2-DL-001` (= vault ADR-018).
  - **P2** — **Defect B** copy: relabel → "Подходящие активности" + de-age wording. **Done** @ 689f30e.
  - **P3** — **Defect B** logic: full de-age (drop the `age_start_months ≤ currentLifeMonth` filter — all unmastered skills shown) + impact-sort (DESCENDING count of DIRECT unmastered dependents via a front-side, read-only reverse-prerequisite map derived per modal-open; stable tie-break = dataset order); zero-case copy reworded to the skills-mastered condition (`Все навыки освоены — подходящих активностей сейчас нет`). Dataset / `milestonesData` byte-for-byte untouched; operability **none** (presentational reorder — fixed rule, no knob, no new signal). **Done** @ d2652b4. Decision `B2-DL-002` (= vault ADR-019).
  - **Б2-CLOSE** — decision-log (`B2-DL-001` + `B2-DL-002`) + execution-map reconciliation; the frozen P1–P3 ladder recorded in the repo ledger. **Landed this packet.**
- **Bio-windows reference layer — Done.** Biological developmental-windows reference/urgency layer in the skills tracker (`/app`), frontend-only. Cross-cutting front Layer-1 track (PDR-004 band-first) — sits alongside Б1 / PWA Update-Delivery / L1 / Б2, **outside** the M1–M5 engine spine; not naively "next" in that ladder.
  - **P2** — Embedded `bioWindowsData` const with window-side `applies_to:[skill_ids]` binding (`milestonesData` untouched); activities urgency ranking (Tier-1 windowed by `peak_fraction × criticality_weight(type, decline_shape)`, ★ excluded, sensitive-capped, attachment front-loaded; Tier-2 = the `B2-DL-002` graph-impact order verbatim) over the **ready (ZPD) subset** (`isSkillReady`, same predicate as the readiness filter); window highlight in the activities modal only (critical `#FBBF24` / sensitive `#FEF3C7`, existing amber palette; graph table untouched). Predicate gate **withdrawn** (ADR-022 §2 retracted by the 2026-06-28 owner amendment; ADR-018 restored to pure prerequisite graph — windows never remove a skill). Decision `BW-DL-001` (= vault ADR-021 / ADR-022; ADR-018 restored). **Done** @ 253c7f1.
  - **UX-debt** — Removed the redundant completed-skills filter (`#filterToggleBtn` + machinery) + renamed the readiness filter; onboarding 3→2. Same-branch cleanup, recorded as a secondary effect of `BW-DL-001`. **Done** @ 2d58b85.
  - **BW-CLOSE** — decision-log (`BW-DL-001`, = vault ADR-021 / ADR-022; ADR-018 restored) + execution-map reconciliation; the frozen P2 + UX-debt ladder recorded in the repo ledger. Bumps `app/sw.js` `CACHE_VERSION` (`v2`→`v3`, first app-shell change since the SW engine) and on that bump physically backfills the owed `changed_in` placeholder → `PWA-DL-001` (the `PWA-DL-001`-owed correction, deferred to the next `CACHE_VERSION` bump). Supersedes the "activities = all uncompleted" part of `B2-DL-002` (base now the ready subset, narrowed by the prerequisite graph, not by age). **Landed this packet.**
- **PWA Logo & Favicon — Done.** Brand logo + favicon icon set across the manifest, the `index.html` head, and the SW precache in the PWA (`/app`), frontend-only. Cross-cutting front-only asset track — sits alongside Б1 / PWA Update-Delivery / L1 / Б2 / Bio-windows, **outside** the M1–M5 engine spine; not naively "next" in that ladder. New-filename asset scheme (`icon-logo-`, `maskable-`, `apple-touch-icon`, `favicon-*`) replacing the old placeholders (`icon-192/512.png`), so a `CACHE_VERSION` bump re-fetches the icons fresh (versioning-by-filename, vault ADR-024).
  - **P1** — Manifest brand icons: `app/manifest.json` `icons` carries the logo at `purpose: "any"` 192/512 (`icon-logo-192/512.png`) + `purpose: "maskable"` 192/512 (`maskable-192/512.png`). **Done** @ d86b14f.
  - **P2** — Head links: `app/index.html` `<head>` adds `rel="icon"` PNGs 16×16 / 32×32 (`favicon-16/32.png`) + `rel="apple-touch-icon"` 180×180 (`apple-touch-icon.png`). Favicon delivered PNG-only — gate #5 narrowed "both (`.ico` + PNG)" → PNG-only; no `favicon.ico` landed (the bare `/favicon.ico` probe 404s harmlessly, PNG `rel="icon"` covers the tab). **Done** @ a133345.
  - **P3** — SW delivery: `app/sw.js` precaches the install-surface set (4 manifest icons + `apple-touch-icon`) + `favicon-16/32`, bumps `CACHE_VERSION` `v3`→`v4` (delivers the icons to installed users via the existing network-first SW + `#updateBanner` path, `PWA-DL-001`; `activate()` purges `theygrow-v3`), and drops the dead placeholder paths (`icon-192/512.png`) from `OFFLINE_URLS`. **Done** @ eae43d1.
  - **PWA-ICON-CLOSE** — decision-log (`ICON-DL-001`, cross-refs vault ADR-024 + records the gate #5 PNG-only narrowing) + execution-map + RUNBOOK reconciliation; the frozen P1–P3 ladder recorded in the repo ledger. **Landed this packet.**
- **graph-why-now — Done.** Skill-modal explanatory graph layer (downstream dependents + readiness "почему сейчас") in the skills tracker (`/app`), frontend-only. Cross-cutting front Layer-1 track (band-first; roadmap §1 п.3 / PDR-004 §3) — sits alongside Б1 / PWA Update-Delivery / L1 / Б2 / Bio-windows / PWA Logo & Favicon, **outside** the M1–M5 engine spine; not naively "next" in that ladder. No dedicated vault ADR by design; cross-refs vault ADR-018 for the readiness predicate (`isSkillReady`) it reuses.
  - **P1** — Downstream "Открывает дальше" section in the skill modal: a reverse `prerequisites` scan over `DATA.milestones` rendering the skills a given skill unlocks as clickable chips (`getDownstreamHtml`); graph leaf → honest empty (no section). `milestonesData` untouched. **Done** @ 7f0289c.
  - **P2** — Readiness "Почему сейчас" block (`getReadinessHtml`; status via `isSkillReady`, the ADR-018 pure-prerequisite predicate — mastered / ready / blocked, blocked lists only un-mastered direct prerequisites as chips) + ✓/○ per-prerequisite completion markers on the "Требуемые навыки" list. **Done** @ 01e8949.
  - **GWN-CLOSE** — decision-log (`GWN-DL-001`, fresh namespace; cross-refs vault ADR-018; records "no dedicated ADR by design") + execution-map reconciliation; the frozen P1–P2 ladder recorded in the repo ledger. Bumps `app/sw.js` `CACHE_VERSION` (`v4`→`v5`) to deliver the inline P1/P2 sections to installed users via the existing `#updateBanner` path (`PWA-DL-001`); `OFFLINE_URLS` untouched (no new asset/route). Adds one `graph_navigate` GA event (`{from_skill, to_skill, relation}`) at the existing delegated chip handler — `/app` analytics, no operability surface. **Landed this packet.**
- **skill-link-from-activities — Done.** Activity-card skill titles deep-link to the existing skill detail modal (`openSkillModal`) in the skills tracker (`/app`), frontend-only. Cross-cutting front Layer-1 track (band-first; roadmap §1 п.4 / PDR-004 §3) — sits alongside Б1 / PWA Update-Delivery / L1 / Б2 / Bio-windows / PWA Logo & Favicon / graph-why-now, **outside** the M1–M5 engine spine; not naively "next" in that ladder. No dedicated vault ADR by design; traces to roadmap §1 п.4 — closes the deep-link predecessor only, **NOT** the full browseable/searchable 174-skill reference (which stays open/gated).
  - **P1** — Activity-card titles become keyboard-activatable buttons (`role="button"` / `tabindex="0"` / `aria-label`) wired to the existing `openSkillModal` via a delegated click + Enter/Space handler on `#activitiesGrid` (one shared `activateActivitySkill` path; `Space` `preventDefault`'d); the skill modal stacks over the still-open activities modal (z 120 > 100) and the first back/close returns to activities (no skill-history push from the activity entry); `activity_skill_open` GA event (`{skill_name}`). `milestonesData` untouched. **Done** @ cc768c7.
  - **SLA-CLOSE** — decision-log (`SLA-DL-001`, fresh namespace; traces roadmap §1 п.4; records "no dedicated ADR by design" + the §1 п.4 divergence — deep-link predecessor only, not the full 174-skill reference) + execution-map reconciliation; the frozen P1 ladder recorded in the repo ledger. Behaviourally verified via a headless gstack smoke (deep-link stacks 120 > 100; first back returns to activities; chip nav walks skill↔skill; `activity_skill_open` / `graph_navigate` fire). Bumps `app/sw.js` `CACHE_VERSION` (`v5`→`v6`) to deliver the inline P1 change to installed users via the existing `#updateBanner` path (`PWA-DL-001`); `OFFLINE_URLS` untouched (no new asset/route). Reconciles the stale `docs/RUNBOOK.md` cache-version line (`v4`→`v6`, left stale by `GWN-CLOSE`). **Landed this packet.**
- **vendor-domain-kb-artifact (M(В)-1) — Done.** The app ends dual-home and becomes a pure READER of the vendored versioned KB artifact (`app/kb-v1.json`; the inline `milestonesData` / `bioWindowsData` copy is gone). Data track (KB curation lives in the separate `theygrow-domain-kb` repo, vault ADR-026) — sits alongside the cross-cutting front tracks, **outside** the M1–M5 engine spine; not naively "next" in that ladder.
  - **P1** — Read-only diagnosis + design: the async fetch-then-init boot (C1), the fork-B narrow nginx immutable location (vault ADR-024 annotation, owner gate 2026-07-04), and the tag-anchored sync mechanics (vault ADR-026 §2 annotation). **Done** (read-only packet — no commit; design recorded in `VDK-DL-001`).
  - **P2** — Tag-anchored sync script (`scripts/sync-kb-artifact.sh <N>`, release-tag `kb-v{N}` anchor, owner-run, not CI-wired) + vendored `app/kb-v1.json` (byte-identical) + `.pre-commit-config.yaml` byte-as-published excludes. **Done** @ d8c60f6.
  - **P3** — The swap + static delivery: async boot in `app/index.html` (inline data + provenance comment deleted, −607 KB; adapter/builder bodies unchanged; honest kb-load error path), `app/sw.js` `CACHE_VERSION` `v6`→`v7` + `/kb-v1.json` precached, `app/nginx.conf` narrow immutable `^/kb-v[0-9]+\.json$` location, `app/Dockerfile` COPY. DOM-parity IDENTICAL (per-section SHA-256, isolated origins); offline boot from precache and error path verified; artifact byte-as-published (sha256 unchanged). **Done** @ 82b9685.
  - **VDK-CLOSE** — decision-log (`VDK-DL-001`, fresh namespace; repo-side definition site for vault ADR-026, pointers to the ADR-026 §2 tag-anchor / ADR-024 narrow-pattern annotations) + execution-map + RUNBOOK reconciliation (new KB-artifact sync/version-bump section; `kb-v1.json` asset line; stale `CACHE_VERSION` `v6`→`v7` fix; promotion-smoke kb checks per the ADR-020 gate) + `docs/INVARIANTS.md` `VDK-P3-INV-001` (the vendoring-seam invariant). Carried follow-up: the skill-id-stability consumer rule lands owner-side in `theygrow-domain-kb`. **Landed this packet.**
- **GA4 instrumentation depth — Done.** Cross-navigation GA4 instrumentation + telemetry PII fix in the skills tracker (`/app`), frontend-only. Cross-cutting front analytics track — sits alongside the other front tracks, **outside** the M1–M5 engine spine; not naively "next" in that ladder. No dedicated vault ADR by design (measurement over existing shipped surfaces); enforces `AGENTS.md` §4 at the emitter. Owner console-side prerequisites (retention 2→14 months; 10 registered custom dimensions; key events) done before the packet.
  - **P1** — `skill_view` gains `source` (threaded through the `openSkillModal` choke point: `table_row` / `{prerequisite,downstream,readiness}_chip` / `activity_card` / `back_navigation`) + `skill_id`; `graph_navigate` gains `nav_depth` + `from_skill_id`/`to_skill_id`; new chain-end `skill_modal_close` (`action: close_button|backdrop`, abandoned `nav_depth`, `skill_id`) fired only in `closeSkillModal`; **PII fix** — `profile_create` loses `profile_name` (child name; the one live §4 telemetry violation), event kept param-free; `activity_skill_open` gains `skill_id`; `activities_open` gains `relevant_count`/`tier1_count`/`tier2_count` (counts only, moved after computation); new `zpd_empty_state_shown` (hidden→shown transition guard in `applyFilter`); new `onboarding_shown` / `onboarding_dismissed` (`action: dont_show_again|closed`). No `CACHE_VERSION` bump (owner-decided: network-first, non-precached `index.html`; analytics-only change warrants no update prompt). Behaviourally verified via headless gstack smoke (`?dbg=1` dataLayer inspection; zero console errors). Decision `GA-DL-001`. **Landed this packet.** Owner follow-up after merge: register the new params as GA4 custom dimensions/metrics.
- **SPA polish (A0) — Done.** Four owner-verified UI defects repaired in the monolithic PWA (`/app`), frontend-only. Thin pre-refactor track — sits alongside the other front tracks, **outside** the M1–M5 engine spine; not naively "next" in that ladder. Its purpose is sequencing: the fixes land **before** the spa-split refactor so that milestone's parity/screenshot baselines capture the healthy state instead of freezing the defects into the definition of "correct". No dedicated vault ADR by design (defect repair over shipped surfaces); cross-refs vault ADR-024 (versioning-by-filename, via `ICON-DL-001`), ADR-015 (honest degradation), ADR-018 (readiness predicate, untouched).
  - **P1** — (a) app shell + mobile full-screen modal sized with `height: 100vh; height: 100dvh` — Chrome/Android resolved `100vh` to the large viewport, laying the `overflow:hidden` shell's footer controls below the fold with no way to scroll to them; (b) ZPD filter no longer shows completed skills — root cause was `saveCompletedSkills` silently no-opping without a current profile (DOM checkbox diverging from persisted state; N ticks left N−1 checked-and-visible rows, the reported 9 reproduced exactly on the pre-fix tree), fixed by honest degradation at the write (refuse + revert + `openCreateProfileModal`, no `skill_complete` emission) plus `applyFilter` recomputing readiness from `getCompletedSkills()`; (c) install-surface icons re-derived (Lanczos, no geometry change) from the owner-supplied opaque-white lockup master `icons/splash-logo-name-1024.png` into new `-v2` filenames — the old set was fully transparent RGBA, which Android composited as the reported black splash plate, and the lockup bakes in the app name that Android 12+ system splashes never render; `background_color` → `#FFFFFF`; **accepted consequence: the launcher icon becomes the logo+name lockup** (owner decision 2026-07-26); (d) `theme-color` → `#000000` at all three declaration sites (owner decision 2026-07-26). `app/sw.js` `CACHE_VERSION` `v7`→`v8` + the five icon paths in `OFFLINE_URLS` (mandatory — the manifest and icons are precached cache-first; `activate()` purges `theygrow-v7`). No `Dockerfile`/`nginx.conf`/`cloudbuild.yaml` change; no localStorage schema or key touched. Behaviourally verified via headless gstack smoke (pre-fix reproduction on an isolated origin; zero console errors); fix (a)'s real surface and the splash remain owner on-device smoke steps. Decision `A0-DL-001`. **Landed this packet.**
- **spa-split (A1) — Done.** Buildless native ES-module split of the monolithic `app/index.html` (3,476 lines: ~1,600 CSS + ~1,480 app JS + install-prompt and service-worker registration blocks), refactor-equivalent with zero product change from P1 onward. Front refactor track — sits alongside the other front tracks, **outside** the M1–M5 engine spine; not naively "next" in that ladder. Realizes vault ADR-037; first consumer of ADR-030 §2.1 (permanent CI parity/smoke suite). The production delivery channel stays buildless — no bundler or transpiler is introduced; Playwright and Node are dev/CI only.
  - **P0** — Sanctioned pre-refactor exception (the last product change of the milestone): the ZPD filter suspends the mobile accordion, separating the accordion's presentation layer from the family's stored intent (`applyAccordionVisibility` becomes its single owner; the destructive `saveCategoryStates()` DOM sweep is replaced by an explicit per-row `saveCategoryState`; `categoryKey` unifies the write and read key; `accordionSyncedForFilter` guards re-entry), while the honest ZPD empty state keeps counting `.hidden` rather than collapsed-ness. Closes the follow-up `A0-DL-001` logged-but-did-not-fix. `app/index.html` only; no `CACHE_VERSION` bump (network-first, not precached by name). Decision `A1-DL-001`. **Done** @ 3e11a1a.
  - **P1** — `parity-suite`: the full three-level parity threshold (ADR-037 §3) lands **before** any extraction and becomes the acceptance gate for every subsequent split packet — (a) DOM snapshots of header / `#mainTable` / control-footer / `#zpdEmptyState` / the four modals across four boot states at two viewports, plus a SHA-256 per skill-modal body for all 174 skills (automating the manual `VDK-P3` method); (b) visual regression with the platform fixed by a pinned browser container, baselines never written implicitly (`updateSnapshots: 'none'`); (c) behavioural smoke over the main tick → ZPD-recompute → persist → reload loop, the P0 honest-degradation refusal, the activity→skill deep link and modal stack, SW registration / offline boot / PWA update flow, and the kb-load error path. Adds the repo's **first non-Python CI gate** (`parity` job in `.github/workflows/ci.yml`, calling the same `scripts/parity-suite.sh` developers run locally) and a drift guard that fails when `app/tests/server.js` diverges from the real `app/nginx.conf`. Every shipped `app/` file byte-untouched; `CACHE_VERSION` stays `v8`. Decision `A1-DL-002`; invariant `A1-P1-INV-001`. **Done** @ 049aa74.
  - **P2** — `restore-runbook`: the database recovery path enters `docs/RUNBOOK.md` as a self-contained **Restore / disaster recovery** section — PITR clone to a new instance → stand up → three-tier validation (intended Alembic revision, row counts + HNSW-index survival, app-level read smoke) → `DATABASE_URL` / Cloud SQL switch → traffic through the existing promotion gate → post-incident review, with every GCP step marked owner-run. Adopts **RTO 2–4 h** carrying "an untested RTO is an assumption, not an RTO", and **RPO 5–15 min** written as conditional on PITR being enabled (unverifiable from a repo with no IaC) with the ≤24 h fallback stated; makes an on-demand backup the standing pre-step before every hand-run migration, and names logical corruption the likelier disaster than physical loss. Erasure-suppression reconciliation sits in the sequence as a marked no-op until accounts (vault ADR-010 / PDR-010); AlloyDB gets a short future note only. Docs-only and DOM-parity-neutral — bundled into A1 by owner directive (ADR-037 §4), zero product code, the parity gate untouched. Piggybacks three stale M3-era claims in the same file and a Cloud SQL identifier stub (empty placeholders) in **Live-infra divergence**. Decision `A1-DL-003`; no invariant (enforced-only bar). **Done** @ 64037d1.
  - **P3** — `out-of-line asset contract`: the first extraction, and the packet that fixes the seam every later one crosses. Defines the delivery contract for anything leaving the shell — a versioned static mount `app/m/v{N}/` served at `/m/v{N}/` (**version-in-path**, chosen over version-in-filename because a ~10-module split bumps in one directory copy instead of N coordinated renames), with **copy-forward** bumps: the new version is a full copy and the published one stays on disk byte-untouched (vault ADR-024). Records why that is load-bearing rather than tidy — `cache.addAll` goes through the HTTP cache, so a `CACHE_VERSION` bump alone would refill the new cache generation with the **stale** bytes of an unchanged URL. `app/nginx.conf` stays untouched (the generic immutable location is the wanted semantics once URLs never carry changed bytes) and `app/Dockerfile` gains one `COPY m/` for the whole mount root, permanently discharging the no-wildcard hazard `A1-DL-002` diagnosed. Carriers: the `<style>` block → `m/v1/app.css` (plus `@charset "UTF-8";`, the one deliberate deviation from a byte-exact slice — the block has non-ASCII in rendered `content:` values and production nginx sends `text/css` bare) and the service-worker-registration block → `m/v1/sw-register.js` as the first `type="module"`; the 1,480-line stateful app script stays inline and byte-untouched, so delivery risk and JS-scope risk land in different packets. `index.html` 3,476 → 1,818 lines. `CACHE_VERSION` `v8`→`v9`. Adds a **ship-list drift guard** — the parity server serves from disk, so a forgotten `COPY` passes the whole suite and 404s only in production — asserting in both directions that the shell's references are shipped *and* precached. Parity green with **zero baseline writes**. Decision `A1-DL-004`; invariant `A1-P3-INV-001`. **Done** @ f505d1d.
  - **P4** — `core extraction + module entry`: the JS-scope packet. The 1,480-line app script leaves `app/index.html` for a module graph under the same mount — a thin `core/` (`storage.js`, `state.js`, `kb-boot.js`, `dom-utils.js`, `format.js`, 35–120 lines each) plus one entry module `m/v1/app.js` that keeps the feature code for now (per-surface modules are P5). `core/storage.js` owns key identity and raw I/O only — every `JSON.parse`, `catch` and `console` stays at the call site it already occupied, because today's parse semantics differ per key on purpose; cross-module state travels as `export let` **live bindings**, so ~50 read sites move byte-unchanged and only the owning module may assign. The central problem was the **test seam**: module scope removes the global lexical bindings the parity suite reached through, and the replacement is the entry module's **export surface** reached via `import('/m/v1/app.js')` — seven names, no `window.*` global, and the same module instance the page booted. A flag-gated test-only hook was rejected on principle (it would make the tested build differ from the shipped build); the seam also shrank, with `visual.spec.js` moved off `profiles`/`saveProfiles` onto the app's own `saveCompletedSkills`. The ship-list guard gains a **third direction** — it walks the static import graph, because the shell now references exactly one JS entry and a missing `core/` file would 404 in production or break offline boot with both existing directions green (`A1-P4-INV-001`). `CACHE_VERSION` stays `v9` and `m/v1/` is extended in place: both were minted on this unmerged branch and have never been published, so copy-forward does not bind. `index.html` 1,818 → 339 lines. The gate earned its keep on the first run: dedenting the two multi-line template literals changed `#skillModalBody` whitespace and three DOM baselines went red by name; those ranges now move byte-untouched. Parity green with **zero baseline writes**. Decision `A1-DL-005`; invariant `A1-P4-INV-001`. **Done** @ bbd67e6.
  - **P5** — `feature modules per surface`: the entry module is cut into eight `surfaces/*.js` (table, skill-completion, zpd-filter, skill-modal, profile, activities, onboarding, accordion — 43–205 lines each) over two new `core/` residents (`zpd.js` for the readiness predicate and the ZPD-filter flag, `urgency.js` for the Tier-1 window arithmetic); `m/v1/app.js` drops 1,278 → 92 lines of imports, `init()` and the seam. The cut follows the call graph rather than the P3 surface sketch: the sketch's single "table/ZPD" file lands ~380 lines and mixes DOM construction with DOM mutation. The graph is a **DAG by construction** — the one backward call, `switchProfile()` rebuilding the table, is inverted by passing the rebuild into `initProfiles()`, so `profile.js` never imports `table.js`; shared mutable state (the filter flag, read by both the filter and the accordion) lives in `core/` rather than in whichever surface owns the button. A cycle would have evaluated correctly and the guard's walker would not even have hung, which is exactly why it was rejected: correctness no later packet can see at the import site. `init()` keeps its exact statement order via per-surface `wireX()` registrars, and **nothing is re-indented** — the `A1-DL-005` (h) template-literal hazard cannot recur, and the whole semantic delta is 25 `export ` prefixes plus five named lines. The two shell questions `A1-DL-005` left open both resolved to *not touching the shell*: the install-prompt IIFE stays inline because `beforeinstallprompt` is unobservable to every level of the gate, and `modulepreload` goes to P6 because the graph is only final once this packet lands. `app/index.html` and **all of `app/tests/`** end the packet byte-identical — the seam kept the same URL and the same seven names. `CACHE_VERSION` stays `v9`; ten `OFFLINE_URLS` entries. A third boot-order cost recorded: `core/*` is one level deeper in the waterfall, paid once per cold first visit, with P6 as the intended discharge. Parity green with **zero baseline writes**. Decision `A1-DL-006`; no new invariant (the guard covers the new files unchanged). **Done** @ 0dbc2ea.
  - **P6** — `modulepreload delivery hints + guard reconciliation`: the boot-order cost `A1-DL-004/005/006` each recorded is paid, and the guard question the payment raises is settled deliberately. `app/index.html` gains fifteen `<link rel="modulepreload">` hints — the non-entry modules only, since the preload scanner already finds the two `<script type="module" src>` entries, and excluding them makes the hint list *exactly* the walked import graph. Measured against the real built container rather than asserted: **four discovery waves → one**, last module response 822 → 671 ms, zero duplicate fetches (the no-`crossorigin` decision, checked empirically). The before-state was worse than the ledger recorded — a *fourth* wave the two-level graph does not predict, because HTTP/1.1 serves six connections per origin and `surfaces/activities.js` fell into the second batch, delaying `core/urgency.js` by a full round trip; the wall-clock gain is correspondingly one round trip and not three, and is stated that way. The guard half was the larger one: hints are `href=` attributes, so `htmlAssetRefs()` would have promoted all fifteen into `SHELL_REFS`, and since `moduleDependencies()` deletes its own seeds, `MODULE_DEPS` would have gone **empty** — `A1-P4-INV-001` reduced to zero assertions with CI green (measured: 17 seeds, 0 dependencies). So the walk is re-rooted at what the shell *executes*, hints stay in `SHELL_REFS` (an unshipped hint is still a real defect), and a **fourth direction** asserts hint-set ↔ graph as a set equality — the "every module is hinted" half being the load-bearing one, since a future extraction that forgets a hint costs a round trip nothing else here would notice. Side-find recorded rather than preserved: `A1-P4-INV-001` said "exactly one JS entry" and there have been two since A1-P3. `app/sw.js` byte-untouched — no new URL, no `CACHE_VERSION` bump (`v9` unpublished; and navigations are network-first, so the shell reaches installed users without one). `modulepreload` fetches and compiles but never evaluates, so boot order is unchanged — proved, and gated. Parity green with **zero baseline writes**. Decision `A1-DL-007`; invariant `A1-P6-INV-001`. **Done** @ beb16f5.
  - **A1-CLOSE** — decision-log (`A1-DL-008`) + ledger and docs reconciliation; the frozen P0–P6 ladder recorded in the repo ledger, and the milestone's carried follow-ups all discharged in one attributable place. Fixes the three shipped comments the milestone falsified — `app/sw.js` (the shell no longer references only the entry: since A1-P6 it names every module in a hint), `app/m/v1/app.js` (the boot-order TODO still calling `modulepreload` a P6 future) and `app/index.html` ("entry + `core/`", stale since A1-P5) — comment-only, zero behaviour change, `CACHE_VERSION` stays `v9` on the unchanged `A1-DL-005` (f) / `A1-DL-006` (h) / `A1-DL-007` (g) reasoning (the version was minted on this unmerged branch and has never been published; the packet adds no URL, and navigations are network-first). Lands the contract-file edits deliberately kept out of P3–P6 so they could arrive together: the live-deploy path enumeration in `AGENTS.md` §7, its two copies in `scripts/check-contract-integrity.sh` and `docs/RUNBOOK.md`, all three gaining `m/` (and `kb-v1.json`, without which the RUNBOOK's claim about what the contract files enumerate would have been false again) — permitted here and verified, not assumed, by re-running the contract-integrity gate that governs those files. Also reconciles `AGENTS.md` §8 (the PWA described as a single `index.html` at repo root — the claim this milestone most directly falsifies), `docs/product/TechSpec.md` (`/app` as shell-plus-assets, now shell plus versioned module mount, buildless), `docs/INVARIANTS.md` `M1-P3-INV-001` Scope (it pointed at a `.gitleaks.toml` allowlist that does not exist), `app/package.json`'s `test:dom` (named a Playwright project that does not exist), and the "lands in M3" staleness in `docker-compose.yml` + `scripts/dev/init-pgvector.sql`. Re-deferred with reasons, each named in `A1-DL-008` rather than dropped: the dead exports (`core/state.js saveProfiles`, `core/format.js formatParentActivitiesText` — deletion is a product change), the two guard-capability gaps (`app/.dockerignore` unread by the ship-list guard; `http`-level nginx directives unseen by the drift guard), routing `app/package.json`'s scripts through `scripts/parity-suite.sh`, and the broader `AGENTS.md` M1-era refresh. No new invariant (a close-out enforces nothing new, `AGENTS.md` §10); operability none. Parity green with **zero baseline writes**; the changed shipped comment bytes verified in the real built image. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).
- **l2-staging (A2) — Done.** A pre-production **staging contour for `/api`** — its own Cloud Run service, its own build-config on its own trigger, and an isolated database seeded from a synthetic corpus and gated by a labeled eval — plus a promotion gate for production `/api`. Cross-cutting deploy/ops delivery-safety track and the repo-side definition site for vault **ADR-020 as amended**: it builds directly on **L1**, whose `--no-traffic`/sha-tag gate this milestone extends from the PWA to `/api`, and it is the first milestone of the PDR-010 spine. Sits alongside L1 and the other cross-cutting tracks, **outside** the M1–M5 engine spine; not naively "next" in that ladder. Live-infra identifiers (the staging service / registry / instance / secret / service account) stay confined to `docs/RUNBOOK.md` (`M1-P3-INV-002`); this map names only build-config **file paths** and links there. **Id split, stated because this is the first track in the map whose prefixes differ:** milestone `A2 l2-staging`, packets `A2-P{k}`, decisions `L2-DL-{NNN}`, invariants `L2-P{k}-INV-{NNN}` — `A2` and `L2` are the same track, not two. **The contour itself is owner-pending:** no staging service, database, role, secret, trigger or seed exists in the live project — the milestone ships an executable handoff (`docs/RUNBOOK.md` "Staging contour", steps 1–12), not its execution.
  - **P1** — Staging contour + promotion gate: a new self-contained `api/cloudbuild.staging.yaml` on its own trigger deploys a **dedicated staging Cloud Run service** — same `api/Dockerfile` and build context as production but a **distinct image name** (a second push to production's tag would rewrite what production's own build-config points at), private (`--no-allow-unauthenticated`), Cloud-SQL-attached with `DATABASE_URL` from Secret Manager, its own least-privilege service account, `--max-instances 2`. Staging takes traffic (it *is* the smoke contour); production `api/cloudbuild.yaml` instead gains the L1 shape verbatim (`--no-traffic --tag sha-$SHORT_SHA`), so deploy stops meaning release for the backend as it already had for the PWA. Live identifiers arrive as **empty-default trigger substitutions** behind a **step-0 guard**, because an empty `--add-cloudsql-instances` parses to an empty list and would deploy green with no database attached. `docs/RUNBOOK.md` gains the ten-step owner-run **Staging contour** stop-gate handoff (pre-create `vector` as superuser; create the staging role in SQL rather than via `gcloud`, which would grant `cloudsqlsuperuser`; revoke `PUBLIC`'s implicit `CONNECT` on production; **verify the refusal by observation**) and a separate `/api` promotion procedure, deliberately not a copy of the PWA's. Decision `L2-DL-001` (cross-refs vault ADR-020 as amended; amends the applicability of `L1-DL-001` alternative 2, which is left byte-untouched). No invariant — an owner-run `gcloud`/SQL procedure enforces nothing in code (`AGENTS.md` §10; the `L1-DL-001` precedent). **Done** @ 5e5d254.
  - **P2** — Synthetic corpus + seed CLI: a committed `/export` v1 corpus under `api/corpus/staging/` (invented Russian throughout, one export file per community) plus `theygrow-seed-staging`, which chains `import_export` → `rederive` → `embed_backfill` in **one transaction** behind a **two-layer fail-closed staging-marker guard** — the database name parsed from `DATABASE_URL` before a connection is opened, and `SELECT current_database()` on the connected server before any write, both against an allowlist of exactly one name so an unrecognized or absent name refuses too. To let the embeddings pass run with both clearance flags unset, the §4 clearance gate is **scoped to egress**: an in-perimeter provider does not *satisfy* the clearance, it removes the surface the clearance exists to authorize, and the check is fail-closed on an absent or non-`True` declaration. Decision `L2-DL-002`; invariants `L2-P2-INV-001` + `L2-P2-INV-002`; amends `M4-P2-INV-002` in place. Deliberately **no** invariant for "the corpus contains no real family data" — unprovable by any test, held by authorship discipline and stated at the top of `api/corpus/staging/README.md`. **Done** @ 5a5677f (pinned ruff-format fixup @ 5b158c9).
  - **P3** — Eval pipeline: `api/theygrow_api/evals/` — 26 labeled cases covering all six case classes with one holdout each, a runner driving every case through both `retrieve()` and `answer_query()`, and a counts-only scorecard — plus `theygrow-eval` in two run modes (CI against an ephemeral pgvector service; owner-run against the staging contour with an identity-token liveness assertion) and a third CI job. The same egress scoping is extended to the **read** path, with the declaration predicate hoisted from the backfill to `ports/provider.py` beside the two ports it now serves; an in-perimeter answers provider answers **by policy over the prompt**, never by a per-case script. The labeled set **is** the baseline — per-case recorded expectations, no pass-rate threshold, no separate baseline file, and therefore no env-overridable way to lower the bar; `check_contour` refuses an empty, stale or wrong contour with its own exit code, because three of the six classes assert **absence** and would otherwise pass trivially. One new signal kind `EVAL_SCORECARD` (`producing_stage="A2-P3"`, `emitted_now=True`); no new knob and `PARAMETERS_SCHEMA_VERSION` stays `4`. Decision `L2-DL-003`; invariants `L2-P3-INV-001` + `L2-P3-INV-002`; amends `M4-P2-INV-002`, `M4-P4-INV-001` and `L2-P2-INV-002` in place. **Done** @ 17f5de6 (pinned ruff-format fixup @ 63605ad).
  - **A2-CLOSE** — decision-log (`L2-DL-004`) + ledger and contract reconciliation; the frozen P1–P3 ladder recorded in the repo ledger. Lands the three contract/spine edits all three L2 entries deferred here identically, because each needed something the packet writing it did not have: this track entry (it needs commit SHAs that do not exist while the packet is being written), the live-deploy-path enumeration in `AGENTS.md` §7 — the third and last copy, after `scripts/check-contract-integrity.sh` and `docs/RUNBOOK.md` gained it in A2-P1, so the RUNBOOK sentence claiming *what the contract files enumerate* stops being false — and `docs/product/TechSpec.md`'s `/api` build-config bullet (one build-config → two, one service → two), whose **Prod vs dev** neighbour is corrected in the same pass because A2 made its two-contour reading false. Records the two upstream residual questions this milestone **answers** — synthetic-corpus volume (settled, with a named revisit trigger) and the eval-against-staging shape — and collects, in one place, the debt the packets each named separately: build-once-promote-the-digest (staging validates the source tree at a commit, not production's image bits), `performs_no_egress` as a **trusted declaration** no test can verify, the duplicate answer-prompt parser in `api/tests/test_query_service.py`, the promotion precondition as owner **convention** rather than mechanism, and the whole owner-pending contour. States plainly that the repo decision-log is the **working copy** and the upstream decision layer is **not yet annotated** — that is the owner-side close task after merge. No new invariant (a close-out enforces nothing new, `AGENTS.md` §10); no new **Current** track (the next-track choice stays the standing open owner decision recorded at the M5 line); operability none. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).
- **A3 — Done.** **Same-origin transport + production storage.** Two things, in the order they bind: origin unification — production `/api` goes private and the PWA container reverse-proxies `/api/*` to it service-to-service with a metadata-minted ID token, so the browser only ever talks to the PWA origin and **no CORS header is issued anywhere** (ADR-007 / ADR-038) — and production storage enablement: the `/api` build-config gains its Cloud SQL attachment, its `DATABASE_URL` secret, a dedicated least-privilege runtime identity and a connection ceiling, with `GET /api/health/ready` as the first deployed route that opens a connection (ADR-039 / ADR-008). Cross-cutting deploy/ops + infrastructure track, **outside** the M1–M5 engine spine; not naively "next" in that ladder. It builds on **L1** (the `--no-traffic`/sha-tag gate both packets smoke through) and **A2** (the substitution + Step-0-guard convention), and it is the milestone that discharges the M5 origin-unification assignment `M2-DL-002` had carried since M2-P3. Live-infra identifiers (services, registry, instance, secret, service accounts, upstream URL) stay confined to `docs/RUNBOOK.md` (`M1-P3-INV-002`); this map names only file paths and links there. **Milestone boundary changed mid-flight, and this is the map's record of it:** A3 was opened as `accounts-oauth-same-origin` and redefined by owner gate on **2026-07-29** to same-origin transport + production storage. The OAuth flow (OIDC endpoints, state/PKCE/nonce, account entities and their migration, the cookie session, the pending/approved status model, the wait-list) and the SPA auth entry / account screen — provisionally numbered A3-P3 and A3-P4 — moved out to a separate milestone, **A3b**, with the rest of the roadmap numbering unshifted. A3b is **not** opened here: recording the split is this packet's job, sequencing the next milestone is a separate owner act. **Neither packet's build-config or code has executed against the live project:** both ship build-config, code and an owner-run procedure, and the milestone has not merged. What *has* happened is the storage half of the P2 procedure — the owner ran its steps 1–7 on **2026-07-29**, so the Cloud SQL instance, the production database, the application role, the `DATABASE_URL` secret and the dedicated runtime service account now exist (`A3-DL-004` records what came back different from what the procedure prescribed). **The store existing is not the wiring working:** nothing is deployed, no substitution is set, no migration has run, and the readiness route that would first exercise the attachment has still never executed anywhere.
  - **P1** — Same-origin `/api` proxy: production `/api` deploys `--no-allow-unauthenticated` (the explicit negative, so an existing `allUsers` binding is actively removed rather than merely unmentioned), and `app/nginx.conf` gains one `location ^~ /api/` block — `^~` load-bearing, since a plain prefix loses to the file's existing static-asset regex location and a future `/api/<name>.js` would be served as a missing file instead of proxied. The file becomes an envsubst template rendered at container start by a new `app/docker-entrypoint.sh`, which mints the ID token from the metadata server with `audience` = the upstream service URL, polls for rotation, reloads nginx only on an actual value change, and **fails closed** (no mintable token after the retry budget ⇒ `exit 1`, and the container never starts — safe because the deploy is `--no-traffic`). njs and OpenResty were both rejected on cost of carriage; the timer is safe by arithmetic, not by hope (≈1 h token life, 5 min poll, ~15 min idle reap). Two live identifiers arrive as trigger substitutions with empty defaults behind a Step 0 guard: `_API_UPSTREAM_URL` (simultaneously the `proxy_pass` target and the token audience) and `_PWA_RUNTIME_SA` (a dedicated least-privilege account, explicitly **not** the project-default compute account, because it is the grantee of `roles/run.invoker`). Container-layer knobs land in the entrypoint rather than `parameters.py` — a `/bin/sh` loop in a different image has no Python runtime — following the `app/sw.js` `CACHE_VERSION` precedent; signals none, since the taxonomy lives in the FastAPI process the proxy container cannot reach. Side-find fixed in place: the parity suite's location-drift guard was matching inside comments, latent since A1-P1. Decision `A3-DL-001` (cross-refs vault ADR-038 / ADR-007 / ADR-020 / PDR-002 / PDR-010; amends `M2-DL-002`); invariant `A3-P1-INV-001`. **Done** @ 14907ce.
  - **P2** — Production database enablement: `api/cloudbuild.yaml` gains four flags behind the same Step 0 guard shape — `--add-cloudsql-instances`, `--set-secrets DATABASE_URL`, `--service-account` and `--max-instances 2` — with three live identifiers as trigger substitutions; and `GET /api/health/ready` lands as a **new** route rather than a key on `/api/health`, because liveness must not acquire a database dependency and its exact body is asserted in three places at once. Readiness is closed and **mute**: a fixed status enum on both paths, and a `readiness.probe` signal carrying a **closed** `failure_class` derived from the control-flow branch and never from an exception, because psycopg/SQLAlchemy connection errors carry the host, the user and the socket path. Two engine lifecycles deliberately stay separate (process-cached bounded pool for the served app; the untouched per-run engine for the offline CLIs and `alembic/env.py`), and the arithmetic is the decision: `db_pool_size` 2 × `--max-instances` 2 = 4 connections, hard-capped by `db_max_overflow = 0`, against an application-role `CONNECTION LIMIT` of 10. Side-find fixed in place: the served FastAPI process configured no logging at all, which would have made the RUNBOOK's "read the failure class in Cloud Logging" instruction false the moment it emitted a signal. Five knobs (`PARAMETERS_SCHEMA_VERSION` 4 → 5) and one signal kind declared **and** emitted in the same packet. No schema, no migration, no new Alembic revision. Decision `A3-DL-002` (cross-refs vault ADR-039 / ADR-008 / ADR-038 as amended / ADR-020 / PDR-009 / PDR-017; amends `A3-DL-001`); invariant `A3-P2-INV-001`. **Done** @ 1f2846e.
  - **A3-CLOSE** — decision-log (`A3-DL-003`) + ledger, contract and spine reconciliation; the frozen P1–P2 ladder recorded in the repo ledger, and the milestone recorded by what it contains rather than by the title it was opened with. Lands the contract-file edits both A3 entries deferred here: the live-deploy-path enumeration gains `docker-entrypoint.sh` in **all four** copies — `AGENTS.md` §7, `scripts/check-contract-integrity.sh`, `docs/RUNBOOK.md` (without which its claim about what the contract files enumerate would have been false again, the `A1-DL-008` / `L2-DL-004` pattern) and `.pre-commit-config.yaml`, the fourth site neither entry named and the only one where the omission was a live hazard rather than a documentation gap. Corrects an **owner-run ordering error** in the A3-P1 procedure: it prescribed setting the PWA trigger substitutions *before* merge, which `MUST_MATCH` turns into a red build on every push until the packet lands — P1 now carries P2's ordering, with the reason stated inline and the two "inverse ordering" cross-references (`docs/RUNBOOK.md`, `api/cloudbuild.yaml`) corrected with it. Rewrites the RUNBOOK **Staging contour** section end to end so the whole section reads as a procedure to execute rather than a description of something running, and collapses the `_API_UPSTREAM_URL` / token-audience rule to exactly one canonical statement referenced from elsewhere. Reconciles the M5 origin-unification assignment in `docs/product/BuildPlan.md`, `docs/product/TechSpec.md` and this file, and the one "accounts-oauth" milestone-name reference in `docs/INVARIANTS.md`; the two A3 decision-log entries are left **byte-untouched** and amended from `A3-DL-003` instead, per this log's own amendment convention. Records the seven debts that stay **OPEN** after the milestone, each with what would close it. No new invariant (a close-out enforces nothing new, `AGENTS.md` §10); no new **Current** track; operability none. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).
- **L1 local structured core — Done.** Family data moves off browser `localStorage` into a device-native, schema-bearing, append-only store with an export contour. Platform/data track, the first milestone of the PDR-026 L1..L7 ladder — sits alongside the other cross-cutting tracks, **outside** the M1–M5 engine spine; not naively "next" in that ladder. Realizes vault **ADR-043** (Capacitor; native storage as the source of truth; WebView storage forbidden for persistent data and permitted only as a losable cache) and extends the **ADR-037** parity-suite precedent to a second delivery channel rather than inventing a mechanism. Approved packet ladder, **reordered by owner act at L1-P2** (vault ADR-046 §2.4): **P1 capacitor-shell-parity → P2 local-schema-and-journal → P3 export-contour → P4 write-path-and-migration**; the PR opens once, after P4. The export contour moves ahead of the write path so that the first family records are recoverable before they are written; the pre-reorder order (P3 write-path, P4 export) stood in this file until `LSC-DL-002` corrected it.

  **Id split, stated because this one is a genuine collision rather than a style choice:** PDR-026's ladder id `L1` is already owned by the shipped **L1 Promotion Gate** track (`L1-DL-001`, a deploy-safety milestone). This milestone therefore takes `LSC-` for decisions and invariants — `LSC-DL-{NNN}`, `LSC-P{k}-INV-{NNN}` — while packets keep `L1-P{k}` as the approved ladder names them. Same track, two prefixes; the `A2`/`L2` precedent applied to a clash.

  - **P1 `capacitor-shell-parity` — Done this packet.** The web app gains an Android container and nothing else: a Capacitor shell whose web root is assembled from `app/Dockerfile`'s `COPY` list, so both channels ship the same bytes and the production web path stays buildless. **Zero product change** — no shipped `app/` file is touched and no parity baseline is re-blessed. Lands two enforced invariants (`LSC-P1-INV-001` — Web Storage has exactly one door, the seam P2 replaces; `LSC-P1-INV-002` — byte-identity between the channels, and Android Auto Backup off so the app sandbox is not uploaded off-device), extends the parity suite with a `native` project that asserts the committed `dom-mobile` baselines under a Capacitor-shaped delivery profile, and adds a fourth CI job (`android`) — the only gate in this repo that cannot be reproduced on the development machine, which has no JDK, no Android SDK and no Gradle. Also corrects a **false `MUST_MATCH` claim** in all three build-configs and `docs/RUNBOOK.md`: no build-config declares `substitutionOption` at all, so the described typo protection never existed; the Step 0 emptiness guard is what does. **The APK has never been built and nothing has run on a device** — those are the CI job's and the owner's to establish (`LSC-DL-001`).
  - **P2 `local-schema-and-journal` — Done this packet.** The schema **freezes**: one SQLCipher-encrypted SQLite file, STRICT tables, an append-only `journal_entry` spine with three typed detail tables, UUIDv4 ids minted at creation, and every piece of derived state expressed as a view rather than a stored row. All sixteen PDR-026 §4 slots are mapped one-by-one to the object that carries them and the test that exercises them; four of the five accompanying rules are closed here and the fifth (export durability) is a requirement on the export contour, addressed to P3. The wrapper (`@capacitor-community/sqlite` 8.1.1, confined to raw SQL) and its encryption (SQLCipher, keyed on-device, on from creation) are chosen **together** and verified against primary sources — which is where the packet's red result came from: the bundled engine compiles **FTS5** in but **not ICU**, and ICU would not have solved Russian word forms anyway, so what the schema secures instead is a fully rebuildable index. The merge-semantics run against **both** CRDT candidates found a real defect in the packet's own first draft — projections keyed on local arrival order (`seq`) diverge between two devices holding identical histories — and the winner rule became `(entry_at_utc, id)` before the freeze. Six enforced invariants (`LSC-P2-INV-001`…`006`) and a fifth CI job (`android-instrumented`, **not** a per-push gate, by owner decision on spend). The web channel's behaviour is unchanged: `core/storage.js` and its call sites are untouched and no baseline is re-blessed. Three of the packet's own premises were corrected rather than served (`LSC-DL-002` (p)). The browser-driven parity projects could not run on the development machine, as `LSC-DL-002`'s validation caveat states. *(Corrected in L1-P3: this line also read "**Nothing has run on a device or an emulator**", which `LSC-DL-002`'s own corrected caveat had already overtaken — the `android-instrumented` job was run twice by `workflow_dispatch`, run `31612297495` passing all eleven tests.)* **The service-worker-inside-the-WebView disposition stays OPEN** and is carried to **P4**: it is a product-behaviour change to the native channel, it was not in this packet's approved scope, and it was not taken unilaterally. Decision `LSC-DL-002`.
  - **P3 `export-contour` — Done** @ 8184606. **Scope narrowed by an owner gate closed immediately before the packet opened**, and the line above it in this file is what the gate corrected: there is no encrypted dump on this rung and no export-key fork. P3 ships exactly **one** object — the durable artifact: an ordinary `.zip` of UTF-8 text files, a JSON sidecar index, a PDF/A print layer, an empty-and-self-explaining `attachments/` directory and a plain-language Russian README, **unencrypted and needing no key**, produced entirely on the device with no network, no account and no subscription check. The encrypted operational snapshot, its key and its recovery channels move to **L7** with the relay and key-scope model that are its only consumers; the pre-migration rollback copy was redefined by the same gate as an *operational* byte-for-byte snapshot restored on the same device, addressed to the first schema migration (L2+). The RUNBOOK gets a **"reading the archive without the app"** procedure rather than a restore procedure — restore implies import, and import is L6. Closes accompanying rule 5 (the export artifact stays human-readable and self-describing for years without the app), which `app/tests/schema/test_store_slots.py` now resolves to a live test rather than a deferral string. Executed in **two checkpoints on this branch**: the first lands the artifact, the SAF sink, the surface and the docs with no print layer; the second lands the PDF/A-2b layer, its vendored font and ICC profile, its structural checker and its veraPDF verification — placed in the existing `android-instrumented` job, which already sets up Java and is where expensive verification lives by the standing owner decision on CI spend. Four enforced invariants (`LSC-P3-INV-001`…`004`). Decision `LSC-DL-003`.
  - **P4 `write-path-and-migration` — Done** @ 460bd39. The app starts using what the three previous packets built. A mark becomes an **attributed assertion** carrying a confirmation by its own author — so "confirmed by one" is what the general consensus function returns rather than a special case (PDR-021) — and a skill's state is projected from the journal on every read rather than stored. The sync→async seam turned out to be **a tenth of the size the handoff described**: `core/zpd.js` never read storage, so holding the projection in memory left all five synchronous readers untouched and the async boundary two functions wide, which is why the write path could be turned on with **no DOM and no visual baseline moving**. The explicit `localStorage` → journal import is re-runnable by **derived ids** with no ledger, completes an interrupted run rather than corrupting it, and **cannot** write to its source — it imports no writer. Migrated entries wear the import date with a NULL instant and `origin = 'migrated_legacy'`, and the **archive says so** (`event_date_basis`), so a reader in 2044 is not handed three hundred skills mastered on one afternoon. Creates the **client-side signal surface** that did not exist (three kinds, console-only sink, payloads structurally unable to carry family text; a network sink is a separate owner decision). **Closes the service-worker disposition deferred twice** — native registers none and purges what an older APK left — and **strips GA4 from the native channel**, closing `LSC-DL-001` (o) for that channel only, the web half staying open. **The two delivery channels diverge after this packet and this milestone does not resolve it**: the import is a one-way, parent-triggered bridge, and reconciliation is L7's. Four enforced invariants (`LSC-P4-INV-001`…`004`); decision `LSC-DL-004`. Executed in **five checkpoints on one branch**. **Three side-finds are carried to the L1 milestone close, addressed rather than listed:** (0) the instrumented job shows **SQLite journal-mode contention** — the app opening its store at boot while a test holds the same file (`database is locked; [PRAGMA journal_mode=delete]`, four lines in run `31680204645`'s logcat). Benign in that run, since every store test passed, but it is two writers on one database and "benign" there is a property of the timing rather than of the arrangement. (1) `closeStore()` is defined and never called, so `clean_shutdown` never returns to 1 and the new `store.open` signal reports `previous_run_clean` **false on every launch but the first** — the signal is honest and the defect is upstream of it; it turns on whether the WebView reliably fires `pagehide` before process death, which needs its own evidence. (2) The gitignored `cap sync` artifact under `native/android/app/src/main/assets/public/` is stale on the development machine (missing `store/` and `export/`) and will mislead anyone who reads it as the shipped set. All three are recorded in `LSC-DL-004` — (0) as debt 6, (1) as debt 1, (2) as debt 2 — and the two an owner meets in practice, (1) and (2), are stated in `docs/RUNBOOK.md` at the point where a reader meets them; (0) is a CI observation and lives in this line and in the entry. *(Corrected in L1-P5: this sentence read "**Both** are recorded … (debt 1 and 2) and **both** are stated in `docs/RUNBOOK.md`", a count left over from when two side-finds were listed here rather than three.)* **Disposed of in L1-P5** (`LSC-DL-005`): (2) closed as documented-not-fixed, (0) and (1) carried as open debt with what would close each. **Also open at the close and not resolved here:** whether the **web** channel should carry analytics at all — `LSC-DL-001` (o) is closed for the native channel only. *(Corrected in L1-P5: this line ended "The milestone PR opens here." The close-out packet L1-P5 was added after it, and the PR opens there.)*
  - **P5 `lsc-close` — Landed this packet.** Milestone close-out: decision-log (`LSC-DL-005`) + contract and ledger reconciliation; the frozen P1–P4 ladder recorded in the repo ledger. Docs only — no product code, no schema, no test, no assertion. Lands the two contract edits `LSC-DL-001` deferred here **by name** (its rejected alternative 15, "contract edits stay out of packets and land at milestone close"): `AGENTS.md` **§8**, whose layout block gains `native/` and whose **Current** paragraph stopped being true the moment a second delivery channel existed; and **§3**, which gains an intent-level entry for the device-local family store (ADR-043 / PDR-026 — the on-device encrypted append-only store is the **source of truth** for the family's marks, WebView storage is a losable cache), widens the monorepo entry to three subtrees, and scopes the ADR-008 entry to what the decision layer has amended it to mean: it describes the **server** contour and reads as a specification of rung **L4**, not as a statement about where family data is stored today. Verified rather than assumed by re-running the contract-integrity gate that governs those files, before and after the edits. **Records the device evidence as an observation, correcting `LSC-DL-004`'s caveat in the opposite direction** — run `31683691498` (`workflow_dispatch`, head 460bd39) is green with `android-instrumented` at **8m39s** and **16/16** instrumented tests, so the corrected `WebViewStorageTest` ran on the emulator and `LSC-P4-INV-004` is **observed**, not merely implemented. The boundary is stated with it, because a fresh emulator has no prior installation: the job proves **no service-worker registration and no shell cache exist after a booted app**, while **a prior registration is actually purged** stays the owner-run `docs/RUNBOOK.md` step 1→2. **No emulator dispatch was owed for this packet** — it changes no shipped byte, so the device layer cannot move; the push run covers `quality`, `parity`, `eval` and `android`, and the PR event re-runs `android-instrumented` on its own trigger with nothing here gating on the result. Disposes of the three carried side-finds (above), re-addresses the ICU wording divergence (`LSC-DL-002` (c)) upstream as an owner-side task after merge, **offers** the `LSC-DL-003` (r-bis) geometry rule to `AGENTS.md` §9 rather than adopting it unilaterally, and rolls up the residual debt surviving the milestone with what would close each item. Two escalations batched into the plan: the **web** channel's analytics question and **release signing + key custody**, both with the deadline restated as the moment the loss window opens rather than as the distribution date. No new invariant (a close-out enforces nothing new, `AGENTS.md` §10); **no new Current track** — the next-track choice is the standing open owner decision recorded at the M5 line; operability none. Parity green in the pinned container with **zero baseline writes** — for a docs packet that is the load-bearing number, since a documentation change that moves a rendered baseline has done something it did not intend. Decision `LSC-DL-005`. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).

- **EMV export-contour reachability + L1 promotion — Done, in six packets.** This line was flipped to Done by the close-out at **four**, and P5 then P6 landed after it: **"close-out" names P4's role, not the track's last packet**, and the Done here is only as good as the `android-instrumented` run that verifies P6 — nothing before that run is evidence about it, and P5's own claim to have fixed that job did not survive its next execution. The L1 export contour is reachable from the UI and proven so by a **click that executes**, on both parity channels. The legacy-import offer and the store-unavailable notice were revived by the same CSS rule, but their behavioural claims are **native-only and remain open**: both parity channels serve the staged web root with no Capacitor bridge, so both take the web branch and neither trigger is reachable there (ADR-043; `EMV-DL-001` (g) + debt 3, `LSC-DL-005` debt 13). What the branch additionally establishes is delivery — an installed client on the previous generation reaches the current mount and sees the fixed surface, executed on both update legs against a staged previous generation. Cross-cutting delivery/correctness track sitting on top of the closed L1 milestone, **outside** the M1–M5 engine spine. Traffic is still held on the pre-L1 build precisely because of the defect P1 fixes — the revision identifiers stay in `docs/RUNBOOK.md` (`M1-P3-INV-002`); promotion itself is a separate manual owner act **after** the milestone (vault ADR-020 — a merge deploys a zero-traffic revision).
  - **P1 `export-modal-visibility` — Done** @ f6378e4. `app/m/v1/app.css` declared `.modal { display: none }` with **no** `.modal.show` rule, so the three surfaces opened by `classList.add('show')` on a bare `.modal` — the export modal, the legacy-import offer and the store-unavailable notice — completed their handlers and stayed invisible, on both channels. Read from the code rather than the report: there are **four** bare-`.modal` elements, and the fourth (`#createProfileModal`) is inline-`style.display`-driven, so it never depended on the rule and is unaffected by it. The rule lands as `display: block` (block-flow, matching `.modal-content`'s `margin: 10% auto` and the ≤767 px fullscreen branch), **in `app/m/v2/`**: `docs/RUNBOOK.md` → **Module mount** (`A1-DL-004`) forbids rewriting the bytes of a published immutable mount, and a `CACHE_VERSION` bump alone refills the precache through the 30-day immutable HTTP cache and can hand back the stale file, so the fix would reach nobody who already has the app installed. Copy-forward `/m/v1/` → `/m/v2/` with `app/m/v1/**` left **byte-untouched**, `CACHE_VERSION` `v11` → `v12`, the shell's 45 references and the worker's 44 `OFFLINE_URLS` entries repointed, and — the step the RUNBOOK checklist was missing until this packet executed it — the mount's **own** self-referential knob URLs (`schemaUrl`, `declarationUrl`, `fontUrl`, `iccUrl`) repointed with `changed_in` provenance. The behavioural claim is carried by a **click**: `app/tests/behavior.spec.js` presses `#exportBtn` and reads the computed style, in the `behavior` and `native` projects. The static half — every `classList.add('show')` has a matching `.show` rule — is a separate guard (`app/tests/show-rule-coverage.spec.js`) that is explicitly **not** allowed to stand in for it, and the mutation runs measure exactly how far it does not reach. Thirty-five mount literals across ten specs and three Python constants now derive the mount from the shell, so a guard cannot keep asserting against the frozen generation after a bump — the same "green for the wrong reason" failure the packet exists to fix. New invariant `EMV-P1-INV-001` (both halves, with the coverage half's limits written into its Scope). Operability: no new knobs, no new signals; four existing knob **values** repointed with `changed_in: EMV-DL-001`. Decision `EMV-DL-001`.
  - **P2 `runtime-claim-rule` — Done** @ 2679915. The measure P1 applied to one surface becomes the repository's standing rule, in the permanent agent layer rather than in a packet's own decision entry: **a claim about runtime behaviour counts only once a test that executes that behaviour has executed it**, and a guard is **static** when its own code never starts the product — no page, no emulator, no process. Scanning for static properties stays legitimate and preferred where it fits (presence of a file, absence of an import, shape of markup, composition of the ship list, provenance of a knob). Its single normative home is a new `AGENTS.md` **§11 Evidence bar**, placed beside §10 because the two are the same kind of rule — §10 bars what may be claimed as *enforced*, §11 bars what counts as *proof* of a runtime claim — with `Historical artifacts` renumbered §11 → §12 (verified to break no citation: the only `§11` occurrence in the tree was that header line itself). It ships with its **detector**, named in three parts, because an obligation without one is an intention: the **check** (could the guard stay green with the handler body emptied or the shipped rule deleted; does its own code start the product), the **admissible hits** (the static list, which makes a negative result provable rather than absent), and the **moment** (plan authoring, plan review, acceptance of the execution report) — including the requirement that a packet with **no** runtime claim says so outright, since a silent Validation section is indistinguishable from an unchecked one. `CLAUDE.md` carries **pointers only**, on the two bullets where the rule bites (Plan shape → Validation; Execution report shape → How it was validated); the normative text exists once, because two copies drift. Grounded in the four L1 claims that passed for the wrong reason, each with its class named — vacuous fixture, over-matching substring, regex failing open, static guard standing in for a runtime claim — the fourth being what held this milestone's promotion. Docs only: no code, no test, no invariant (`AGENTS.md` §10 — the detector is procedural and enforces nothing in code), no knobs, no signals, zero egress change. Decision `EMV-DL-002`.
  - **P3 `installed-client-upgrade-path` — Done** @ ef951c2. The one runtime claim the branch still carried by derivation is executed: that a client already installed on the previous generation ends up running the new mount and sees the fixed surface. `EMV-DL-001` said so in its own caveat, and it was not hypothetical — L1-P3/P4 rewrote `app/m/v1/**` in place, so live clients hold pre-L1 bytes of that mount behind `public, immutable, max-age=2592000`. The parity server gains a **second** cookie-keyed switch beside the `/sw.js` bump, serving the **previously published generation**: the shell and worker rewritten from the current mount to the previous one with `CACHE_VERSION` decremented, while `/m/v1/**` is served from disk as the frozen generation it already is — so the staged client **reproduces** the invisible-modal defect rather than imitating it. Verified rather than claimed: the staged shell is **byte-identical** to `app/index.html` at `711b5bc`, EMV-P1 having changed nothing in it but the repoint. **Both update legs are asserted, and the second is the one that mattered:** the client that accepts the banner, and the client that never touches it and simply opens the app again — most installed clients — which the plan had scoped out as a curiosity and which is in fact the promotion-relevant path. Measured: **both land on the current mount**, the unaccepting one on its next navigation, because navigations are network-first and the new stylesheet sits at a URL it has no cached copy of; with the fix shipped at the *same* mount URL the immutable window would have handed it the stale file, which is `A1-DL-004` observed rather than argued. The fixture is generation-relative (`previousMount()` beside `currentMount()`), so the next bump re-aims it with no test edit and a retired mount makes the two legs **skip with the reason printed** instead of passing quietly. Soundness by mutation, including the one that **did not** bite: reverting the shell repoint makes the fixture skip rather than red — 44 mount-literal assertions in `delivery-contract.spec.js` red instead — and that is reported rather than replaced. New invariant `EMV-P3-INV-001`. `docs/RUNBOOK.md` gains **Promotion + rollback step 5**, written as actions with a pass condition — open the app on a client installed *before* the deploy and see the modal appear — because the spec proves the mechanism against a staged generation that no particular live client holds, and naming that bound honestly while leaving the real check unwritten would reproduce the gap one level up. **No shipped byte changes**; operability: no new knobs, no new signals. Side-find carried, not fixed: `page.waitForFunction` with an async predicate resolves on the first call, so the precache-settle wait in `behavior.spec.js` does not wait (measured). Decision `EMV-DL-003`.
  - **P4 `emv-close` — Done** @ 330089e. Close-out **in role, not in sequence**: it was authored as the track's last packet and P5 followed it, because the PR-only `android-instrumented` job was red and the close-out's own record could not see that (`LSC-DL-005` debt 13 — the parity suite is structurally blind to the native branch). Decision-log (`EMV-DL-004`) + ledger reconciliation; the frozen P1–P3 ladder recorded in the repo ledger, and the track's **goal sentence narrowed to what executed** — it claimed the legacy-import offer was made reachable "proven by tests that execute the behaviour", which is exactly the substitution `AGENTS.md` §11 forbids, and a track that introduced that bar cannot close on a record the bar rejects. Retires the one measured false wait `EMV-DL-003` carried as a side-find: the precache-settle wait in `app/tests/behavior.spec.js` moves from `page.waitForFunction` with an **async** predicate — which resolves on the first call whatever the predicate computes — to the `expect.poll` form P3 already adopted, predicate body unchanged and the measurement written into the comment. Taken here rather than carried because it is one line, in a file this close-out's own record describes, and a wait that does not wait is the failure class the track exists to end. **Measured both directions rather than argued:** a predicate that sleeps 1.5 s and returns `false` **reds** the corrected wait at the 30 s poll timeout with its message, and under the old form the same predicate returned after a single evaluation (1537 ms) with the test **green**. **No shipped byte changes** and no `CACHE_VERSION` bump; no new invariant (a close-out enforces nothing new, `AGENTS.md` §10); operability none; **no new Current track** — the next-track choice is the standing open owner decision recorded at the M5 line. Rolls up the debt surviving the track in three blocks, each item with what would close it: this track's own, L1's carried forward unchanged, and the **stale contract/docs statements** this track found and deliberately did not fix — named in the ledger rather than only in a plan, because the ledger is where the next milestone looks. Decision `EMV-DL-004`. The PR opened at this packet (`AGENTS.md` §9) and stayed open through P5.
  - **P5 `native-webroot-integrity` — Landed this packet.** The PR's `android-instrumented` job was red on one test of sixteen, and the cause was **not** what the branch had changed about staging: the failing test addressed the frozen mount, which a copy-forward bump leaves shipped, so its import **succeeded** against the generation nobody runs while the app under test had opened its store in 884 ms — read from the run's own logcat, which was the packet's first task and which refuted both offered hypotheses before any fix was designed. Two instrumented sources stop writing a mount version down and derive it instead, a third named the frozen generation in prose; the drift class is closed by a new guard that runs on **every push** rather than only at PR, which is the window that let a mount bump travel four packets before meeting a real WebView. New invariant `EMV-P5-INV-001`, **static and said so** (`AGENTS.md` §11) — it carries no runtime claim, and the claim that the app opens its store at boot on the native channel is executed only by the emulator job. **No shipped byte changes**; operability none. Closes `LSC-DL-005` side-find (2) on the local hop, demoted in the diff itself to what it is: not the fix. Decision `EMV-DL-005`. **Its store-probe re-anchor did not make the job green** — see P6.
  - **P6 `store-probe-anchor` — Landed this packet.** `android-instrumented` stayed red on the same one test of sixteen, now failing in ~10 s with P5's own diagnostic speaking: the probe found no `store/boot.js` delivery hint in the document. The handoff read that as the hint being absent from the shipped shell — a store subgraph reached by dynamic `import()` and therefore legitimately unhinted — and the code refutes it: `app/m/v2/app.js` imports `./store/boot.js` **statically**, so direction (b) of `A1-P6-INV-001` forces the hint, `app/index.html` carries it, the ship-list guard reds without it, and the walker throws on `import(` in a shipped module so a dynamic store import is not currently expressible. **The invariant is true, held throughout, and is not amended.** What it asserts is a set equality over `app/index.html` — a property of the **file**; P5 inferred from it a property of the **DOM at an arbitrary instant**, which is `AGENTS.md` §11 one layer in from where this track found it. The real defect is that the store probe **had never waited for anything**: it evaluated on the line after the activity launched, and the store's hints are the last block of a ~147-line `<head>` behind an inline script, so a mid-parse `querySelector` legitimately answers null — the `EMV-DL-005` logcat dates the old probe at **16 ms** after the shell's first module request, inside the parse. The same defect class was already found, fixed and device-executed **in this repo** (`WebViewStorageTest`, run `31680204645`, "a document that had not booted"), and this test did not inherit the fix. So: gate first, anchor second. The probe waits on the sentinel the app itself produces — populated table rows, which appear only after `Promise.all([kbReady, initNativeStore()])` resolves, so a row proves the graph **evaluated** rather than that the parser finished; the gate returns the `src` the live document executes and it is asserted equal to the mount `MountAddress` reads out of the APK, so a WebView running `offline.html`, a cached response or the frozen generation reds by name; and only then is `boot.js` composed as `MountAddress.prefix() + "store/boot.js"` — the volatile half derived, the stable half stable **by construction** (`app.js` names it as a static relative specifier and `A1-P4-INV-001` asserts the resolved path shipped and precached every push), the same composition `StoreEngineTest` executed on this device in the same run. One `assertEquals` on the dispatch return closes the hole where `evaluate()` reports a thrown expression as null — which is why run `31750267059`'s red is re-read as **not established** rather than as wrongly diagnosed: an `about:blank` base throws, and a probe that never dispatched produces the identical 31.4-second silence as one that dispatched against the frozen generation. The sibling test's accidental wait is **settled rather than observed** — it keeps no boot gate, because its claim is about the injected bridge and must not depend on the app booting, and the code now states why the wait is sound (it cannot go green early; every failure mode is false-red) and what would end it (any rewrite making the polled expression total). **`EMV-P5-INV-001` is not extended, and that is the finding:** this defect had no statically checkable shape — the one static assumption in play was true and already machine-checked — so a guard here would be for appearance. No new invariant (the packet enforces nothing new in code, `AGENTS.md` §10). **No shipped byte changes**; operability none. Two comments the code had falsified fixed in the file already being edited. **Whether this works is established only by the next `android-instrumented` run** — nothing on the development machine compiles Java, and the off-device check is a shape check under stubs, said so. Decision `EMV-DL-006`.

- **release-signing (RSN) — Done, in five packets.** *(Read "Done" as: the milestone's design landed and closed at `RSN-CLOSE`. Two packets were opened **after** that close, because the gate `RSN-P2` built went red on the first real run of the thing it was built to prove — `RSN-P3` to diagnose, `RSN-P4` to repair. This line read "in three packets" until `RSN-P4` corrected it; `RSN-P3` landed without a ledger line at all, added below with it.)* The Android channel gets **its own signing key** and a release build that is fail-closed without it, machine-checked against a committed certificate fingerprint, and published **by hand**. Cross-cutting delivery/custody track on the direct-APK channel (ADR-043 §3, vault ADR-047) — sits alongside the front tracks and EMV, **outside** the M1–M5 engine spine; not naively "next" in that ladder. **The `Release` workflow has never run, on any trigger** — every step inside it executes for the first time under the owner's hand, and `docs/RUNBOOK.md` marks which ones those are rather than reading as a record of things already done (`AGENTS.md` §11 applied to prose). **The first release build costs an uninstall-reinstall on every family device** — the release key is not the debug key, so Android will not update over the installed build and the app sandbox goes with the uninstall; it is done now because the window in which that costs nothing closes with the first diary packet (ADR-047). Key custody is the accepted solo shortcut with a non-deferrable obligation attached (`solo-shortcuts-registry` C-2): two separate backups, the password held apart, and a **periodic restorability check** that this track had to write correctly rather than as ADR-047 §7 words it. Nothing here creates automatic delivery on this channel — ADR-020, and on Android there is none at all.
  - **P1 `release-signing` — Done** @ ea70259. The release build signs with a key that exists in **no branch of this repository**: it reaches Gradle as three environment variables (path, store password, alias), never as the base64 transport encoding, and nothing echoes a value or interpolates one into a string. `keyPassword` is deliberately the **same variable** as `storePassword` — PKCS12 has no separate key-entry password, established by a `keytool` run that refuses to set one, so ADR-047's two-password wording is implemented as the recorded factual deviation and there is no fourth secret. Fail-closed is a `gradle.taskGraph.whenReady` guard that aborts **before any task executes**, matching release **packaging** tasks of this module only (`lint*Release` and every debug task deliberately outside it), each error opening with a bare greppable `THEYGROW_RELEASE_*` token and none of them printing a secret — the keystore-open failure prints the exception **class**, and the alias failure prints neither the alias asked for nor the aliases present. `versionCode` becomes `git rev-list --count HEAD` — reproducible from the commit and monotone along ancestry, where a CI run number is neither — with a **shallow clone treated as unresolved and failing the release path closed** while every other path keeps the bit-for-bit value it already had. **Executed, not read** (`AGENTS.md` §11): the `android` job runs `:app:assembleRelease` twice, with no signing environment and against a keystore it cannot open, asserting the named token and that **no release APK exists** — both passwords minted random at runtime, so the leg is proven with no real secret and no password-shaped literal in the tree. Placed in the free per-push job rather than `android-instrumented` because the guard throws at task-graph-ready and costs a configuration, and **after** the debug-APK upload so a red here does not also cost the owner the installable artifact. New invariant `RSN-P1-INV-001`. Decision `RSN-DL-001`.
  - **P2 `signed-release-build` — Done** @ 19f62af. The signed build gets **its own workflow** — tag push `v*` and `workflow_dispatch`, and deliberately **no branch push**, because every run decodes the keystore and the number of runs that touch it is minimised on purpose (C-2). It **publishes nothing**: `permissions: contents: read`, no release created, no `gh` — CI-side publication would need `contents: write` (PDR-018) and is a decision ADR-047 leaves untaken. The verdict is a **file, not a `grep`**: `scripts/verify_release_signature.py` exists because the release path's positive verdict only ever runs with the real secret, so an inline check could never be *executed* proving it can go red — this one is, twice on every push (`pytest app/tests/release` on synthesised output; real `apksigner` against the real **debug** APK in `android`, where the correct verdict is exit **3** specifically). *(Corrected in RSN-P4: the second of those two has never returned 3 — it returned **4** from this packet until `RSN-P4`, on a signer-line shape the pattern did not cover, failing closed but never reaching the comparison. The synthesised half is proven; the real-tool half is **unproven until a run is green**. `RSN-DL-005`.)* **The parse pattern's `certificate` is load-bearing:** `apksigner` prints a certificate digest *and* a public-key digest for the same signer, they differ, and matching "SHA-256 digest" alone would be wrong in both directions — including **green on a wrongly signed APK** had a baseline ever been recorded from the same wrong line; a test pins it with output carrying only the public-key lines. The baseline is **committed and public on purpose**, uppercase colon-separated so it reads against `keytool`, and never an Actions secret — a detector whose expected value shares a store with the key it checks proves nothing. Each release run **arms the detector before trusting it**, on a differing fingerprint derived in-run from the live baseline rather than from a committed fixture that could be edited into agreement with a broken comparator. The artifact is named from the build's own `output-metadata.json` (never re-derived, never `latest`) with a `sha256sum`-format sidecar; the decoded keystore lives only under `RUNNER_TEMP` and is removed by an `if: always()` step — `rm`, not `shred`, because the runner disk goes with the run and `shred` on an overlay filesystem does not deliver what its name promises. New invariant `RSN-P2-INV-001`. Decision `RSN-DL-002`.
  - **RSN-CLOSE — Landed this packet.** Milestone close-out: the owner release procedure in `docs/RUNBOOK.md` (trigger → what the run produces → hand-publication with the versioned filename, the SHA-256 beside it and the explicit unknown-sources warning → each red outcome), decision-log (`RSN-DL-001`…`003`) + ledger + invariants reconciliation. Docs and ignore hygiene only — no workflow, no Gradle, no comparator, no test, **no shipped byte**. Three things it refused to blur: the procedure **says inside itself** which steps are proven per push and which the owner executes for the first time; the two red outcomes are written **apart**, because a wrong secret is cured by **re-minting the secret and never the key** (a new key is a new app identity and a second reinstall for the family) while a `CERT_MISMATCH` has two causes the run prints side by side and a cure that is **never** to relax the check — with the baseline branch documented as the **two-file** edit it is, the committed fingerprint and the `EXPECTED` constant pinned in the suite; and the **restorability check is two legs**, because ADR-047 §7 as written names the `apksigner`-on-an-APK leg, which cannot fail when a backup is unreadable — the `keytool`-against-each-backup-copy leg is what discharges the obligation and is named as such, the artifact leg proves a published binary is still ours, and neither substitutes for the other. The first-install consequence is stated as a **data event**: uninstall-then-install takes the app sandbox, export first — **and the app-side export does not protect the family's real history**, which is in the browser's `localStorage` under the production origin, is untouched by any uninstall, and must not be cleared until the browser→app transfer path exists (ADR-043 §3 and its 2026-08-15 annotation). Corrects the statements this milestone falsified — `docs/RUNBOOK.md`'s "no release keystore exists", `native/.gitignore`'s "debug signing only", and a third the packet's own containment check surfaced in a file it already had open (`LSC-P1-INV-002`'s Scope, same claim) — and **no others**: a close-out is not a licence to sweep, and the candidates found and left are named in the roll-up rather than fixed. Closes the keystore-ignore asymmetry at owner direction: `*.p12` / `*.pfx` reach `native/.gitignore` and the repository root, which carried no keystore pattern at all, since the failure mode of that gap is a private key in history that no later commit removes. Rolls up what the milestone carries forward in `RSN-DL-003` with what would close each: the **three unexecuted guard legs** — with `THEYGROW_RELEASE_VERSIONCODE_UNRESOLVED` called out as the one the release path's `fetch-depth: 0` contract rests on, and the shape that closes all three in the existing per-push step, free, because that job's checkout is already shallow; that release runs produce **no Telegram notification** (`notify.yml` is scoped to `workflows: ["CI"]`) and why that was left alone; and that publishing from CI needs `contents: write` and has not been decided. No new invariant (a close-out enforces nothing new, `AGENTS.md` §10); operability none; **no new Current track** — the next-track choice is the standing open owner decision recorded at the M5 line. Decision `RSN-DL-003`. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).
  - **P3 `detector-parse-fidelity` (checkpoint A, diagnose) — Done** @ 59748ee. Opened **after** the close, because the gate P2 built went red on the first real run of the thing it was built to prove: exit **4** (`CERT_UNPARSEABLE`) against the debug APK, and a log that could not say why. It **repairs nothing on purpose** — at least two explanations survived that log and they needed opposite repairs, so shipping a candidate fix beside the diagnostic would have destroyed the measurement, and a green would not have said which change earned it. What it adds is a comparator that says what it SAW on the three verdicts that are blind by construction (4, 7, 8): how `apksigner` was resolved and which candidates lost, what argv ran, how many bytes came back on **each** stream, a bounded DN-redacted excerpt of both, and a one-line `READING:` naming which explanation those bytes support. Second change, one line: the fail-closed step gains `if: ${{ !cancelled() }}`, because steps default to `if: success()` and a red comparator had been **skipping** the other detector entirely — so the milestone did not know whether those legs passed. No new invariant. Decision `RSN-DL-004`.
  - **P4 `detector-pattern-repair` (checkpoint B, repair) — Landed this packet.** The measurement came back on run 31896998977 and named a **PATTERN defect** in one run — certificate block on stdout, stderr empty, so the wrong-stream hypothesis died on the spot. build-tools 37.0.0 prints `V2 Signer: certificate SHA-256 digest: …` — **scheme-qualified, and with no `#N` on the line at all** — which the pattern missed three ways at once. The repair is generous about the **prefix** (four shapes now: the observed one, the classic `Signer #N`, the rotation-aware `(minSdkVersion=…)` form, and both combined — older shapes **kept**, since the two workflows need not run the same image) and immovable about `certificate SHA-256`, because the same block carries **five near-misses** and one of them is a 64-hex **public-key** digest: same length, same shape, wrong key. Each of the five gets its own test; the public-key one gets a second in the positive direction; and a comparator mutated to accept it was measured returning `CERT_OK` and exit **0**. The class is bounded to one line (`[^:\n]`, not `[^:]`, which crosses newlines under `re.MULTILINE`) — latent, not live, and caught in plan review. Lands the repository's **first recorded apksigner bytes** (`app/tests/release/fixtures/`), with a provenance header stating that the DN is **reconstructed, not observed**, and that the file records **SHAPE and never a value** — those digests are an ephemeral debug key's. Multi-scheme stated and tested: one certificate under several schemes **collapses** to one digest, different certificates still reach exit **6**. "Highest build-tools wins" **kept**, re-examined against the evidence rather than by habit, and its price paid in the open — every verdict of the `--apk` path now names the tool that produced it. No new invariant; `RSN-P1-INV-001` and `RSN-P2-INV-001` touched up instead. Decision `RSN-DL-005`. **The claim it still cannot make:** that the comparator returns 3 against a foreign certificate with the real tool — executed by the `android` job and by nothing else, and **unproven until a run is green**.

- **local diary (L2 / DIA) — Done.** The family's history leaves the browser and the product gains the first thing a parent WRITES. Second milestone of the PDR-026 L1..L7 ladder, on branch `feat/l2-local-diary` cut from `main` at 9ba19dd. Recorded here by what it contains rather than by the ladder it was opened with, and the ladder was four packets against eight landed — three of the four extra are repairs, kept as their own entries because each was found by a device dispatch after the packet had been called done, and the fourth is P5, which is not a repair of anything this milestone shipped but a closure of a leak underneath all of it.
  - **P1** — `history-transfer-bridge`: the browser→native transfer (ADR-048), staged on both hops, every guard refusing before a byte is retained, with an automatic file fallback and no fork shown to the parent. Mount `/m/v4/`, `CACHE_VERSION` v14. Three invariants; the device halves discharged on `android-instrumented` run **31956524510** after two harness-fault dispatches. Decisions `DIA-DL-001`…`003`. **Done** @ 56bd46d / 751a005 / e103683 / 552a8d1 / 7adfd2e / 96a4fdf / c9ac7d5.
  - **P2** — `web-channel-and-export-ux`: one control per channel from one build, the download offer fail-closed against a release that does not exist, and the export flow's three owner findings repaired. Mount `/m/v5/`, `CACHE_VERSION` v15. Three invariants; the device half discharged on run **31963730354**. Decision `DIA-DL-004`. **Done** @ 679eb35 / f7f131e.
  - **P3** — `diary-entry-core`: a parent writes in the app; an entry is corrected by OVERWRITE; event time and entry time stay apart; sensitivity is written NULL because nobody was asked. Mount `/m/v6/`, `CACHE_VERSION` v16. **Two repair rounds, both found by dispatches:** P3R, because the save handler spread an identifier that did not exist and every create threw before reaching the store (`DIA-DL-006`, run 31971968427); and P3R2, because both write paths asked the wrapper for a transaction whose failing ROLLBACK spoke for the write, so a full disk asked a parent to try again instead of to free space (`DIA-DL-007`, run 31979084821). Green at run **31982061125**. Decision `DIA-DL-005`. **Done** @ 438710a / c04e03d / d322466 / 003951d / 844e027 / 64f068e / 6d88783.
  - **P4** — `diary-fts-and-close`: search over the diary, with the word-form strategy answered in the query rather than in the index and its ceiling **measured** across four settings; the rebuild path given an owner (the app, on the observable symptom, once per session); the embedded-JS parse guard promoted from an ad-hoc script to a gate over 103 snippets; and this docs tail. No schema change, no mount bump. Three invariants; the two device halves are **observed** on `android-instrumented` run 32044006357. Decision `DIA-DL-008`. **One repair round, found by a dispatch:** P4R, because the disk-full leg's arming proved the store full for a row nearly eight times wider than the entry under test — so the leg's earlier green was page layout rather than the property, and this repository had quoted it as evidence (`DIA-DL-009`, run 32012897363). **Landed in DIA-P4; this packet is its repair round P4R** (milestone PR opens at close, `AGENTS.md` §9).
  - **P5** — `family-text-out-of-the-device-log`: the milestone's own privacy invariant was false about a device. Capacitor traced **every** plugin call's argument object to logcat, so run 32044006357's artefact carries 707 trace lines with the child's name on 31, a diary body on 13, and the store's passphrase in cleartext on 1 — a surface open since **L1**, not opened by L2, and reaching a child's name on the launch that creates the child and on every read of the child list rather than on marks. Closed with one key (`android.loggingBehavior: "none"`), which also shut the `[signal]` channel that rode the same gate, so the app now carries that channel itself behind a one-prefix allowlist. New invariant `DIA-P5-INV-001` with a device executor and three in-run controls; `DIA-P4-INV-002` amended in both directions. No product change and zero bytes under `/m/v6/`. Decision `DIA-DL-010`, which is also the address `DIA-P4-CLOSE` resolves to. **Device half discharged on `android-instrumented` run 32074105863** — the dispatch, not the local emulator run that preceded it; the passphrase needle in particular could only be armed by CI's fresh image, and it read zero on a run that minted the key.
  - **What this milestone hands on.** The debt list lives in one place, in `DIA-DL-008` — continued at items 10-12 in `DIA-DL-010` — and one item is marked for the next milestone's **opening** rather than for a list: `closeStore()` is defined and never called, so `previous_run_clean` is false on every launch and `PRAGMA integrity_check` runs at effectively every start on the family's device. *(Corrected at L3-P1, `FIU-DL-001`: that item was taken up as this milestone asked — it is the opening packet of L3, and `closeStore()` is called there. The sentence is left standing as the hand-on it was, with its outcome attached rather than rewritten out of it.)* The artefact gap — that the archive carries no diary text — was **decided** by the owner gate of 2026-08-17 and addressed to **L3**; until it ships, a parent's diary text exists in exactly one copy on one device, which is a state with an address rather than a property of the product.

- **first live-install UX (L3 / `FIU`) — Done** @ `cccff4b` **(P1-P5; P5 is a repair packet after the first closing dispatch came back red).** Merged as PR #26 from `feat/l3-first-install-ux`. The ten invariant fields the ladder landed **Not yet observed on CI** were discharged after the merge, on `android-instrumented` run **32535042631** (`workflow_dispatch` @ `b882238`; `quality`, `eval`, `parity` and `android-instrumented` all green), and written into `docs/INVARIANTS.md` at PPR-P3. The rung after L2 on the PDR-026 ladder, opened by owner act on 2026-08-18 out of what the first LIVE install showed: twelve owner UX points, the removal of the in-app transfer offer, diary text in the long-term artefact (the 2026-08-17 gate addressed it here), and an in-app privacy-policy link. Decision ids are `FIU-DL-{NNN}` and invariants `FIU-P{k}-INV-{NNN}` — see the ladder above for why the ids are not `L3-*`.
  - **P1** — `store-close-and-first-run`: the store closes when the app goes to the background and the next open finds a clean marker, so `PRAGMA integrity_check` stops running at every launch on the family's device (`DIA-DL-008` debt 8, closed by the condition it set); a return from background stops putting the first-install transfer screen back over the parent's work; and `webContentsDebuggingEnabled` is shut (`DIA-DL-010` debt 12, closed). Mount `/m/v7/`, `CACHE_VERSION` v17 — the first bump of L3, forced because `/m/v6/` has been published since the L2 merge. One new signal (`store.close`), no new knob, no schema change. Two invariants, `FIU-P1-INV-001` and `FIU-P1-INV-002`, green on a **local** emulator (40 tests, 0 failures, `StoreLifecycleTest` 1/1, `WebInspectionTest` 2/2, `StoreEngineTest` 12/12) and both landing **Not yet observed on CI** — the `android-instrumented` dispatch that discharges them is owner-run. Decision `FIU-DL-001`. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).
  - **P2** — `fresh-install-self-sufficiency`: the in-app browser-transfer offer removed outright (owner act 2026-08-21), the app asking for the child itself on a first native launch, the diary's no-profile refusal offering the act that resolves it, and `DIA-DL-010` debts 10-11 (the `renderList` await with no `try/catch`, and the save the store ACCEPTED being reported as «Запись НЕ сохранена»). The plugin's own log tag moves inside the Capacitor gate, closing the exposure half of `DIA-DL-010` debt 13. No mount bump — `/m/v7/` is unpublished, edited in place, `CACHE_VERSION` stays v17. One new signal (`diary.list`), two kinds flipped to `emittedNow: false`, no new knob, no schema change. Two invariants, `FIU-P2-INV-001` and `FIU-P2-INV-002`, green locally (`behavior` 82, `contract` 1200, instrumented **41 tests, 0 failures**) and both landing **Not yet observed on CI**. Decision `FIU-DL-002`. **Landed this packet.**
  - **P3** — `header-install-and-policy-link`: the header's actions pushed to the edge they never reached on a phone (measured: 117 px of dead space at 412 px) and the download offer given a label and `<button>` semantics; a pre-install window stating what the app is and where the family's data lives, opened by that control; the PWA install prompt deleted **with** the `<link rel="manifest">` that made the browser's own offer possible (escalation E1, owner branch (i)); an in-app privacy-policy link behind a fail-closed `<meta>` declaration, with `https://theygrow.app/privacy` declared once in `CHANNEL_CONFIG`; the `[hidden]`-cascade guard `DIA-DL-008` designed as debt 9, built to that design and its bite measured; `FIU-DL-002` debt 19; and `FIU-DL-001` debt 14 in **both** halves — ✕ now means "read it", and the round «?» in the header makes the intro reachable again, which is what the link's one home needs. **No consent gate, by decision**; the web channel's analytics-consent surface stays owed. No mount bump — `/m/v7/` is unpublished, edited in place, `CACHE_VERSION` stays v17, forty-five modulepreload hints unchanged. **Three new knobs**, no new signal, no schema change; four GA4 events removed with the surface that emitted them and none added. Three invariants, `FIU-P3-INV-001`, `FIU-P3-INV-002` and `FIU-P3-INV-003`, green locally (`contract` 1219, `behavior` 93, `dom-desktop` 14, `dom-mobile` 16, `native` 33, instrumented **41 tests, 0 failures, 137.1 s**, `pytest` 243) and all landing **Not yet observed on CI**. Six DOM baselines re-blessed; the visual PNGs stay unblessed for the closing packet. Decision `FIU-DL-003`. **Landed this packet.**
  - **P4** — `diary-text-in-artifact-and-close`: the diary's scope bound at the query by AUTHOR rather than by area (the leak was reproducible on a seeded row and reached every layer, including the manifest's counts); the artefact format bumped to **v2** for that narrowing; `text/diary.txt` rendered to be read rather than parsed, which the PDF inherits; long values wrapped and long tokens broken after a 300-character line was measured running off the printed page; three false product statements about what the archive carries corrected, including the one inside the app; `FIU-DL-003` debt 20 closed by recording why `app/manifest.json` stays. Two invariants, `FIU-P4-INV-001` and `FIU-P4-INV-002`, both green locally (`pytest` **249**, the five non-visual parity projects **1378**, instrumented **42 tests, 0 failures, 105.3 s**) and both landing **Not yet observed on CI**. The 17-vs-20 counting note for the visual re-bless and everything else the owner still has to run is in `docs/RUNBOOK.md` **Closing a milestone**. Decision `FIU-DL-004`. **Landed this packet.**
  - **P5** — `print-layer-notdef-repair` (repair): the milestone's closing dispatch, run 32530473473, failed `android-instrumented` at the PDF/A step before the emulator started, so it discharged nothing; the cause was the print layer's own declared degradation substituting U+FFFD, which PT Sans does not cover, so the writer drew `.notdef` and the file stopped being PDF/A (clause 6.2.11.8). The mark is now `◊` U+25CA, whose coverage was measured; the artefact's promise amended to match and held there by a drift guard; a per-push guard over every content stream with an in-run arm-check, since the suite was green on a non-conformant file; and the CI step's failing branch made readable, tested against a recorded copy of that run's report. One new knob, no new signal, no schema change, no mount bump, no format bump, zero baselines. One invariant, `FIU-P5-INV-001`, green locally (`pytest app/tests/export` **58 passed**, 50 before; the five non-visual parity projects **1380**; the instrumented suite **42 tests, 0 failures, 99.5 s** with `ExportTransferTest` **3/3**, since the amended writer runs in the WebView at export time) and landing **Not yet observed on CI**. Decision `FIU-DL-005`. **Landed this packet.**

- **privacy policy and first L3 release (`PPR`) — Current, MERGED AND NOT PROMOTED (P1-P3 landed and merged as PR #27 @ `e68dc2a`; P4 is a repair on its own branch; the deploy, the promotion, the tag and the release remain owner acts).** *(Status corrected at PPR-P4: this line read "at its close (P1-P3 landed; the milestone PR opens now…)" and stayed there through the merge — the same staleness its own L3 sibling was corrected for one milestone earlier.)* The milestone that makes the product publishable: a privacy policy a parent can actually read, analytics that ask before they measure, and the declaration that turns the in-app link on. It exists because two Play preconditions and one accepted deviation all came due together — ADR-050 §5 needs a reachable non-PDF policy URL **and** an in-app link before the closed test can open, and `PDR-019`'s "GA4 without consent" was accepted for the dark-build period only. Branch `feat/privacy-policy-and-consent`, cut from `main` @ `cccff4b`. Decision ids are `PPR-DL-{NNN}` and invariants `PPR-P{k}-INV-{NNN}`.
  - **P1** — `privacy-policy-page`: the address the app had been told to link since `FIU-DL-003` got a document behind it. `/privacy` was already live and answering **200 with the app shell** — `app/nginx.conf` had one catch-all, so a skills table served under the word «конфиденциальность» with nothing anywhere to notice it by. An exact-match location plus its trailing-slash twin removes the shell fallback in both directions; the page is a committed hand conversion of `docs/privacy-policy-v1.0.md`, buildless by contract, paired to its source heading by heading and on both statements of the effective date. Carried in: the service-worker repair the second navigable page re-arms (`DIA-DL-005` (m)'s defect, `/privacy` being the second page to break the premise), the `NON_SHELL_PAGES` correspondence guard whose absence a comment had claimed as present, and one invariant leg narrowed by exactly one file and paid for with something stricter on it. Two paths added to the hygiene hooks' excludes, one of them **measured rather than foreseen**: `trailing-whitespace` stripped the owner document's Markdown hard line breaks on the hook's first run over it. No mount bump, no `CACHE_VERSION` bump, no baseline moved. One invariant, `PPR-P1-INV-001`. Decision `PPR-DL-001`. **Landed** @ `4523c25` / `f66edd1` / `8dc41fc`, with the controller named with the developer's legal name at `d1ab56f`.
  - **P2** — `consent-transfer-platform`: `/m/v8/` (`CACHE_VERSION` v17 → v18), and three things in it. **Analytics ask first**: nothing is fetched from `googletagmanager.com` and no GA4 configuration runs before an explicit yes, the banner offers refusal in one action of equal weight, and withdrawal lives in a «Cookie» control in the footer — which is what makes §5 of the policy true at the moment of the flip, and what ends `PDR-019`'s accepted deviation. **The browser-to-native history transfer is retired outright**, in both languages and on both sides of the ship list: the plugin, the intent-filter, the handoff page, the instrumented test and five specs are deleted, because the mechanism had no product caller and could not gain one. **The download offer became platform-honest**: a visitor whose platform cannot install an `.apk` is offered the honest sentence instead, failing closed on an unreadable platform. Two invariants, `PPR-P2-INV-001` and `PPR-P2-INV-002`. Four DOM baselines re-blessed; **16 of the 20 visual PNGs left red for the owner**, every changed region measured to the footer band rather than assumed. Decision `PPR-DL-002`. **Landed** @ `de411f0` / `88cc42a`.
  - **P3** — `declaration-flip-and-docs-tail`: the closing packet. `<meta name="theygrow-privacy-policy">` goes `none` → `published`, which is the whole of the runtime change and the last step of the fail-closed order — safe here without ceremony because the document and the declaration ship in the **same image**, so the gap that order protects against has no moment in which to open. The effective date was re-checked before the flip as `PPR-DL-001` (f) requires and moved `22.08.2026` → `23.08.2026` in all four places, publication having slipped a day. Two tests moved with the flip and neither was optional: the PPR-P1 tripwire that reds on exactly this change, and the withheld-state leg that would otherwise have **skipped itself** the moment the shipped token became `published`, retiring half of `FIU-P3-INV-002` silently and green. Then the docs tail: three invariants written, three amended, **the first two retirements in `INVARIANTS.md`'s history** (`DIA-P1-INV-001` and `DIA-P1-INV-002`, whose every enforcer P2 deleted), ten `Not yet observed on CI` fields discharged on run **32535042631**, the retired transfer fenced or corrected in fourteen passages of `docs/RUNBOOK.md` so no owner procedure instructs a step that cannot be taken, the `/privacy` promotion smoke added before the declaration is trusted, and this file. No mount bump, no `CACHE_VERSION` bump, no new knob, no new signal. Decision `PPR-DL-003`. **Landed this packet** (milestone PR opens at close, `AGENTS.md` §9).
  - **P4** — `privacy-redirect-absolute`: a **repair**, found by the milestone's own promotion smoke before a single user met it. `/privacy/` answered `301` with `location: http://<the tagged revision's own host>:8080/privacy` — nginx rewrites any `Location` beginning with `/` into `scheme://host[:port]` + URI unless `absolute_redirect` is off, and behind Cloud Run the scheme it knows is `http` and the port is its own unexposed `listen 8080`. The spelling a visitor is most likely to type by hand therefore landed on an address that does not answer, under the word «конфиденциальность». `absolute_redirect off;` at **server** scope closes the class rather than the address, and the config comment states what that does to every future `return 30x` in the file. **Why nothing was red:** `app/tests/server.js` had always answered that route with a relative `Location`, so the mirror was never wrong and no leg compared the two in FORM — the gap is closed by five static legs over `app/nginx.conf` (one of which builds its own failing configs in-run) plus one executed leg asserting the mirror's `Location` form, each labelled as scan or execution. Two `docs/RUNBOOK.md` step-3 smoke lines were repaired with it: the `<script>` count returned **1** on a clean page because it counted the page's own head comment, and the trailing-slash line accepted a `301` without reading where it pointed. `PPR-P1-INV-001` amended — Statement, Enforced by, and a Scope sentence that was **false** about what paired the mirror to production. No mount bump, no `CACHE_VERSION` bump, no new knob, no baseline moved; `app/tests/server.js` untouched. Decision `PPR-DL-004`. **Landed this packet**, on branch `fix/privacy-redirect-absolute`; whether the redirect now lands is the next tagged-revision smoke's to establish and nothing before it.
  - **What this milestone hands on.** The «Скачать APK» label on a desktop that cannot install one; `/privacy` with no in-page path back to the showcase and its four third-party policies still `<code>` rather than links, both editorial passes over the owner's document; `/privacy.html` reachable as a second address by decision; the 16 visual PNGs the owner re-blesses in the container; the effective date, which only the owner can re-check against the day they actually promote; and the counsel trigger on the policy's §2 — see `PPR-DL-003`.

- **code graph (`CGR`) — Done, one packet** (the packet that writes this line; the checkpoint and the PR are owner acts that follow it). Graphify adopted as a code-navigation tool for agent sessions: AST-only extraction over **this repository**, run by hand, consumed through the plain CLI. Repo-side definition site for vault **ADR-051**, and the packet that closes the two debts vault **ADR-049**'s 2026-08-24 annotation names — the `.gitignore` artifact rule (three bare filenames replaced by `graphify-out/`) and the Graphify regeneration rules missing from the `CLAUDE.md` section PR #29 landed. Documentation and ignore rules only: **no product or runtime file is touched**, no dependency enters the repository (the tool is a workstation install), and the graph itself is generated, never committed. Cross-cutting tooling track, **outside** the M1–M5 engine spine and outside the PDR-026 ladder; it sequences before nothing and blocks nothing. **Id split, on the standing precedent for standalone tracks (`GWN`, `SLA`, `VDK`, `ICON`, `XPT`):** decisions take `CGR-DL-{NNN}`; no invariant namespace is opened, because the packet's detector is a session-run count rather than a CI-observable property (`CGR-DL-001` d6). Branch: **owner-cut at checkpoint**, on the PR #29 precedent (`docs/adr-049-vault-clone-rules`); the name is `docs/adr-051-graphify-adoption`.
  - **P1** — `graphify-adoption`: `.gitignore` (the `graphify-out/` directory rule, replacing `graph.json` / `graph.html` / `GRAPH_REPORT.md`), `.graphifyignore` (new — the frozen module generations `app/m/v1…v7`, which were 323 of 556 scanned files and 48.5% of all graph nodes), `CLAUDE.md` (new `## Code graph (Graphify)` section, a sibling of `## Vault clone (read-only)` and never nested inside it), `docs/RUNBOOK.md` (new `## Agent tooling on this workstation` — the vault clone's operational reality, including the silent `GIT_ASKPASS` failure mode, plus the Graphify install/run/undo block), and this map. Decision `CGR-DL-001`. **Landed this packet.**
  - **P2** — `commit-gate-bootstrap`: `docs/RUNBOOK.md` gains `### The commit gate — install it once per clone` under `## Local dev`. The hook that runs `.pre-commit-config.yaml` lives in `.git/`, is not created by `git clone`, and until it exists a commit succeeds in silence with no hook output — which reads exactly like a green gate, and did: `CGR-DL-001` was committed without it. Documentation only; no id minted and no invariant opened, on the PR #29 precedent — materializing a vault rule mints no repo-side decision, and the detector here is procedural (vault contract §11 (2а), added 2026-08-24), not a CI-observable property of the tree. **Landed this packet.**

- **M5** — Closed-corpus family-memory chat. **Deferred** (ADR-017 — Б1 was sequenced ahead of M5; M5 is not naively "next"). **The next-track choice is a standing open owner decision** (contract §5/§6), taken in the decision layer at milestone close and recorded here as open rather than resolved — L1 closed without proposing a successor, as A2 and A3 did before it. The roadmap queues **A3b** (accounts / Google OAuth) after A3, and the PDR-026 ladder continues at **L2** after L1; opening either was a separate owner act. **That act has now been taken for one of them:** the PDR-026 ladder was opened at **L2 (`DIA`)**, which has since closed — *(corrected at DIA-P4: this read "which is the track marked **Current** above", and nothing is marked Current now)*. **L2 closes without proposing a successor**, as L1, A2 and A3 did before it, so the next-track choice is open again; what L2 leaves on the table for whoever answers it is the artefact work the 2026-08-17 owner gate addressed to **L3**, and the `closeStore()` item `DIA-DL-008` marks for a milestone's opening rather than for a debt list. *(Corrected at L3-P1, `FIU-DL-001`: that question has since been ANSWERED. The owner opened **L3 (`FIU`)** on 2026-08-18 out of the first live install, and it is the track marked Current above — it took up both things this sentence left on the table, the `closeStore()` item as its opening packet and the artefact work as its P4. A3b remains unopened and M5 remains where it was, so the rest of this paragraph stands.)* A3b remains unopened, and M5 remains where it was. The **EMV** track above was marked Current and closed without answering that question: it is a cross-cutting delivery/correctness track on top of the closed L1 milestone, not a successor in the spine, and it exists to make L1 promotable rather than to open what comes after it.

## How to update this file

Bump on every packet close: move the closed packet from **Current** to **Done @ <sha>**; advance the next packet to **Current**. At milestone close, mark the milestone **Done** and move the next milestone's first packet to **Current**.
